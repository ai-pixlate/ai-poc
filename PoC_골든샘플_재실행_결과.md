# PoC 골든 샘플 재실행 결과

> 계획 `PoC_골든샘플_재실행_계획.md`. 확정 기술 7종을 **골든 샘플 3상품 34장 → 섹션 102개**로 재실행한 결과.
> 12장·34장 표본으로 낸 기존 결과(`PoC_추가검증_결과.md`)는 건드리지 않음. 이 문서는 재실행분만 담음.
> 실행·1차 판정 2026-09-15 ~ 09-16. 등급 원본은 각 단계 `poc/golden/{단계}_{과업}/summary.md`.

⚠️ **판정 주체 — 전 단계 Claude 1차 판정. 예람님 검토 전임**(계획 8장 결정 #5). 아래 등급은 확정값이 아님.

---

## 0. 현황

| 단계 | 과업 | 게이트 | 결과 | 판정 |
|---|---|---|---|---|
| 1 | 텍스트 추출 | 섹션 A+B 70% (텍스트·bbox) | 텍스트 **98%** · bbox **99%** | 통과 |
| 2 | 줄·문단 병합 + 역할 분류 | 병합·역할 각 70% | 병합 **89%** · 역할 **94%** | 통과 — 단, 주의문구 미탐 22건 |
| 3 | 제품 라벨 판정 | **미탐 0** | 미탐 **0** · 오탐 **0** | 통과 |
| 4 | 브랜드 로고 제외 | 놓침·오탐이 기존(1·0)보다 늘지 않음 | 찾음 5 · **놓침 2** · 오탐 0 | **미달** |
| 5 | 원문 지우기 | 섹션 A+B 70% (사진 위 글자 포함) | `erase_all` **66%**(사진 54%) / `erase_s50` **72%**(사진 71%) | 확정 조건 **미달** · 하한 조건 통과 |
| 6 | 스타일 추출 | 색·크기·정렬 각 70% | 색 **96%** · 크기 **100%** · 정렬 **100%** | 통과 |
| 7 | 번역 길이 팽창률 | 통과선 없음 — 결론 유지 여부 | 잔여 초과율 **97%**(12장 94%) | 결론 유지 |
| 0 | (참고) 섹션 분해 | 계획 단계 0 — 완료분 재사용, 재실행 안 함 | 34장 → **섹션 102개**. 이번 재실행 전 구간의 입력 | 확정(`color_snap_vlm2`) |
| — | (참고) 제품컷 누끼 | 계획 밖 — 잠정 기술이라 재실행 대상 아님 | 2026-09-14~15에 **같은 골든 섹션 102개**로 이미 검증함 | 잠정(`vlm_pick`) |

**5종 통과 · 2종 미달**(단계 4 로고, 단계 5 확정 조건). 미달 2종은 원인이 특정됨 — 각 장 `개발 참고` 항목 참조.

**섹션 분해는 이번에 다시 돌리지 않음** — 2026-09-14에 골든 34장으로 종결한 결과를 그대로 입력으로 씀. 재실행 전 구간이 이 102섹션 위에 얹혀 있으므로, 부수적으로 확인된 것을 아래에 남김.

- 102섹션 중 **4,000px 초과가 1개**(4,491px) — OCR 검출기 상한에 걸려 단계 1에서 추가 분할함. 섹션 분해 입도가 OCR 상한과 맞지 않는 구간이 남아 있다는 뜻
- 단계 6에서 **글자가 위아래로 잘린 영역 2건**이 크기 과소 추정(실제 90px대 → 41·52px)으로 드러남. 확인 결과 둘 다 **원본 페이지 경계**(`016.jpg` 하단 `HOW` / `017.jpg` 상단)이고 섹션 절단선이 아님. 판매자가 나눠 올린 이미지 자체가 글자를 가른 것이라 **섹션 분해로는 해결 못 함**

제품컷 누끼는 확정 기술이 아니라 이 계획의 7단계에 들어가지 않으나, **검증 표본이 이번 재실행과 같은 골든 섹션 102개**임. 지출 $0.2482는 지출표에 **참고 행으로 따로 표시**하고 재실행 합계에는 넣지 않음. 상세는 `PoC_추가검증_결과.md` 7장.

### 규모·비용

| 항목 | 값 |
|---|---|
| 표본 | 3상품 34장 → 섹션 102개 · OCR 영역 2,281 · 블록 803 |
| 유료 단계 | 3종 — 단계 2 · 3 · 7. 전부 `gemini-3.8-flash` |
| **지출** | **$0.6438** (단계 2 $0.1761 · 3 $0.2171 · 7 $0.2506) — **장당 $0.0189** · 100만 화소당 $0.0046 |
| 화소 | 34장 140.7M — 장당 평균 4.14M · 최대 1000×37,665px |
| 로컬 소요 | OCR 28s · LaMa 590s(2조건) · 스타일 9s · 폭 측정 70s |
| API 소요 | 단계 2 473s · 3 440s · 7 466s |
| 표본 배수 | 기존 12장 대비 — 영역 8.1배 · 블록 7.3배 · 번역 세그먼트 17.2배 |

---

## 1. 텍스트 추출 — **통과**

### 결과

| 항목 | 값 |
|---|---|
| 조건 | `baseline` — PaddleOCR `PP-OCRv5_server_det` + `korean_PP-OCRv5_mobile_rec`, 전처리 없음 |
| 추가 분할 | 4,000px 초과 섹션에 **27번 규칙 적용** — 여백(std ≤ 6.0 · 40px 연속) 우선, 없으면 2,000px 띠 · 300px 겹침 |
| 분할된 섹션 | 1개(`A000000250199_014_003` 4,491px) → 여백 절단, 겹침 없음 |
| 영역 | 2,281 (섹션당 22.4) · 실행 오류 0 |
| 소요 | 인식 28.43s (섹션당 0.28s) · 비용 0 |

### 판정

| 텍스트 | bbox | 사진영역 텍스트 | 사진영역 bbox |
|---|---|---|---|
| **98/100 (98%)** | **99/100 (99%)** | 39/39 (100%) | 39/39 (100%) |

- 글자 없는 섹션 2개는 분모에서 뺌
- **사진·그라데이션 배경 위 글자 39섹션 전부 A+B** — 원문 지우기 입력으로서 문제 없음

### 확인된 것

- **분할이 축소보다 나음** — 분할 안 하면 검출기 4,000px 상한에 걸려 축소됨. 분할 후 참조(`tile_2000_ov300`) 일치율 91.9% → **94.6%**, 영역 35 → 36
- **여백 절단은 글자를 지나지 않음** — 이음선을 가로지르는 참조 글자 0건. 잘림·누락 0
- **저신뢰·빈 텍스트 영역 111개(4.9%)** — 빈 텍스트 40 · 신뢰도 0.5 미만 109(합집합 111). 전수 분류함
  - 실제 글자 90 — 대부분 **표 잔글씨·제품 인쇄·캡처 문서**. 검출은 맞고 인식만 실패한 것
  - 글자 아님 21 — 얼굴·신체·물방울·아이콘·제형. **원문 지우기에서 지워지면 안 되는 것**(단계 5로 이어짐)

---

## 2. 줄·문단 병합 + 역할 분류 — **통과**

### 결과

| variant | 조건 | 블록 | 토큰 in/out | 비용 | 소요 |
|---|---|---|---|---|---|
| `heuristic_v2` | `line_gap=0.7` `para_gap=0.6` `h_ratio=1.5` `overlap=0.5` `gutter=True` | 1,262 | — | 0 | 1.29s |
| **`llm_assist`** | `gemini-3.8-flash` · `temperature=0` · 분할 금지 | **803** | 140,305 / 18,890 | **$0.1761** | 472.63s |

- 응답 적용 실패 0
- LLM이 경계를 바꾼 섹션 75/102. 휴리스틱 겹침 220 → **17**, 이질 8 → 133(병합의 대가)

### 판정

| 축 | A | B | C | A+B |
|---|---|---|---|---|
| 병합 | 82 | 7 | 11 | **89/100 (89%)** |
| 역할 | 83 | 11 | 6 | **94/100 (94%)** |

- 12장은 병합 92% · 역할 92%였으나 **판정자가 달라(예람님 → Claude) 증감으로 읽지 말 것.** 읽을 것은 두 축 모두 게이트를 넘었다는 것
- 12장에 계수 규칙이 문서화되지 않아 **병합·역할 세는 법을 이번에 정함**(단계 2 `summary.md` 5장). 검토 필요

### 확인된 것

- **주의문구 미탐 22건 · 17섹션** — 정답이 주의문구인데 캡션 20 · 본문 9로 매김
  - 유형 ① **시험 조건 각주** — 시험기관·기간·대상·개인차 있음. 캡션으로 감
  - 유형 ② **경고 문장** — 이상 증상 시 상담·사용 중단·국소 테스트 권장. 본문으로 감
  - 5섹션은 이 사유로 등급을 덮어씀(미탐 1건 → 최대 B, 2건 이상 → C)
- **12장 한계가 해소됨** — 12장에는 가격 1 · 주의문구 4건뿐이었음. 골든에서 주의문구 40건 확보. **가격은 여전히 1건**(이벤트 기프트카드 3,000원)으로 미해소
- **역할 분포가 12장과 다름** — 12장은 캡션 80/110(73%)이었으나 골든은 캡션 386 · 제목 212 · 본문 164. 상품·페이지 종류가 늘며 분포가 달라짐

### 집계에서 뺀 것

- **광고 근거 각주 32건** — 랭킹 기준·특허번호·효과 한정 문구·출고량 기준. 주의문구 정의에 들어가는지 기준이 없어 세지 않고 비고에만 기록함
- 이를 주의문구로 보면 미탐이 22 → **최대 54건**. 역할 등급도 함께 내려감. **주의문구 범위를 개발 전에 확정할 것**

---

## 3. 제품 라벨 판정 — **통과**

### 결과

| 항목 | 값 |
|---|---|
| 조건 | `vlm_relation` — `gemini-3.8-flash` · `temperature=0` · **섹션 이미지 동봉**(긴 변 1024px) |
| bbox | **섹션 원본 좌표 그대로** — 확정 조건대로 축소 비율 환산 안 함 |
| 블록 · 라벨 판정 | 803 · **95 (12%)** |
| 토큰 · 비용 | in 216,415 · out 14,612 · **$0.2171** |
| 소요 | 440.18s · 응답 적용 실패 0 |

### 판정

| 미탐 | 오탐 | 정답 라벨 | 판정 섹션 |
|---|---|---|---|
| **0** | **0** | 95 | 102/102 |

- 12장(미탐 0 · 오탐 0)과 같음. **전수 판정에서도 무결**
- 판정 결과를 `truth.json`으로 저장해 **단계 2 역할 · 단계 5 마스크 · 단계 6 · 단계 7의 라벨 정답지**로 씀

### 확인된 것

- **긴 섹션 축소 위험이 현실화되지 않음** — 긴 변 1024px 고정이라 긴 섹션은 짧은 변이 좁아짐. 짧은 변 300px 미만 5섹션에서도 **미탐 0**
  - 189px까지 줄어든 `A000000250199_014_003`(4,491px 섹션)에서도 모델 사진 속 튜브·용기 라벨을 모두 잡음
- **컷 유형 전수** — 라벨 없음 66 · 단일 제품 16 · 여러 제품 11 · **겹친 패키지 8** · 캡처 문서 7 · 글자 없음 1
  - 12장에 부족했던 겹친 패키지 컷 8건 확보. 전부 정확

---

## 4. 브랜드 로고 제외 — **미달**

### 결과

| 블록 기준 | 찾음 | 놓침 | 오탐 | 종결 기준 |
|---|---|---|---|---|
| 기존 `heuristic_v2` 블록 | 6 | 1 | 0 | — |
| **이번 `llm_assist` 블록** | **5** | **2** | **0** | **미달** |

- 규칙 `block_exact`는 그대로 — 블록 텍스트 전체 == 브랜드명(NFKC · 소문자 · 공백·문장부호 제거)
- 비용 0 · 대조 0.022s. 브랜드명 통과 블록 12(라벨 소관 7 · 페이지 로고 5)

### 원인

**규칙이 아니라 입력 블록이 달라져서 놓침** — 단계 2를 `llm_assist`로 바꾼 부작용.

| # | 페이지 | 로고 | 기존 | 이번 | 품은 블록 텍스트 |
|---|---|---|---|---|---|
| 5 | A000000219554_002 | `goodal` | 찾음 | **놓침** | `Good all goodal` — LLM이 장식 문구와 로고를 한 블록으로 병합 |
| 7 | A000000250199_002 | `celimax×` | 놓침 | **놓침** | `celimax× OLIVE / YOUNG` — 콜라보 로고 2개가 한 줄. 기존에도 놓침 |

- 오탐은 0 유지. **놓침만 1건 늘어남**
- 로고가 다른 글자와 붙으면 완전 일치 규칙이 성립하지 않음. **로고 제외를 문자열 완전 일치로 두는 한 병합 결과에 종속됨**

### 개발 참고

로고 제외는 **병합 블록에 얹은 문자열 규칙**이라 병합 결과가 바뀌면 같이 흔들림. 구현 시 아래를 고려할 것.

| 방식 | 내용 | 특성 |
|---|---|---|
| **영역 단위 대조** | 블록 대신 OCR 영역에 규칙 적용 | 병합 앞 단계라 블록 경계 변화에 영향받지 않음 |
| **부분 일치** | 블록 텍스트에 브랜드명이 들어 있으면 해당 영역만 제외 | 놓침은 줄지만 브랜드명이 본문에 쓰인 경우 오탐 위험 |
| 현행 유지 | `block_exact` 그대로 | 놓친 로고는 번역 대상에 남아 검수에서 걸러짐. 오탐 0은 유지됨 |

- 놓친 2건은 **로고가 다른 글자와 한 블록으로 묶인 경우**뿐임. 단독 로고 5건은 전부 잡음
- `celimax×`는 콜라보 로고 2개가 한 줄에 붙은 것으로, **기존 방식으로도 못 잡던 유형**임

---

## 5. 원문 지우기 — **확정 조건 미달 · 하한 조건 통과**

### 결과

| variant | 조건 | 지우는 영역 | 지운 섹션 | LaMa 소요 |
|---|---|---|---|---|
| `erase_all` | 모든 OCR 영역 — **확정 조건** | 1,838 | 101 | 309.19s |
| `erase_s50` | 신뢰도 0.5 이상 · 텍스트 있는 영역 | 1,769 | 99 | 280.32s |

- 마스크 = poly 채움 + 글자 높이 15% 팽창(최소 3px). LaMa(iopaint) · CUDA
- **라벨 블록 95 · 로고 블록 5는 마스크에서 뺌**(영역 443개) — 단계 3·4 예측 사용

### 판정

| variant | 전체 | 사진영역 | 게이트 |
|---|---|---|---|
| `erase_all` | 67/101 **(66%)** | 19/35 **(54%)** | **X** |
| `erase_s50` | 71/99 **(72%)** | 22/31 **(71%)** | O |

- 12장 LaMa는 83%(10/12)였으나 **표본·판정 단위·판정자가 모두 다름**(12장 이미지 단위 · 예람님 → 101섹션 단위 · Claude). 두 수치를 빼서 성능 하락으로 읽지 말 것
- 이 판정에서 읽을 것은 **같은 표본·같은 판정자 아래 두 조건의 우열** — `erase_s50`이 전 지표에서 앞섬
- 두 조건이 갈린 섹션은 6개뿐. **차이는 작지만 게이트 선을 가로지름**

### 확인된 것 — 결과 문서 8장 ⑫의 실측

**글자 아닌 영역을 지우는가** — 단계 1에서 분류한 비텍스트 21개 기준.

| 구분 | 영역 | 섹션 |
|---|---|---|
| `erase_all`에서 지워짐 | **15** | 9 |
| `erase_s50`에서 지워짐 | **0** | 0 |
| 라벨·로고로 이미 제외됨 | 6 | — |
| `erase_s50`이면 **안 지워지는 실제 글자** | 54 | 8 |

- **얼굴·신체·물방울·아이콘이 `erase_all`에서 실제로 지워짐.** ⑫가 골든에서 재현됨
- 애초에 ⑫와 **신뢰도 0.5 하한은 제품컷 누끼 검증(2026-09-14, 같은 골든 섹션 102개)에서 나온 것**임. 그때는 지운 결과를 누끼 입력으로만 썼고 등급 판정은 하지 않았음 — 이번이 같은 조건을 판정으로 확인한 첫 실행임
- 맞교환 — `erase_s50`은 비텍스트를 지키는 대신 **잔글씨 54개를 남김**. 남는 글자는 표 잔글씨·캡처 문서라 번역 대상에서 빠짐

### 개발 참고

**두 조건의 차이는 신뢰도 하한 하나**이고, 그 하나로 게이트와 비텍스트 보호가 모두 갈림.

| 조건 | 게이트 | 얼굴·아이콘 보호 | 남는 글자 |
|---|---|---|---|
| `erase_all` | 66% · 사진 54% — 미달 | 15영역이 지워짐 | 없음 |
| **`erase_s50`** | 72% · 사진 71% — 통과 | 0 | 잔글씨 54개 |

- 구현 시 **신뢰도 하한을 파라미터로 두고 0.5를 기본값으로 둘 것.** 두 조건의 코드 차이가 마스크 필터 한 줄임
- `erase_s50`이 남기는 54개는 표 잔글씨·캡처 문서 안 글자로, **인식 결과 자체가 판독 불가라 번역 대상에서도 빠지는 것**임
- **마스크 면적으로는 실패를 예측 못 함** — `erase_all` 등급별 마스크 비율 중앙값이 A 18.0% · B 17.0% · C 17.2%로 갈리지 않음. 12장에서 나온 "면적 최대 2장이 C"는 골든에서 재현되지 않음. **면적 상한을 품질 예측 지표로 쓰지 말 것**

---

## 6. 스타일 추출 — **통과**

### 결과

| 항목 | 값 |
|---|---|
| 조건 | `otsu_border` — Otsu로 가르고 **테두리에 많이 닿는 쪽을 배경**으로 봄 |
| 크기 · 정렬 | em = bbox 높이 ×1.35 · 정렬 허용 오차 블록 폭 12% |
| 판정 대상 | **조판 대상 1,838영역** — 라벨 438 · 로고 5 제외 |
| 소요 | 9.44s · 비용 0 |

### 판정

| 색 | 크기 | 정렬 |
|---|---|---|
| **95/99 (96%)** | **99/99 (100%)** | **99/99 (100%)** |

- 12장은 색 91% · 크기 100% · 정렬 91%였음. **판정자가 달라 증감 자체는 근거로 못 쓰나**, 세 축 모두 게이트를 넘음
- 정렬 100%는 12장에서 문제였던 비교 단위(결과 문서 8장 ⑦)를 블록 단위로 고친 뒤의 첫 대규모 측정 결과임

### 확인된 것

- **색 실패 4섹션은 전부 반전** — 글자색과 배경색이 뒤바뀐 것. 유형이 12장 ⑧과 같음

| 섹션 | 반전 | 유형 |
|---|---|---|
| `A000000250199_009_007` | 4 | **인물 사진 위 흰 글자** — 머리카락·중간톤 피부를 글자로 잡음 |
| `A000000250199_008_004` | 5 | 논문 캡처 잔글씨 — 흰 바탕 회색 글자 |
| `A000000250199_002_001` | 2 | **검정 배지 위 흰 글자** — 배지 테두리에 끌림 |
| `A000000250199_001_003` | 2 | 영수증 캡처 — 빨간 강조 박스에 테두리 판정이 끌림 |

- **확인 항목 두 유형(사진 배경·배지 테두리)에서만 실패함.** 단색 배경 위 글자는 전부 정확
- 명도 대비 1.5 미만 조판 대상 47영역 — 대부분 연한 바탕 위 연회색 각주로, **원본 자체가 저대비**라 실패 아님
- 크기 B 2건은 **원본 페이지 경계에 잘린 글자**(`016.jpg` 하단 `HOW` / `017.jpg` 상단)라 bbox 높이가 실제 획 높이의 절반으로 잡힘. 섹션 절단선 탓이 아님 — **조판 시 경계에 걸친 영역은 크기를 이웃 줄에서 보정할 것**

---

## 7. 번역 길이 팽창률 — **결론 유지**

### 결과

| 항목 | 값 |
|---|---|
| 번역 | `gemini-3.8-flash` · 프롬프트 `default`(기존 전문 그대로) · 온도 0 |
| 입력 | 단계 2 블록 − 단계 3 라벨 − 단계 4 로고 − 한글 없는 블록 |
| 규모 | 호출 96건(섹션 단위) · **세그먼트 569** (12장 표본 33) |
| 토큰 · 비용 | in 117,115 · out 43,401 · **$0.2506** · 466.48s |
| 폭 측정 | Arial · 자간 0 · em = 줄 높이 ×1.35 · 로컬 70s · 비용 0 |

### 판정 — 기계 집계(통과선 없음)

| 조건 | 골든 | 12장 | 차 |
|---|---|---|---|
| `nowrap` 한 줄 가정 | 99% | 100% | -1p |
| `wrap_allowed` 줄바꿈 | 99% | 100% | -1p |
| **`wrap_and_shrink`** 줄바꿈 + 80% 축소 | **97%** | 94% | +4p |
| `wrap_grow_20` 줄바꿈 + 높이 2배 | 95% | 94% | +2p |

**결론 유지** — 표본을 17배로 늘려도 조판 조정만으로 흡수 불가. 오히려 3%p 악화됨.

### 확인된 것

- **폭 배율 중앙 2.27배** — 원문 자리의 2배가 넘게 필요함. 최대 14.48배(12장 4.33배)
- **역할로 갈리지 않음** — 캡션 98% · 제목 97% · 본문 97% · 주의문구 100%. 전 역할 97% 이상
- **긴 본문 섹션에서 더 나쁨** — 세그먼트 10개 이상 섹션 12개 합계 99%(195/197) > 전체 97%. 결과 문서 9장 한계 4가 남긴 "긴 본문에서 달라질 가능성"은 **반대 방향으로 나옴**
- 흡수된 15건은 전부 짧은 수치 문장·해시태그(`100% agreed moisture lasted long.` 등)

---

## 8. 기존 결과 대비 달라진 점

### 두 표본이 무엇인지

| | **12장 표본** | **골든 샘플** |
|---|---|---|
| 경로 | `data/images/1~12.jpg` | `data/golden_sample/images_{상품코드}/` |
| 정체 | 상세페이지에서 **잘라낸 조각** 12장. 사진·그라데이션 배경이 들어가도록 손으로 고름 | 실제 이커머스 상세페이지 **원본 전체** 3상품 34장 |
| 크기 | 1,000~1,200 × 649~1,280px · 총 13.5M화소 · 장당 평균 **1.13M화소** | 800~1,000 × 220~**37,665**px · 총 140.7M화소 · 장당 평균 **4.14M화소** |
| 처리 단위 | 이미지 1장 = 1단위 | 섹션 102개(34장을 `color_snap_vlm2`로 분해) |
| 규모 | OCR 영역 280 · 블록 110 | OCR 영역 2,281 · 블록 803 |
| 성격 | 조각이라 한 장에 한두 장면. **긴 본문·이벤트 안내·캡처 문서가 없음** | 상품 페이지 전체. 시험 각주·규제 문구·캡처 문서·겹친 패키지가 모두 들어옴 |

- 화소 **10.4배** · 영역 **8.1배** · 블록 **7.3배**. 장당 화소로도 3.7배
- 12장은 `PoC_추가검증_결과.md`·`PoC_검증_계획_및_기록.md`의 기준 표본이고, **골든 샘플과 겹치는 이미지가 없음**

### 과업별

| 과업 | 기존 표본 · 결과 | 골든 | 해석 |
|---|---|---|---|
| 텍스트 추출 | 12장 · **등급 미기록**(2026-08-20 육안 총평만) | 텍스트 98% · bbox 99% | 비교 대상이 없음. **기준선이 처음 기록됨** |
| 줄·문단 병합 | 12장 · 110블록 — 92% | 89% | 게이트 유지. 예람님 판정과 Claude 판정이라 3%p 차는 근거로 못 씀 |
| 역할 분류 | 12장 · 110블록 — 92% | 94% | 게이트 유지. 단, **주의문구 미탐이 표본 확대로 드러남** |
| 제품 라벨 | 12장 · 110블록(라벨 60) — 미탐 0 · 오탐 0 | 미탐 0 · 오탐 0 | 등급이 아니라 건수라 판정자 차이의 영향이 적음. 겹친 패키지·축소 위험 모두 통과 |
| 브랜드 로고 | **골든 34장**(같은 표본) · `heuristic_v2` 블록 — 놓침 1 | **놓침 2** | 양쪽 기계 대조라 **직접 비교 가능. 악화** — 표본이 아니라 병합 단계 교체의 부작용 |
| 원문 지우기 | 12장 · **이미지** 단위 · 예람님 판정 — 83%(10/12) | 101**섹션** 단위 · Claude 판정 — **66%** | **표본·판정 단위·판정자가 모두 다름.** 17%p를 성능 하락으로 읽으면 안 됨 |
| 스타일 추출 | 12장 중 11장 · 조판 대상 110영역 — 색 91% · 정렬 91% | 색 96% · 정렬 100% | 게이트 유지. 정렬은 버그 수정이라는 상승 요인이 따로 있음 |
| 번역 팽창률 | 12장 중 **`block` 입력 10장** · 33세그먼트 — 초과 94% | 초과 97%(569세그먼트) | 양쪽 기계 집계라 **직접 비교 가능. 결론 유지** |

**기존 등급은 예람님이 직접 매긴 것이고, 골든 등급은 Claude 1차 판정임(예람님 검토 전).**

- **잣대가 같은 것은 브랜드 로고·번역 팽창률 두 과업뿐임** — 양쪽 다 사람 판정 없는 기계 산출. 나머지 6개는 판정자가 달라 **증감이 아니라 게이트 통과 여부만 읽을 것**
- 원문 지우기는 판정 단위까지 다름 — 기존은 이미지 12장을 통으로 봤고, 골든은 섹션 101개를 하나씩 봤음. **섹션 단위가 더 촘촘해 한 곳의 얼룩이 그대로 등급에 드러남**
- 브랜드 로고는 **표본도 판정 방식도 같음** — 기존 검증도 골든 34장 전수 기계 대조였음. 달라진 것은 대조에 쓴 블록(`heuristic_v2` → `llm_assist`)뿐이라 **악화 원인이 특정됨**
- 스타일 추출 12장 판정은 11장 기준 — 조판 대상이 0인 `3.jpg`를 분모에서 뺐음
- 번역 팽창률 12장 판정은 10장 기준 — `block` 입력에 번역 대상 블록이 있는 이미지만 셈

---

## 9. 검증 중 확인된 사실

### ① OCR 검출기는 글자가 아닌 것도 잡음 — 인식이 빈 텍스트로 답할 뿐

- 빈 텍스트 40개 중 **28개는 실제 글자**(흐린 종이 17 · 인증서 4 등) — 검출은 맞고 인식만 실패
- 나머지 12개가 얼굴·물방울·아이콘. **빈 텍스트를 "글자 아님"으로 읽으면 안 됨**

### ② 비텍스트 오검출이 원문 지우기로 그대로 전파됨

- ⑫(OCR 오검출 박스를 그대로 지우면 얼굴이 지워짐)가 골든 102섹션에서 **15영역 · 9섹션**으로 재현됨
- 신뢰도 0.5 하한 하나로 **전부 차단됨**(15 → 0). 하한의 효과가 처음 정량화됨
- 이 현상은 제품컷 누끼 검증에서 먼저 관측됐고(같은 골든 섹션 102개), **원문 지우기 판정으로 확인된 것은 이번이 처음**임

### ③ 블록 단위 교체는 후속 문자열 규칙을 깨뜨림

- 로고 제외는 `block_exact` 완전 일치라 **블록 경계가 바뀌면 바로 놓침**이 됨
- 병합 품질(89%)과 무관하게, 병합이 더 크게 묶을수록 완전 일치 규칙에는 불리함

### ④ 주의문구는 표본을 늘려야 드러남

- 12장 주의문구 4건 → 골든 40건. 그제서야 **미탐 22건**이 보임
- 시험 조건 각주(캡션으로 감)와 경고 문장(본문으로 감)은 **서로 다른 실패 유형**임

### ⑤ 긴 섹션의 VLM 입력 축소는 라벨 판정에 영향 없었음

- 짧은 변 189px까지 줄어든 섹션에서도 미탐 0. **계획 위험 항목이 해소됨**
- 확정 조건이 bbox를 환산하지 않고 원본 좌표로 두는 구조라 **좌표는 실질적으로 쓰이지 않음** — 판정이 이미지에만 의존함(결과 문서 8장 ⑤와 일치)

### ⑥ 번역 길이 문제는 긴 본문에서 완화되지 않음

- 세그먼트 10개 이상 섹션에서 오히려 99%. **길이 압축 없이는 조판 흡수 불가**라는 기존 결론이 강화됨

---

## 10. 재현

```bash
# 단계 1 텍스트 추출
python poc/B_ocr/run.py --sample golden && python poc/B_ocr/compare.py --sample golden

# 단계 2 줄·문단 병합 + 역할 분류
python poc/block_role/run.py --sample golden
python poc/block_role/run_llm.py --sample golden
python poc/block_role/compare.py --sample golden

# 단계 3 제품 라벨 판정
python poc/product_label/run_llm.py --sample golden --variant vlm_relation
python poc/product_label/compare.py --sample golden

# 단계 4 브랜드 로고 제외
python poc/logo_match/run_text.py --sample golden && python poc/logo_match/compare.py --sample golden

# 단계 5 원문 지우기
python poc/E1_inpaint/run_golden.py && python poc/E1_inpaint/compare_golden.py

# 단계 6 스타일 추출
python poc/style_extract/run_golden.py && python poc/style_extract/compare_golden.py

# 단계 7 번역 길이 팽창률 (유료)
python poc/length_expansion/run_llm.py --input golden --dry-run
python poc/length_expansion/run_llm.py --input golden
python poc/length_expansion/run_golden.py --variant all
python poc/length_expansion/compare_golden.py

# 결과 이동 — 실행 뒤 매번
python poc/golden/move.py {단계}
```

- 출력 경로는 과업 폴더 `results/golden/`. `move.py`가 `poc/golden/{단계}_{과업}/`로 옮김
- 재실행은 `move.py {단계} --restore` 후 진행 — 채운 등급을 compare가 원위치 판정표에서 회수함
- API 응답은 과업 폴더 `cache/`에 남음. 재실행해도 캐시 우선이라 비용 0

---

## 11. 지출

| 단계 | 모델 | 토큰 in / out | 실측 | 장당(34장) | 섹션당(102) |
|---|---|---|---|---|---|
| 2 — `llm_assist` | `gemini-3.8-flash` | 140,305 / 18,890 | **$0.1761** | $0.0052 | $0.0017 |
| 3 — `vlm_relation` | `gemini-3.8-flash` | 216,415 / 14,612 | **$0.2171** | $0.0064 | $0.0021 |
| 7 — 번역 | `gemini-3.8-flash` | 117,115 / 43,401 | **$0.2506** | $0.0074 | $0.0025 |
| **재실행 합계** | | 473,835 / 76,903 | **$0.6438** | **$0.0189** | **$0.0063** |
| (참고) 누끼 `vlm_pick` | `gemini-3.8-flash` | 161,000 / 6,900 | $0.1467 | $0.0043 | $0.0014 |
| (참고) 누끼 `sam_pick` | `gemini-3.8-flash` | — | $0.1015 | $0.0030 | $0.0010 |
| **(참고) 누끼 소계** | | | **$0.2482** | **$0.0073** | **$0.0024** |

- 단가 입력 $0.75 / 출력 $3.75 per 1M 토큰. 단계 1 · 4 · 5 · 6은 로컬 실행이라 비용 0
- 세 단계 모두 **드라이런으로 건수·비용을 먼저 보고하고 승인받은 뒤 호출함**(CLAUDE.md 규칙)
- **누끼는 계획 밖이라 재실행 합계에 넣지 않음**(2026-09-14~15 실행 · 같은 골든 섹션 102개). 합치면 **$0.8920 · 장당 $0.0262**. `sam_pick`은 잠정안에 들어가지 않은 탈락 조건임

### 이미지 1장 기준

| 항목 | 골든 34장 | 12장 표본 |
|---|---|---|
| 총 화소 | 140.7M | 13.5M |
| **장당 평균 화소** | **4.14M** (중앙 2.04M · 최소 0.18M · 최대 **37.66M**) | **1.13M** (중앙 1.00M · 최소 0.70M · 최대 1.54M) |
| 장당 크기 | 800~1,000 × 220~37,665px | 1,000~1,200 × 649~1,280px |
| 유료 3단계 합계 | $0.6438 | $0.0639 |
| **장당 비용** | **$0.0189** | **$0.0053** |
| 100만 화소당 비용 | **$0.0046** | $0.0047 |

- **장당 비용 3.6배는 장당 화소 3.7배와 거의 같음.** 화소당 비용이 두 표본에서 $0.0046 · $0.0047로 사실상 동일
- 곧 **비용은 이미지 장수가 아니라 총 화소(=글자량)에 비례함.** 세로로 긴 상세페이지일수록 장당 비용이 커짐
- 12장 표본 비용은 채택 조건만 합산한 것 — 병합 $0.0231 · 라벨 `vlm_relation` $0.0270 · 번역 `block` $0.0138(10장 호출)
- 상세페이지 1장(평균 4.14M화소) 처리에 **약 $0.019 = 약 27원**(1,400원/$ 기준). 34장 한 세트 약 900원
