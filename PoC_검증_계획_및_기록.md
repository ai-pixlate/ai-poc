# PoC 검증 계획 및 기록

> 이 문서의 위치: Notion/마크다운으로 "진행 순서 · 방법 · 결과"를 한 곳에 기록하는 작업 로그. 후보 기술을 먼저 선별해 각각 테스트하고, 우선순위 높은 것부터 검증하며, 최종 채택은 결과물을 놓고 팀이 정성적으로 논의해 결정한다.
> ⚠️ "판정 기준"란 중 [확정] 표시가 없는 항목은 제안(가정)임 — 개발계획문서에 명시되지 않았으므로 팀 논의로 확정 필요.
> **평가 방식 [확정]**: 정량 지표(CER·IoU)는 산출하지 않음. 전 과업을 육안 A/B/C 등급으로 판정. 절대 성능 수치가 남지 않으므로 최종 채택은 팀 논의로 결정.

---

## 0. 검증 대상 전체 개요

MVP 파이프라인 과업(B·E1·C)에 더해, MVP 제외 대상인 F(배경 가공)·A(초안 배치)도 PoC 범위에 포함한다. D(규제 판단)는 C에 통합되어 별도 기술 검증 대상이 아니다(6.2). E2(번역문 배치)는 기술이 확정되어 PoC 대상에서 제외한다.

| 우선순위 | 과업 | 후보 기술 | 
|---|---|---|---|
| **1순위** | B. 텍스트 추출 | **PaddleOCR** (실질 검증 대상은 전처리 기법 선택) | 
| **2순위** | E1. 원문 지우기 | LaMa / OpenCV 고전 인페인팅 / SD 인페인팅 | 
| **3순위** | C. 로컬라이징 번역 (D 통합) | Claude / GPT / Gemini / Qwen | 
| **후순위 (여력 시)** | F. 배경 가공 | E1 인페인팅 재활용 + OpenCV 색보정 | 
| **후순위 (여력 시)** | A. 초안 배치/리플로우 | MLLM 레이아웃 생성 / 규칙 기반 + 조각 분류 | 

**PoC 진행 중 추가된 과업** — 개발계획문서에 없음. 위 우선순위 체계 밖이다.

| 착수 | 과업 | 후보 기술 |
|---|---|---|
| **즉시 (2026-08-20)** | **G. 제품컷 누끼** | rembg / transparent-background / BiRefNet / SAM 2 / GrabCut(대조군) |



---

## 1. 실행 주체 — 누가 직접 돌리는가

기술마다 필요한 리소스(GPU, API 키)가 달라 실행 주체를 미리 나눠둔다. 착오 방지용.

> **갱신 2026-08-20**: 원안은 "Claude 환경엔 GPU 없음"을 전제로 GPU 과업을 전부 예람님께 배정했으나, Claude Code가 예람님 PC에서 직접 구동되므로 **로컬 GPU를 그대로 쓸 수 있음**이 확인됨. 로컬 GPU 과업을 Claude로 이관한다.
>
> 실행 환경: RTX 4060 Laptop **VRAM 8GB**, CUDA 12.5(드라이버 555.97), Python 3.12.10, Windows 11. 가상환경 `.venv/`.
> 학교 GPU 클라우드는 8GB로 부족할 때만 쓴다.

| 기술 | 실행 주체 | 이유 |
|---|---|---|
| PaddleOCR | **Claude** (로컬 GPU) | 8GB로 충분. 12장 3.5초. **2026-08-20 실행 완료** |
| PaddleOCR-VL | **Claude** (로컬 GPU) | VRAM 7.3/8.1GB로 빠듯하게 적재됨. 12장 17분. **2026-08-20 실행 완료** |
| 이미지 전처리(OpenCV) | **Claude** | CPU만으로 가능 |
| 상용 OCR 참조 측정 (Clova 또는 Google Vision) | **예람님** (API 키 보유) | 키 보유자가 예람님. `.env`로 전달 시 Claude 실행 가능하나 **유료라 사전 비용 확인 필수** |
| LaMa | **Claude** (로컬 GPU) | 모델이 작아 8GB에 여유. 학교 클라우드 불필요 |
| OpenCV 고전 인페인팅 | **Claude** | GPU·API 키 불필요 |
| SD 인페인팅 | **Claude** (착수 시 재검토) | SD1.5 계열은 8GB에 여유, SDXL은 빠듯함. 백업 후보라 착수 시점 미정 |
| LLM 벤치마크 (C) | **예람님** (각 벤더 API 키) | 키 보유자가 예람님. `.env` 전달 시 Claude 실행 가능하나 **유료라 사전 비용 확인 필수** |
| F — LaMa 재적용 | **Claude** (로컬 GPU) | E1과 동일 환경 |
| A — MLLM 레이아웃 | _미정_ | 사용할 MLLM 미정이라 실행 환경도 미정. 로컬 8GB로 가능한 모델이면 Claude |
| G — 누끼 라이브러리 | **Claude** (로컬) | 전부 로컬 실행, **API 비용 0**. rembg·transparent-background는 CPU로도 구동, BiRefNet·SAM 2는 GPU 유리 |
| E2 렌더링 | **프론트·백엔드 담당** | 기술 확정됨. AI 담당 소관 아님 (→ 2.4) |

**VRAM 8GB 제약** — 현재 유일하게 걸리는 지점은 PaddleOCR-VL(7.3GB 점유). 배치 처리나 더 큰 입력은 OOM 위험이 있어 1장씩 순차 처리 중. E1·F 후보는 모두 여유 있음.

---

## 2. 과업별 상세

### 2.1 B. 텍스트 추출 (1순위)

**목적**: E1·E2가 쓸 bbox·글자색·배경유형을 정확히 뽑아내기. 엔진은 PaddleOCR로 고정

**후보**: PaddleOCR 단독. 실질 검증 대상은 어떤 전처리를 적용할지(2.1.2)

- 상용 API(Clova/Google Vision)는 과금 문제로 채택 후보에서 제외. 단 참조 기준선으로 1종만 소량 측정(2.1.1)
- PaddleOCR-VL: 같은 라이브러리 내 모델 교체이므로 2.1.2 1단계에 포함
- EasyOCR·Tesseract: 한국어 성능 열위로 제외

**판정 기준**

*텍스트 인식*

| 등급 | 기준 |
|---|---|
| A | 텍스트 대부분 정확, 오탈자 거의 없음 — 그대로 번역에 넘겨도 됨 |
| B | 일부 오탈자 있으나 1차 검수로 고칠 수 있는 수준 |
| C | 오탈자 과다하거나 인식 실패 — 사용 불가 |

*bbox 정밀도* (시각화 이미지로 판정)

| 등급 | 기준 |
|---|---|
| A | 텍스트 영역을 정확히 감쌈 |
| B | 약간 어긋나지만 마스크 확장으로 흡수 가능 |
| C | 크게 빗나가거나 영역 누락 |

*사진 위 글자 처리*: 위 두 등급을 **사진·그라데이션 배경 위 글자에 한정해 다시 한 번** 매긴다. 해당 영역이 없는 이미지는 `-`

**판정 단위 [확정 2026-08-20]**: B는 `이미지 × variant` 한 행. 영역 단위가 아니다.

- 근거: 표본 12장 × variant 3종 × 이미지당 영역 20~60개 = 수백 행. 육안 판정이 불가능한 분량임
- 대신 한 행에 등급 4개를 매긴다 — `텍스트` / `bbox` / `사진영역 텍스트` / `사진영역 bbox`
- 배경유형을 이미지 단위로 분류할 수 없다는 문제(한 장에 단색·사진 혼재)는 뒤 두 열이 흡수한다. 사진 구간만 따로 보는 등급이라 파일 분류가 필요 없음
- **E1은 영역 단위로 판정한다**(2.2). 영역마다 지운 결과가 따로 나오므로 그쪽은 자연스러움. B와 E1의 판정 단위가 다른 것은 의도된 것

**판정표 위치**: `poc/B_ocr/summary.md` 2장. `compare.py`가 빈 칸으로 생성하며, 각 행에 bbox 시각화 링크가 붙는다. 재실행 시 덮어쓰므로 **채운 등급은 이 문서로 옮겨 적을 것**

---

#### 2.1.1 참조 기준선 측정 (상용 API) — **생략 [2026-08-20 / 예람님]**

**생략 근거**: 원 목적이 "PaddleOCR 결과의 절대 수준을 판단할 기준점 확보"였으나, 1단계에서 PaddleOCR 채택이 확정되어 비교 대상이 없어짐. 유료 호출이므로 **집행 비용 0**.

원안: 상용 API 1종(Clova 또는 Google Vision 중 택1), 테스트 이미지 10~15장 1회 호출.

> 재개 조건: 2.1.2 "재검토 조건"이 발동해 PaddleOCR 품질을 다시 따지게 될 때. 그 시점에 유사정답 용도로 쓰면 라벨링 없이 variant 간 상대 비교가 가능함.

---

#### 2.1.2 전처리 방법 비교

**목적**: PaddleOCR 정확도 미흡의 원인 분리 + 채택할 전처리 결정
**방식**: 단계적 게이트. 앞 단계에서 목표에 도달하면 **뒤 단계는 생략**

**진행 규칙**
- 각 단계 종료 시 **이미지** 기준 A+B 비율이 **70% 이상**이면 다음 단계 생략하고 종료
- 미달 시에만 다음 단계 진행
- 집계는 variant별로 따로 낸다. 분모는 표본 이미지 수(현재 12)

> 집계 단위 [확정 2026-08-20]: B는 이미지 단위, E1은 영역 단위. 판정 단위가 다르므로 두 과업의 70%를 직접 비교하지 말 것 — 각자의 게이트로만 쓴다.

**1단계 — 모델·파라미터**

| 항목 | variant | 판정 여부 | 텍스트 | bbox | 비고 |
|---|---|---|---|---|---|
| baseline | `baseline` | **판정 대상** | | | 비교 기준점 |
| det_limit_side_len | `det_side_1280` | 제외 | — | — | baseline과 인식 결과 **완전 동일**. 아래 근거 |
| PaddleOCR-VL 교체 | `vl_spotting` | 제외 | — | — | **속도** — 12장 1042초, baseline의 약 300배 |

**제외 결정 [2026-08-20 / 예람님]**

- `det_side_1280` — 12장 전부 검출 영역 수·인식 텍스트가 baseline과 일치. 판정해도 같은 등급이 나오므로 생략. 원인은 3장 실측 기록 참조(인식이 축소본이 아니라 원본에서 크롭되는 구조)
- `vl_spotting` — 품질과 무관하게 속도로 탈락. 실행 결과는 `poc/B_ocr/results/vl_spotting/`에 근거로 보존하고, `summary.md` 3장에서 baseline 판정 시 참고용으로 볼 수 있게 남겨둠

**진행 방침 [2026-08-20 / 예람님]**: baseline 판정 결과 70% 미달이면 **2단계 이미지 전처리로 진행**한다. 1단계에서 추가 모델 실험을 하지 않는다.

**한국어 인식 모델 상향 불가 — 실측 확인**

`PP-OCRv5_server_rec`(server 등급)을 `1.jpg`에 직접 적용한 결과:

| 대상 | baseline (`korean_PP-OCRv5_mobile_rec`) | `PP-OCRv5_server_rec` |
|---|---|---|
| 한글 (`키캡 키링`·`선착순!`·`화잘먹 모공 세럼`) | 정상 인식 | **빈 문자열, score 0.00** |
| 라틴 (`Biodance` ×2) | `Biodan` / `Buodance` — 둘 다 오탈자 | **둘 다 정확** |

- server 등급 인식 모델은 `PP-OCRv4_server_rec`·`PP-OCRv5_server_rec` 등 3종뿐이며 **전부 중국어·영어·일본어용**. 한국어·아랍어·태국어 등 언어별 모델은 예외 없이 `*_mobile_rec`
- 따라서 **단일 모델 교체로 한국어 인식을 상향할 방법은 없음**. 검출은 server(`PP-OCRv5_server_det`), 인식만 mobile인 비대칭은 구조상 불가피

**보류 카드 — 인식 모델 병행 (`rec_hybrid`)**

표본이 한글·라틴 혼재라 두 인식 모델을 함께 돌려 영역별로 고르는 조합이 성립한다. 관측된 오탈자 2건이 모두 브랜드명(라틴)이었고 server_rec이 그것을 정확히 읽었으므로 개선 여지가 있다.

- 이미지를 건드리지 않으므로 **1단계 성격의 수단**임
- 위 진행 방침에 따라 **이번에는 쓰지 않는다.** 2단계 전처리도 미달일 경우 재검토 대상으로 남김

> **1단계 결과 [확정 2026-08-20 / 예람님]: baseline 통과 — PaddleOCR 기본 설정 채택.**
>
> 근거: 예람님이 시각화·인식 텍스트를 육안 확인한 결과 **대부분 A 수준**으로, 70% 게이트를 여유 있게 넘는다고 판단. 2단계 전처리로 진행하지 않는다.
>
> ⚠️ **개별 등급은 기록되지 않음.** `poc/B_ocr/summary.md` 판정표 12행은 빈 칸 상태이며, 남아 있는 근거는 위 총평뿐이다. 다음 두 경우에 기준선이 없어 곤란해질 수 있다.
> - 팀 논의에서 채택 근거를 되짚을 때
> - 나중에 전처리를 붙였을 때 **개선 여부를 비교할 baseline 등급이 없음**
>
> → 전처리를 실제로 착수하게 되면 그 시점에 baseline 등급도 함께 매겨 둘 것.

**2단계 — 이미지 전처리** (1단계 미달 시)

| 기법 | 기대 효과 | 텍스트 | bbox | 비고 |
|---|---|---|---|---|
| 업스케일링 | 작은 텍스트 인식률 개선 | | | 단순 리사이즈부터 |
| bbox 크롭 후 재인식 | detection은 되는데 recognition만 틀릴 때 | | | 2단계 처리라 복잡도 증가 |
| CLAHE(대비 강화) | 흐릿한 글자 개선 | | | ⚠️ 타 사례 효과 미미 — 실측 필요 |

제외 기법 (필요 시 재검토)
- 노이즈 제거·샤프닝: 상세페이지 원본 화질이 대체로 양호해 실익 낮음
- 이진화: 사진/그라데이션 배경에 적용 불가(글자 소실 위험). 단색 배경은 이미 인식 양호
- 기울기 보정: 상세페이지는 대부분 정방향

> 2단계 결과: **미실시** — 1단계에서 게이트를 통과해 생략(진행 규칙대로). 아래 "재검토 조건"에 걸리면 착수.

**3단계 — 배경 유형별 분기** (2단계 미달 시) — **폐기 (2026-08-20)**

- 원안: `background_type` 라벨로 전처리를 유형별 분기 (단색 → 이진화·대비 강화 / 사진·그라데이션 → 크롭·업스케일)
- **폐기 근거**: 이미지 단위 사전 분류가 전제였으나 표본이 한 장 안에 단색·사진 구간을 함께 갖고 있어 분류 불가. 배경유형은 OCR 실행 **후** 육안 판정 시점에 영역 단위로 확정되므로 전처리 분기 입력으로 쓸 수 없음
- 원안에 이미 "실제 파이프라인은 OCR이 먼저라 순서가 반대"라는 경고가 달려 있었고, 그 문제가 PoC 단계에서 그대로 드러난 것

> 2단계까지 70% 미달 시 대안: _미정_ — 해당 시점에 팀 논의

**결론 [확정 2026-08-20 / 예람님]**

| 항목 | 결정 |
|---|---|
| 엔진 | **PaddleOCR** |
| 모델 | `PP-OCRv5_server_det` + `korean_PP-OCRv5_mobile_rec` (기본 조합) |
| 전처리 | **적용 안 함** |
| 근거 | 1단계 baseline이 육안 기준 대부분 A. 전처리 없이 충분하다고 판단 |

실행 설정은 `poc/B_ocr/run.py`의 `VARIANTS["baseline"]`이 그대로 채택 사양이다.

**재검토 조건** — 아래 중 하나라도 발생하면 2단계 전처리를 다시 꺼낸다.

- E1이 B의 bbox를 마스크로 썼을 때 품질이 안 나옴 (bbox 정밀도가 실제로는 부족했던 경우)
- C 번역 단계에서 오탈자가 번역 품질을 떨어뜨리는 게 확인됨
- 표본을 늘렸더니 지금 12장에서 안 보이던 실패 유형이 나옴
- 세로로 긴 실제 상세페이지 원본(현재 표본은 조각 단위)에서 결과가 달라짐

보류 카드 2종은 그때 함께 검토: 2단계 전처리 3종(업스케일·크롭 재인식·CLAHE)과 `rec_hybrid`(라틴 오탈자용).

---

### 2.2 E1. 원문 지우기 (2순위)

**목적**: 사진/그라데이션 배경 위 한국어 글자 제거가 기술적으로 가능한지 검증. 안 되면 MVP를 단색 배경 위주로 축소(9월 이전 결정, 문서 4.3·5.1).

**후보 및 순서**
1. LaMa — MVP 주력 후보 (문서 6.4에 이미 지정)
2. OpenCV 고전 인페인팅(Telea/NS) — 단색 배경용 baseline
3. SD 인페인팅 — LaMa 품질 부족 시 백업

**참고 구현** (재현 출발점)
- AnyTrans 공식 구현: `github.com/qzp2018/AnyTrans` — 저자가 "간단한 프레임워크만 제공, LLM·지우기 도구는 필요에 따라 추가"라고 명시. 구조 참고용에 가까움
- image-translator: `github.com/saikoneru/image-translator` — OCR→레이아웃→세그멘테이션→인페인팅→번역→렌더링이 Docker 마이크로서비스로 분리, E1만 떼어 실행하기 편함 (단, 레이아웃 워커가 HiSAM 체크포인트 별도 다운로드 필요)

**테스트 데이터**: 소수 수동 확보 — 총 10~15장, `data/test_images/` 평면 배치. B와 동일 표본을 쓴다.

- **수동 라벨링을 하지 않는다 (2026-08-20 결정).** 육안 판정이라 정답 데이터가 불필요
- 인페인팅 마스크는 B(OCR)의 bbox 출력에서 생성한다. 실제 파이프라인 순서와 일치
- ⚠️ B가 C등급이면 마스크도 함께 나빠져 E1 단독 성능을 분리할 수 없음. 발생 시 E1 표본 한정으로 bbox를 손으로 적은 JSON을 만들어 대체 (도구 불필요)
- 표본 선정 기준: 사진·그라데이션 **영역**이 충분히 포함되도록 고른다. 실행 후 영역 집계에서 photo 수가 부족하면 이미지 추가

**마스크 팽창 폭 — 실서비스에 규칙으로 둬야 할 항목 [2026-08-20 발견]**

B의 poly는 글자에 거의 딱 붙는다. 그대로 지우면 테두리 안티에일리어싱이 얼룩으로 남고, 넓히면 배경까지 뭉갠다. **이 값은 상수로 못 박을 수 없다.**

실측 (팽창 = 글자 높이 × 비율, 최소 3px)

| 이미지 | 영역 수 | d15 마스크 면적 | d10 마스크 면적 | 차이 |
|---|---|---|---|---|
| 3.jpg | 5 | 0.8% | 0.7% | 0.1p |
| 5.jpg | 38 | 11.1% | 10.7% | 0.4p |
| 11.jpg | 46 | 24.0% | 22.1% | 1.9p |
| 9.jpg | 39 | 32.2% | 29.7% | 2.5p |
| 4.jpg | 11 | 47.0% | 43.4% | 3.6p |

- **잔글씨는 두 설정의 결과가 같다.** 최소값 3px에 걸리기 때문 (15px 글자면 10%·15% 모두 3px)
- **차이는 큰 글자에서만 난다.** 80px 글자면 8px vs 12px
- 육안 소견(예람님): d10은 대체로 d15와 비슷하나 **일부 이미지에서 d15보다 부자연스러움**

즉 비율 하나로는 양끝이 둘 다 어긋난다.

- 비율이 크면 → 큰 글자에서 **인접 줄의 마스크가 서로 붙어 큰 덩어리**가 되고, 복원할 단서가 사라져 뭉개진다 (`4.jpg`가 이 경우)
- 비율이 작으면 → 큰 글자 테두리에 **잔재가 남는다**

**제안 규칙 [미확정 — 실서비스 설계 시 확정 필요]**

1. **비율 + 상한**: `팽창 = clamp(글자높이 × 0.10~0.15, 3px, 상한)`. 상한을 둬야 큰 글자에서 덩어리가 안 생긴다. 상한값은 미검증
2. **병합 감지**: 팽창 후 인접 영역이 하나로 붙으면 그 영역만 팽창을 줄인다
3. **마스크 면적 상한**: 연결된 마스크 한 덩어리가 일정 비율을 넘으면 품질이 급격히 떨어진다. 본 표본에서 LaMa가 실패한 2장이 마스크 면적 최대 1·2위(`4.jpg` 47%, `9.jpg` 32%)
4. **배경 유형별 차등**: 단색은 넉넉히 넓혀도 무해(메우기가 자명), 사진은 좁게 유지(넓힌 만큼 질감을 복원해야 함)
5. **검수 플래그**: 3의 상한을 넘으면 자동 통과시키지 말고 사람 검수로 보낸다

> 이 항목은 2.1 bbox 등급 B 기준("마스크 확장으로 흡수 가능")과 맞물린다. B의 bbox 부정확은 팽창으로 흡수하도록 설계돼 있으므로, 팽창 규칙이 곧 B 오차의 허용 범위를 정한다.

**판정 기준 [확정]**

| 등급 | 기준 |
|---|---|
| A (완전 제거) | 원문 흔적 없음, 배경과 자연스럽게 이어짐 |
| B (경미한 흔적) | 자세히 봐야 티가 남 — 번역문을 얹으면 가려질 수준, 검수로 흡수 가능 |
| C (실패) | 원문이 읽히거나 왜곡·얼룩이 눈에 띔 |

집계: 사진/그라데이션 **영역** 중 **A+B 비율 70% 이상 → Go(사진 배경도 MVP 포함)**, 미만 → No-Go(단색 위주 축소 검토).

> 집계 단위 [확정 2026-08-20]: **영역** 단위. 원안은 "표본(=이미지)" 단위였으나 한 장에 단색·사진 구간이 섞여 이미지 단위 분류가 불가능함. E1은 영역마다 지운 결과가 따로 나오므로 영역 단위 판정이 자연스럽다. B(2.1)는 분량 문제로 이미지 단위 — 두 과업의 70%를 직접 비교하지 말 것.

**결과 기록**

> ⚠️ **실제 판정은 영역 단위가 아니라 이미지 단위로 이뤄졌음(2026-08-20).** 아래 표는 원안(영역 단위) 틀이라 그대로 채우지 않는다. 실측 결과는 그 아래 표에 기록한다. 영역 단위 재판정은 미실시.

| 후보 기술 | 테스트일 | 영역 수(solid/photo) | A | B | C | photo A+B 비율 | 소요시간 | 비고 |
|---|---|---|---|---|---|---|---|---|
| LaMa | | | | | | | | *(영역 단위 미실시)* |
| OpenCV 고전 | | | | | | | | *(영역 단위 미실시)* |
| SD 인페인팅 | | | | | | | | *(영역 단위 미실시)* |

**실측 결과 — 이미지 단위 판정 (2026-08-20, 표본 12장, 판정 예람님)**

마스크: B baseline poly를 글자 높이의 15%(최소 3px) 팽창. 판정 시 제품 인쇄 글자는 반영하지 않음.

| variant | 후보 | 실행 | A | B | C | A+B | 소요시간 |
|---|---|---|---|---|---|---|---|
| `lama` | LaMa (IOPaint, CPU) | 12장 | 8 | 2 | 2 | **10/12 (83%)** | 149s |
| `telea` | OpenCV Telea | 12장 | 1 | 2 | 9 | 3/12 (25%) | 1.5s |
| `ns` | OpenCV Navier-Stokes | 12장 | 1 | 1 | 10 | 2/12 (17%) | 1.5s |
| SD 계열 5종 | 아래 참조 | 2~3장 | — | — | — | 미판정 (탈락) | — |

- LaMa가 C를 받은 2장은 마스크 면적이 가장 큰 `4.jpg`(47%)·`9.jpg`(32%)
- 원본·마스크·결과 대조판: `poc/E1_inpaint/results/compare/`, 판정표: `poc/E1_inpaint/summary.md`

**SD 계열 — 탈락 [2026-08-20 / 예람님]**

| # | 설정 | 결과 |
|---|---|---|
| 1 | SD1.5 기본 (guidance 7.5 / steps 50) | 지운 자리에 **가짜 영문 문장** 생성 |
| 2 | PowerPaint `object-remove` (7.5 / 50) | 가짜 라벨·무늬 생성 |
| 3 | PowerPaint 보수 (1.5 / 20 / 네거티브 13단어 / match_histograms) | 개선 없음 |
| 4 | 3 + `hd_strategy=Original` | **3과 바이트 동일 — 옵션 무효** |
| 5 | SD1.5 보수 | 1과 거의 동일 |

탈락 근거 3가지

- **설정 3축(guidance·steps·네거티브 프롬프트)을 바꿔도 같은 실패가 반복됨**
- **대조군 파괴** — LaMa가 A를 받은 `1.jpg`에 적용했더니 상단·하단 띠, 말풍선, 하트가 전부 가짜 무늬로 채워짐. 어려운 케이스를 못 살리는 데 그치지 않고 쉬운 케이스를 깨뜨림
- **"문맥 부족" 가설 기각** — `hd_strategy`는 SD 계열에 적용되지 않는다. `DiffusionInpaintModel.__call__`이 부모를 덮어쓰고 그 값을 읽지 않으며 항상 이미지 전체를 처리한다(iopaint `model/base.py:280`). SD는 처음부터 전체 문맥을 보고도 지어낸 것

> 구조적 원인: SD 계열은 마스크 주변을 보고 채울 내용을 **생성**한다. 상세페이지는 주변이 온통 글자·라벨이라 "여기도 글자"가 모델에게 가장 그럴듯한 답이 되며, 설정으로 누르기 어렵다. LaMa는 주변 화소를 확장·복원하는 방식이라 실패해도 흐려질 뿐 **없던 것이 생기지 않는다.** 이 과업에는 이 성질이 결정적.

**최종 결정 [확정 2026-08-20 / 예람님]**

> ## **Go — 사진 배경도 MVP에 포함한다.**

| 항목 | 결정 |
|---|---|
| 채택 기술 | **LaMa** (IOPaint 경유) |
| 탈락 | OpenCV 고전(Telea·NS), SD 계열 전체 |
| 마스크 | B baseline poly + 글자 높이 15% 팽창(`d15`). 판정이 이 조건에서 이뤄짐 |
| 실행 설정 | `poc/E1_inpaint/run.py`의 `VARIANTS["lama"]` |

**판정 근거**

- LaMa 이미지 단위 **10/12 (83%)** — A 8, B 2, C 2
- 판정 시 **사진·그라데이션 배경 영역을 함께 확인**했으며, 그 영역들도 통과 수준으로 판단됨
- 제품 인쇄 글자는 판정에서 제외 (실서비스에서 지울 대상이 아님)
- LaMa가 C를 받은 2장은 마스크 면적 1·2위(`4.jpg` 47%, `9.jpg` 32%) — 기술 한계가 아니라 **마스크 과대**가 원인으로 보이며, 위 "마스크 팽창 폭" 규칙으로 대응할 사안

**기록상 유의점**

- 문서 원안의 집계 단위는 **영역 단위**였으나 실제 판정은 **이미지 단위**로 이뤄졌다. 영역 수가 280개라 전수 육안 판정이 비현실적이었음
- 따라서 위 83%를 원안의 "사진 영역 A+B 70%"와 **같은 수치로 취급하지 말 것**. 사진 영역 통과 여부는 판정자의 육안 확인에 근거하며 별도 수치로 집계되지 않았다
- `d10`(팽창 10%)도 12장 실행했으나 판정은 `d15` 기준으로만 이뤄졌다. `d10` 결과물은 `poc/E1_inpaint/results/lama_d10/`에 보존

**후속 (E1 종료 후 남는 과제)**

- 마스크 팽창 규칙 확정 (위 항목, 4장 체크리스트 등재)
- 마스크 면적이 큰 케이스의 품질 저하 대응

---

#### E1 종료 후 추가 검증 — 나노바나나 (Gemini 이미지 편집 모델)

> 위 **Go 결정과 LaMa 채택을 덮어쓰지 않는다.** 추가 후보 탐색이다.

**작동 방식이 근본적으로 다르다 — 마스크를 받지 않는다**

| | LaMa / SD | 나노바나나 |
|---|---|---|
| 입력 | 이미지 + **마스크** | 이미지 + **자연어 지시** |
| 영역 지정 | 픽셀 단위 정확 | **모델이 판단** |

기존 `results/masks/d15/`를 쓸 수 없어 지시 방법을 따로 설계해야 한다.
- **방법 A** 전체 이미지 + 문장 지시
- 방법 B 마스크를 색으로 칠한 이미지 제공
- 방법 C 영역별 크롭 후 되붙이기

**1차 시험 — `gemini-2.5-flash-image`, 방법 A, `1.jpg` 1장 (0.039$)**

| 항목 | 결과 |
|---|---|
| API 호출 | 성공 (17.9s) |
| 안전 필터 | 통과 — 인물 사진 거부 없음 |
| 비용 | 장당 **0.039$** (문서 기재값과 일치) |

**문제 3가지 — 모두 구조적이다**

| # | 문제 | 내용 |
|---|---|---|
| ① | **출력 크기가 바뀐다** | 1000×1000 → **1024×1024**. 프롬프트로 크기 유지를 지시했으나 지켜지지 않음. 자체 해상도 격자로 출력 |
| ② | **지시한 텍스트를 다 지우지 않는다** | 상단 `My First Collagen`, 하단 `윈이 PICK! 1등 화잘먹 모공 세럼`이 **그대로 남음**. 말풍선·`1+1`만 제거됨 |
| ③ | **원본을 다시 그린다** | 인물 얼굴·머리·표정이 바뀌고 구도가 이동. **키캡 키링 증정품이 통째로 소실**, 하트·거품 배경 요소 소실 |

**①은 E2에 직결된다** — B의 bbox와 E2 렌더링 좌표가 원본 기준인데 결과물 크기가 달라지면 전부 재계산해야 하고, 되돌리는 리사이즈에서 화질이 손실된다.

**③이 가장 치명적이다.** 상세페이지는 촬영 원본을 그대로 써야 하는데 모델이 새로 그린다. 지시문에 `Do NOT change the products, people, objects, layout`을 명시했으나 지켜지지 않았다.

> **SD 계열과 실패 방향이 다르지만 결과는 같다.** SD는 *없던 것을 만들어 넣어* 탈락했고, 나노바나나는 *있던 것을 다시 그려서* 문제다. 공통점은 **원본 보존 실패**다.
>
> LaMa가 이 과업에서 강한 이유가 재확인됐다 — **마스크 밖은 손대지 않는다는 구조적 보장**이 있다. 지시 기반 편집 모델에는 그 보장이 없다.

**가격 참고** (ai.google.dev/gemini-api/docs/pricing, 2026-08-20 확인)

| 모델 | 장당 |
|---|---|
| `gemini-2.5-flash-image` | 0.039$ |
| `gemini-3-pro-image` | **0.134$** (1K/2K), 0.24$ (4K) |

LaMa는 로컬 실행이라 추가 비용이 0이다. 12장 기준 나노바나나는 0.47~1.61$가 든다.

**2차 시험 — `gemini-3-pro-image`, 방법 A, 같은 이미지 (0.134$)**

상위 모델은 **지시 준수가 확연히 개선**됐다.

| 항목 | flash | pro |
|---|---|---|
| 텍스트 제거 | 2개 남김 | **전부 제거** |
| 키캡 키링 소품 | 소실 | **보존** |
| 하트·거품 배경 | 소실 | **보존** |
| 인물 | 다른 사람처럼 변형 | 유사하게 유지 |
| **출력 크기** | 1024 | **1024 (동일하게 실패)** |
| 소요 | 17.9s | 57.4s |

**그러나 핵심 문제는 그대로다.**
- **크기 1024 고정은 모델 등급과 무관한 구조적 제약**이다
- 여전히 재생성이다 — 인물이 확대·이동하고, 키캡 글자가 `오이쉬`→`오미쉬`로 **가짜 글자가 생성**됐으며, 제품 라벨이 뭉개졌다

**3차 시험 — 방법 C: 영역별 크롭 후 되붙이기 (`flash`, 13영역, 0.468$)**

방법 A의 구조적 결함 2개를 우회하는 설계. **마스크 안쪽만 합성하고 바깥은 원본 픽셀을 유지**해 LaMa와 같은 보장을 만든다.

| 문제 | 방법 A | **방법 C** |
|---|---|---|
| 출력 크기 | 1024로 변경 | **1000×1000 원본 유지** ✓ |
| 원본 보존 | 전체 재생성 | **마스크 밖 픽셀 그대로** ✓ |

**설계 의도대로 두 문제는 해결됐다.** 인물·소품·구도가 원본과 동일하다.

**그러나 새 문제 3가지가 나왔다**

| # | 문제 |
|---|---|
| ① | **저작권 필터로 일부 영역 거부** — 13개 중 1개가 `400 Image generation blocked due to copyright/recitation`으로 실패. 실서비스에서 일부 영역만 안 지워지면 파이프라인이 성립하지 않는다 |
| ② | **가짜 글자 생성** — 말풍선 자리에 읽을 수 없는 문자 같은 얼룩을 그려 넣었다. LaMa는 깨끗이 비웠다. SD 계열의 실패가 크롭 단위에서도 재현됐다 |
| ③ | **하단 띠 잔여 얼룩** |

**최종 비교 — LaMa 우위 4항목, 동등 2항목, 열위 0항목**

| 축 | LaMa (채택) | nano-C |
|---|---|---|
| 크기·좌표 보존 | ✓ | ✓ |
| 마스크 밖 원본 유지 | ✓ | ✓ |
| **가짜 글자 생성** | **없음** | **있음** |
| **전 영역 처리** | **보장** | **저작권 필터로 일부 실패** |
| 비용 (12장) | **0원** (로컬) | 5.6$ (flash) / 21$ (pro) |
| 속도 (12장) | **67초** | 44분 |

> ①이 결정적이다. 품질은 튜닝 여지가 있으나 **모델이 특정 크롭을 거부하는 것은 우회 수단이 마땅치 않다.** 실패한 영역이 그대로 남으면 그 이미지는 쓸 수 없다.

**결론 [2026-08-20 / 예람님]: 나노바나나 검증 종료. E1 채택 기술은 LaMa 유지.**

지시 기반 이미지 편집 모델은 **마스크 밖을 손대지 않는다는 구조적 보장이 없고**, 방법 C로 그 보장을 코드에서 만들어도 저작권 필터와 가짜 글자 생성이 남는다. E1처럼 **원본 좌표계를 지켜야 하는 파이프라인 단계**에는 부적합하다.

**총 지출: 0.641$** (flash 방법A 0.039 + pro 방법A 0.134 + flash 방법C 0.468)

**코드**: `poc/E1_inpaint/probe_nano.py` (방법 A), `run_nano_crop.py` (방법 C). 마스크 방식이 달라 기존 `run.py`와 분리

---

#### E1 종료 후 추가 검증 — 인페인팅 API (OpenAI `images.edit`)

> 위 **Go 결정과 LaMa 채택을 덮어쓰지 않는다.** 추가 후보 탐색이다.

**나노바나나와 범주가 다르다.** 나노바나나는 마스크를 못 받는 **지시형 편집**이라 "어디를 지울지" 지정할 수 없어 탈락했다. 이쪽은 **마스크를 받는 표준 인페인팅 API**다.

**검증 목적**: LaMa가 C를 받은 큰 마스크 케이스(`4.jpg` 47%, `9.jpg` 32%)를 상용 API가 구제할 수 있는지. 되면 **"작은 마스크는 LaMa 무료, 큰 마스크만 API"** 하이브리드 전략의 근거가 된다.

**구현 주의 2가지**

| 항목 | 내용 |
|---|---|
| 마스크 규약이 반대 | 우리 d15 마스크는 **흰색=지울 곳**, OpenAI는 **투명(alpha=0)=편집할 곳**. 알파를 반전해 넘긴다 |
| 출력 규격 고정 | 1024x1024 등으로만 나와 원본 크기와 안 맞는다. E1 방법 C처럼 **원본 크기로 되돌린 뒤 마스크 안쪽만 합성** |

**실측 (`gpt-image-2`)**

| 실행 | 마스크 | 출력tok | 소요 | 비용 |
|---|---|---|---|---|
| `4.jpg` medium | 47.0% | 1,716 | 42.8s | 0.0628$ |
| `4.jpg` **high** | 47.0% | 6,945 | 96.8s | **0.2197$** |
| `9.jpg` medium | 32.2% | 2,058 | 50.6s | 0.0704$ |
| `9.jpg` **high** | 32.2% | **8,232** | **139.0s** | **0.2556$** |

단가: 입력 $8 / 출력 $30 per 1M (developers.openai.com/api/docs/pricing, 2026-08-20 확인). 12장 환산 medium 0.84$ / high 3.07$.

**결과 — 1장은 개선, 1장은 아님**

| | LaMa | OpenAI |
|---|---|---|
| `4.jpg` (분홍 단색 배경) | C — 상단이 회색 얼룩 | **개선** — 분홍 배경으로 깔끔히 채움 |
| `9.jpg` (검정+노랑+파랑, 제품 다수) | C — 검은 덩어리 | **개선 아님** — 다른 방식으로 실패 |

**핵심 문제 — 주변 색을 읽지 않고 흰색으로 채우는 경향**

`9.jpg`에서 드러났다.
- 노란 병 위 텍스트 자리 → 노란색이어야 하는데 **흰 사각형**
- 파란 배지 안 → 파란색이어야 하는데 **흰 사각형**
- 검정 배경 위 → 검정이어야 하는데 **흰 여백**

> `4.jpg`가 잘 나온 것은 **배경이 밝은 분홍이라 흰색과 가까웠기 때문**으로 보인다. 주변 색을 맞춘 것이 아니라 우연히 맞았을 가능성이 크다.

**`quality=high`로도 해결되지 않으며, 효용이 배경에 따라 갈린다**

| | medium → high 변화 | 비용 |
|---|---|---|
| `9.jpg` (다색·복잡) | **있음** — 배지 안 색 복원, 잔여 사각형 감소. **그래도 노란 병 위·하단 검정 배경의 흰 사각형은 잔존** | 3.6배 |
| `4.jpg` (분홍 단색) | **거의 없음** — 상단 사각형 모서리가 약간 부드러워진 정도 | 3.5배 |

**두 경우 모두 마스크 모양이 결과물에 그대로 드러난다.** `4.jpg` 상단은 원본 옷 주름 질감이 복원된 것이 아니라 **둥근 사각형 평면으로 덮인 것**이다. 품질 단계로 넘을 수 있는 문제가 아니며, 비용 대비 효용도 낮다.

> **판정 기준(2.2)으로 보면 A(원문 흔적 없음, 배경과 자연스럽게 이어짐)가 아니라 B(자세히 봐야 티가 남 — 번역문을 얹으면 가려질 수준)에 가깝다.** LaMa의 C보다는 낫지만 A는 아니다.

**약점의 축이 LaMa와 다르다**

| | 약점 |
|---|---|
| LaMa | **마스크 면적** — 넓으면 무너진다 |
| OpenAI `images.edit` | **배경 복잡도** — 색이 여럿 섞이면 흰색으로 뭉갠다 |

> **"큰 마스크는 API로 폴백"은 조건부로만 성립한다** — 배경이 단순할 때다. LaMa 실패 2장 중 1장만 구제됐고, 그것도 배경이 밝은 단색이었기 때문이다. 현 시점에서 하이브리드 전략의 근거는 불충분하다.

**되붙이기는 정상 작동했다** — `RAW`와 합성본이 거의 같아 모델이 마스크 밖을 크게 건드리지 않았음을 확인했다. 나노바나나가 전체를 재생성했던 것과 대조적이며, **마스크 기반 API의 구조적 장점**이다.

**미검토 후보**: Stability AI `/stable-image/edit/erase` — **"지우기 전용"**으로 설계된 별도 엔드포인트라 "흰색 기본값" 문제가 없을 수 있다. OpenAI `images.edit`는 범용 생성 API에 마스크를 붙인 것이라 제거에 특화돼 있지 않다. **키 발급이 필요해 미착수.**

**총 지출: 0.6085$** (medium 2회 0.1332 + high 2회 0.4753)

**코드**: `poc/E1_inpaint/run_openai.py`

---

### 2.3 C. 로컬라이징 번역 (3순위, D 통합)

**목적**: 뷰티 도메인 용어·규제 표현을 반영한 번역을 가장 안정적으로 수행하는 범용 LLM 선정.

**후보**: Claude / GPT / Gemini / Qwen

**선행 조건**: 규제 매핑 테이블 최소 스펙을 데이터 담당과 합의(6.2 PoC 확인 항목) — 이게 없으면 RAG 검증 자체가 의미 없음. C 벤치마크보다 먼저 처리 필요.

**평가 방식 [문서 6.3 명시]**: LLM-as-judge 메인 + 팀 검수 보조. 평가 기준은 번역 품질 + 도메인 특화 기준(성분·인증 용어 정확도, 규제 표현 적절 대체 여부). 평가 데이터는 실제 뷰티 상세페이지 텍스트 표본.

**판정 기준 [제안 — 미확정]**: judge 점수 척도(예: 5점 척도)와 통과선은 문서에 없음. 팀 논의로 정하는 걸 추천.

**착수 준비 완료 [2026-08-20] — 합의 대기 중**

합의 없이 만들 수 있는 것을 먼저 구축했다. `poc/C_translate/`

| 파일 | 내용 |
|---|---|
| `build_evalset.py` | B(OCR) 출력 → 평가셋. **문서 10개 / 세그먼트 88개 / 929자** |
| `prompts.py` | 번역·judge 프롬프트. 4종에 동일 문안을 써서 프롬프트 차이가 모델 차이로 오독되지 않게 함 |
| `config.py` | 모델 레지스트리 + 미확정 항목 |
| `estimate_cost.py` | 호출 전 건수·비용 추정. **API를 호출하지 않음** |
| `run.py` | 번역 실행기. 캐싱·안전장치 |

*평가셋* — 이미지 단위 문서로 묶어 같은 페이지의 전체 문구를 문맥으로 함께 넘긴다(낱개로 던지면 "1등"·"세럼" 같은 조각이 문맥 없이 번역됨).

---

#### 평가셋 방식 변경 [확정 2026-08-20 / 예람님]

**1차(B 출력 그대로) → 2차(합성 데이터).** 원문 OCR이 완벽하다는 가정으로 입력을 바꾼다.

**예비 실행에서 드러난 문제 2건**

Claude Code 세션에서 88개 세그먼트를 프롬프트대로 번역해 배선을 검증한 결과(비용 0).

| # | 문제 | 근거 |
|---|---|---|
| 1 | **OCR 오류가 번역 품질과 섞임** | 88건 중 **17건**에 문맥 추정이 필요했음. 무엇을 재는지 불분명해짐 |
| 2 | **regulation 축 검증 불가** | 규제 표 금지표현 8종이 평가셋에 **0건** 등장. 판촉 문구·성분 목록 위주라 효능 주장이 거의 없음 |

1번 세부 — 인물·브랜드명 오인식(`윈이`→WONI, `이디협 하이퍼 클라견아스크`→MEDIHEAL Hyper Collagen Mask), 조각 분절(`블랙`+`헤드`가 따로 검출), **성분명 훼손 19종**(`페녹시에탄음`→Phenoxyethanol, `테칠파라벤`→Methylparaben), 복원 불가 1건.

> ⚠️ **성분 목록은 추정하면 안 되는 정보다.** 알레르기 유발 성분 목록이라 틀리면 안전 문제가 된다. 실서비스에서는 이 구간을 자동 번역 대상에서 제외하거나 반드시 사람 검수를 걸어야 한다. → 4장 체크리스트 등재

**2차 평가셋 — 합성 데이터**

`poc/C_translate/synthetic_source.py`. 실존 제품이 아니며 브랜드명 `LUMIÈRE / 루미에르`는 지어낸 이름이다. **실존 브랜드명을 쓰면 지어낸 효능 주장이 실제 회사에 귀속되므로 피했다.**

| 항목 | 1차 (OCR) | 2차 (합성) |
|---|---|---|
| 문서 / 세그먼트 | 10 / 88 | **8 / 64** |
| 문자 수 | 929 | 1,060 |
| 원문 정확도 | OCR 오류 포함 | **정답** |
| 규제 표현 등장 | **0건** | **8종 전부** |

문서 구성: 인트로 / 효능 / 성분 / 안전·인증 / 사용법 / 텍스처 / 판촉 / 주의사항. 평가 4축이 모두 자극되도록 배치했다 — 성분명·인증명(terminology), 금지표현 8종(regulation), 한 단어 헤드라인부터 두 줄 고지문까지(length).

1차 평가셋은 `evalset_ocr.json`으로 보존. 예비 번역 결과는 `results/claude_session/`.

> 예비 실행 결과를 **Claude의 벤치마크 점수로 쓰지 말 것.** 세션에는 Claude Code 시스템 프롬프트와 PoC 전체 대화가 맥락으로 들어 있어 조건이 다르고, 다른 후보는 그 맥락 없이 돈다. 공정 비교가 아니다.

---

*평가 축* — 문서 2.3을 따라 4축: `quality`(번역 품질) / `terminology`(성분·인증 용어) / `regulation`(규제 표현 대체) / `length`(원문 대비 길이 — 이미지에 얹을 문구라 증가하면 배치가 깨짐). 구조화 출력 안정성은 JSON 파싱 성공 여부로 기계 집계한다.

*예상 규모* (2차 평가셋 기준) — 번역 32회(4종 × 8문서) + 채점 32회 = **총 64회**. Claude 기준 번역 0.105$. 나머지 3종은 가격 미확인.

*안전장치* — `run.py`는 기본이 dry-run이며 `--execute`를 명시해야 호출한다. 아래 미확정 항목이 남아 있으면 `--execute`를 거부한다. API 응답은 캐싱하며 재실행 시 캐시를 우선한다.

---

#### 1차 API 실행 — `gpt-5.5` 번역 (2026-08-20)

**실측**

| 항목 | 추정 | 실측 | 비율 |
|---|---|---|---|
| 입력 토큰 | 11,752 | **7,003** | 60% |
| 출력 토큰 | 1,852 | **3,701** | **200%** |
| 비용 | 0.1143$ | **0.1460$** | 128% |

호출 8회(문서 8건, 번역만). 문서당 3.8~14.2초. 단가 입력 $5.00 / 출력 $30.00 per 1M.

> 추정기 보정 필요 — 입력은 과대(한글 문자당 0.9토큰이 높음), **출력은 절반으로 과소**. 모델이 번역문뿐 아니라 JSON 구조와 `note`까지 생성하는 몫을 세지 않았음. `OUT_RATIO`를 2.0 → 4.0 수준으로 올려야 맞음.

**배선 검증 결과 — 전부 통과**

| 항목 | 결과 |
|---|---|
| OpenAI SDK 호출 | 8/8 성공 |
| **구조화 출력 안정성** (문서 2.3 평가 항목) | **JSON 파싱 실패 0건** |
| 세그먼트 | 64/64 |

`구조화 출력 안정성`이 처음으로 실전 검증됐다. 세션 예비 실행에서는 사람이 직접 기록해 이 항목을 시험하지 못했다.

**핵심 발견 — 규제 표를 문자열 치환표로 두면 법정 고지문이 깨진다**

가설(세션 예비 실행)이 **의도를 모르는 모델에서 실제로 재현됐다.**

| | `08_notice-06` |
|---|---|
| 원문 | 본 제품은 의약품이 아니므로 질환의 **치료**를 목적으로 사용할 수 없습니다 |
| `gpt-5.5` | This product is not a medicine and is not intended for disease **care**. |
| 세션(참고) | This product is not a drug and cannot be used for the purpose of **treating disease** |

`gpt-5.5`는 규제 치환 8건 중 **7건을 정확히 적용**하고 `note`에 근거까지 남겼으나, 마지막 하나에서 표를 기계적으로 따랐다. 이 문장의 `치료`는 효능을 **주장**하는 것이 아니라 "치료 목적으로 쓸 수 없다"고 **부인**하는 법정 고지문이다. 치환 결과 `not intended for disease care`는 "질환 케어용이 아니다"로 읽혀 고지 기능이 약해진다.

> **모델의 결함이라기보다 규제 표 설계의 결함이다.** 표가 "이 단어는 무조건 바꿔라"로 돼 있으니 지시대로 따른 것이다. 세션 실행에서 이를 피한 것은 합성 평가셋을 작성한 주체가 함정의 의도를 알고 있었기 때문일 가능성이 크다 — 공정 비교가 아니다.

**→ 규제 매핑 테이블 스펙에 문맥 조건이 반드시 필요하다.** 최소한 주장 문맥(효능을 내세움)은 치환하고, 부인·고지 문맥("~할 수 없습니다", "의약품이 아니므로")은 치환을 금지해야 한다. 데이터 담당과 합의할 때 이 조건이 빠지면 법정 고지문이 깨진 채로 나간다. → 4장 체크리스트 등재

---

#### 2차 API 실행 — `claude-opus-5` 번역 (2026-08-20)

**실측 비용 — 2종 누적 0.2751$ (약 385원)**

| 모델 | 입력tok | 출력tok | 단가(in/out per 1M) | 비용 |
|---|---|---|---|---|
| `gpt-5.5` | 7,003 | 3,701 | $5 / $30 | 0.1460$ |
| `claude-opus-5` | 11,079 | 2,948 | $5 / $25 | **0.1291$** |

추정 0.1275$ → 실측 0.1291$ (**101%**). 추정 근거를 문자수 근사에서 **`gpt-5.5` 실측 토큰**으로 바꾼 것이 적중했다. 이후 추정은 이 방식을 쓸 것.

> 토큰 구성이 대조적이다 — **Claude는 입력이 58% 많고 출력이 20% 적다.** 토크나이저의 한글 처리 차이와 출력 스타일 차이가 겹친 결과. 결과적으로 Claude가 약간 저렴.

> 배선 사고 1건: `anthropic` SDK 미설치로 첫 시도 실패. **호출 전 단계라 비용 0.** 설치(1.0.0) 후 정상. 이런 것을 미리 걷어내는 것이 파일럿의 목적이었다.

**배선 검증 — 2종 모두 통과**

| 항목 | `gpt-5.5` | `claude-opus-5` |
|---|---|---|
| SDK 호출 | 8/8 | 8/8 |
| JSON 파싱 실패 | 0 | 0 |
| 세그먼트 | 64/64 | 64/64 |

**규제 표 진단 확정 — 모델 문제가 아니라 표 설계 결함**

| | `08_notice-06` |
|---|---|
| 원문 | 본 제품은 의약품이 아니므로 질환의 **치료**를 목적으로 사용할 수 없습니다 |
| `gpt-5.5` | not intended for disease **care** |
| `claude-opus-5` | cannot be used for the **care** of any disease |

**서로 다른 벤더의 플래그십 2종이 동일하게 기계 치환했다.** 둘 다 `note`에 "규제 매핑 적용"이라 남겨 지시를 따랐다는 자각까지 있었다. 나머지 7건은 양쪽 모두 정확히 적용.

> 세션 예비 실행에서 이를 피한 것은 **실력이 아니라 합성 평가셋 작성자가 함정의 의도를 알았기 때문**임이 확정됐다. API 조건의 동일 모델이 걸렸다.
>
> 역설적으로 이는 **좋은 신호**다 — 두 모델 모두 규제 지시를 성실히 따른다는 뜻이므로, 표만 제대로 설계하면 동작한다. 반대로 표가 "무조건 바꿔라"인 한 어떤 모델을 써도 결과는 같다.

**판정 시 볼 지점 — 원문에 없는 정보 추가**

`04_safety-06` "부작용 없음이 확인된 순한 처방"

| 모델 | 번역 | 추가된 정보 |
|---|---|---|
| `gpt-5.5` | Gentle formula **for sensitive skin** | "민감성 피부용" |
| `claude-opus-5` | A gentle formula **you can trust** | "믿을 수 있는" |

양쪽 모두 원문에 없는 내용을 넣었다. 규제 관점에서는 후자가 더 위험할 수 있다(신뢰성 단정). judge의 `quality`·`regulation` 축에서 다룰 사안.

번역 대조표: `poc/C_translate/summary.md` (실행 요약 + 규제 치환 점검 + 세그먼트 64건 3종 대조)

---

#### 3차 API 실행 — judge 채점 (2026-08-20)

채점 모델 `claude-opus-5`, 5점 척도, 2종 × 문서 8건 = 16회.

**채점 결과**

| 축 | `gpt-5.5` | `claude-opus-5` | 차이 |
|---|---|---|---|
| quality | 4.41 | **4.55** | +0.14 |
| terminology | 4.75 | **4.83** | +0.08 |
| regulation | **4.80** | 4.77 | -0.03 |
| length | 4.94 | 4.95 | +0.02 |
| **전체 평균** | 4.72 | **4.77** | **+0.05** |

**⚠️ 이 점수를 채택 근거로 쓸 수 없다 — 이유 3가지**

| # | 문제 |
|---|---|
| 1 | **차이가 0.05점.** 5점 척도의 1%. 세그먼트 64개로는 우연과 구분되지 않는다 |
| 2 | **자기 채점 편향이 그 방향으로 나왔다.** Claude judge가 Claude 번역을 4축 중 3축에서 높게 줬다. 편향인지 실제 우위인지 **지금 데이터로 구분 불가** — 다른 벤더 judge로 교차 채점해야 판별된다 |
| 3 | **점수가 천장에 붙었다.** 네 축 모두 4.4~4.95. 통과선을 4.0에 두면 둘 다 통과, 4.8에 두면 둘 다 미달. **척도가 이 과업 난이도에 맞지 않는다** |

**통과선 결정 보류 [2026-08-20 / 예람님]**: 위 이유로 지금 정하면 임의값이 된다. **후보 4종이 모여 변별이 생긴 뒤 정한다.**

**낮은 점수 — 두 모델이 같은 곳에서 걸렸다**

| 세그먼트 | 원문 | `gpt-5.5` | `claude-opus-5` |
|---|---|---|---|
| `04_safety-06` | 부작용 없음이 확인된 순한 처방 | quality **2** | quality **3** |
| `04_safety-03` | 식약처 고시 기준 준수 | regulation **3** | regulation **3** |

- `04_safety-06`은 양쪽 최저점. **"원문에 없는 정보 추가"** 문항으로, 육안 소견(위 2차 실행 기록)과 judge 판단이 일치했다
- `04_safety-03`은 규제 관련 문구인데 규제 표에 없어 처리 기준이 없었던 경우
- **두 모델이 같은 문항에서 걸린다는 것은 모델 역량 문제가 아니라 원문·지시가 애매한 지점을 가리킨다.** 프롬프트나 규제 표를 손볼 단서

**실측 비용 — C 누적 0.7615$ (약 1,066원)**

| 단계 | 호출 | 비용 |
|---|---|---|
| 번역 `gpt-5.5` | 8 | 0.1460$ |
| 번역 `claude-opus-5` | 8 | 0.1291$ |
| judge(gpt) | 8 | 0.2565$ |
| judge(claude) | 8 | 0.2299$ |
| **합계** | **32** | **0.7615$** |

judge 추정 0.4411$ → 실측 0.4864$ (110%). **judge가 번역보다 비싸다** — 감점 사유를 문장으로 쓰게 해 출력이 많다.

**judge 배선 검증 — 통과**

호출 16/16 성공, JSON 파싱 실패 0건, 채점 세그먼트 128건, 집계·정렬 정상.

캐시 키를 `judge_{채점자}_s{척도}_{대상}`으로, 점수 폴더를 `{대상}__by_{채점자}`로 고쳤다. 채점자나 척도를 바꿔도 옛 점수를 재사용하지 않으며, 나중에 교차 채점 결과를 나란히 비교할 수 있다.

축별 평균·낮은 점수 15건: `poc/C_translate/scores/{대상}__by_claude/summary.md`

---

#### 4차 API 실행 — `qwen3.8-max` 번역 (2026-08-20)

DashScope 국제(싱가포르) OpenAI 호환 엔드포인트. 새 SDK 없이 `call_openai`에 `base_url`만 넘겨 재사용.

**추정이 5.4배 빗나갔다 — 원인은 추론 토큰**

| 항목 | 추정 | 실측 |
|---|---|---|
| 비용 | 0.0362$ | **0.1964$ (543%)** |
| 출력 토큰 | 3,701 가정 | **30,321** |
| 소요 | — | **702s** |

`usage`를 뜯어본 결과 **출력의 91%가 `reasoning_tokens`**였다(문서 1건 기준 2,263 중 2,059). `qwen3.8-max`는 기본적으로 사고 모드로 동작하며, 사고 과정은 `reasoning_content`에 따로 담겨 응답 본문(`content`)에는 실리지 않지만 **출력 토큰으로 과금되고 소요 시간에 반영된다.** 실제 번역문은 3,000토큰 남짓으로 타 모델과 비슷하다.

> **단가가 싸도 총액이 역전된다.** 출력 단가는 $6으로 Claude($25)·GPT($30)보다 훨씬 싼데, 토큰을 10배 써서 3종 중 가장 비쌌다.

> ⚠️ **추정 방식의 한계가 드러났다.** "타 모델 실측 토큰 × 단가" 방식은 Claude에서 101%로 정확했으나, **모델마다 출력 길이가 비슷하다**는 가정에 서 있었다. Qwen이 그 가정을 깼다. **새 모델은 1건만 먼저 돌려 출력 길이를 실측한 뒤 전체를 추정할 것.** Gemini에도 같은 함정이 있을 수 있다.

**사고 모드 유지 결정 [2026-08-20 / 예람님]**: `enable_thinking=False`로 끄면 비용·속도가 10배 개선되나, Claude·GPT도 내부 추론을 하므로 기본 설정끼리 비교하는 편이 공평하다고 판단.

| 항목 | 결과 |
|---|---|
| JSON 파싱 실패 | 0/8 |
| 세그먼트 | 64/64 |

---

#### 참고 측정 — `qwen-mt-plus` (번역 전용 모델)

**⚠️ 벤치마크 후보가 아니다.** 문서 2.3의 과업은 "범용 LLM 선정"이며 D(규제 판단)가 통합돼 있는데, Qwen-MT는 구조상 그 조건을 못 맞춘다.

| 요구 | Qwen-MT |
|---|---|
| 시스템 프롬프트로 규칙 지시 | **불가** — 공식 문서에 "system 역할 메시지로 전역 동작을 설정할 수 없다"고 명시 |
| JSON 구조화 출력 | 불가 |
| 같은 페이지 문맥 전달 | 불가 — 세그먼트 단위 호출 |

규제 표는 이 모델 고유의 `terms`(용어집) 파라미터로 전달했다. 다른 후보와 **전달 경로가 다르다**는 점을 감안할 것.

**실측 — 압도적으로 가볍다**

| 모델 | 출력tok | 소요 | 비용 |
|---|---|---|---|
| `claude-opus-5` | 2,948 | 45s | 0.1291$ |
| `gpt-5.5` | 3,701 | 56s | 0.1460$ |
| `qwen3.8-max` | 30,321 | 702s | 0.1964$ |
| **`qwen-mt-plus`** *(참고)* | **604** | **26s** | **≤0.0295$** |

> 비용은 **상한**이다. `qwen-mt-plus` 단가가 공개돼 있지 않아 `qwen3.8-max` 단가($2/$6)로 계산했다. 실제는 더 낮을 가능성이 크나 확인된 값이 아니다.

**법정 고지문 — v1 평가셋에서는 참고 모델만 통과했다**

> ⚠️ **정정 [2026-08-20]**: 아래는 **v1 평가셋의 1문항 기준**이다. v2 확장 표본(함정 29건)에서는 `qwen-mt-plus` 정답률이 **76%로 상위권 동률**이며, 아래 "유일하게 통과"라는 표현은 과했다. 6차 실행 기록 참조.

| 모델 | `08_notice-06` |
|---|---|
| `gpt-5.5` | not intended for disease **care** ✗ |
| `claude-opus-5` | cannot be used for the **care** of any disease ✗ |
| `qwen3.8-max` | cannot be used for disease **care** ✗ |
| **`qwen-mt-plus`** *(참고)* | should not be used for the **treatment** of any medical condition **✓** |

용어집이 켜진 상태에서 **주장 문맥은 치환하고 부인 문맥은 치환하지 않았다.** 별도 검증:

| 문맥 | 용어집 없음 | 용어집 있음 |
|---|---|---|
| 여드름 **치료**에 효과적 (주장) | effective for treating acne | **effective for blemish-prone skin** |
| 질환의 **치료** 목적으로 쓸 수 없음 (부인) | for the treatment of diseases | for the **treatment** of any disease |
| 미백 기능성 화장품 | **Whitening** | **brightening** |

미백이 치환된 것으로 보아 용어집은 확실히 동작 중이었다.

> **해석 주의**: MT 모델이 규제를 이해했다기보다, 용어집을 문장 구조에 맞춰 선택 적용하는 메커니즘이 결과적으로 맞아떨어진 것으로 보인다. **표본 1문항이라 일반화할 수 없다.**
>
> 다만 새 질문이 생겼다 — **규제 규칙을 "프롬프트 지시"로 주는 것과 "용어집 파라미터"로 주는 것 중 어느 전달 경로가 문맥에 덜 휘둘리는가.** 서로 다른 벤더의 범용 LLM 3종이 프롬프트 경로에서 모두 실패했고, 용어집 경로가 통과했다.

**→ 파이프라인 분리 검토 근거** — 번역은 전용 모델이 싸고 빠르게, 규제 판단은 문맥을 읽는 LLM이 별도 단계로. 다만 이는 문서 6.2의 "D를 C에 통합" 결정을 되돌리는 사안이라 PoC 범위 밖이다. → 4장 체크리스트 등재

---

#### 5차 — `gemini-3.1-pro-preview` 추가, 후보 4종 완료 (2026-08-20)

Gemini도 OpenAI 호환 엔드포인트를 제공해 어댑터 한 줄 추가로 끝났다. **Qwen에서 배운 "새 모델은 1건 선실측" 절차를 적용** — `06_texture` 1건이 출력 198토큰으로 정상 범위임을 확인한 뒤 전체를 돌렸다(Qwen 같은 추론 토큰 폭증 없음).

**4종 judge 점수 — 후보를 늘려도 변별이 안 생겼다**

| 축 | claude | gpt | gemini | qwen | 폭 |
|---|---|---|---|---|---|
| quality | **4.55** | 4.41 | 4.48 | 4.47 | 0.14 |
| terminology | **4.83** | 4.75 | 4.77 | 4.77 | 0.08 |
| regulation | 4.77 | **4.80** | 4.75 | 4.75 | 0.05 |
| length | 4.95 | 4.94 | **4.98** | 4.95 | 0.05 |
| **전체 평균** | **4.77** | 4.72 | 4.75 | 4.73 | **0.05** |

> **2종 0.05 → 3종 0.05 → 4종 0.05.** 후보 수와 무관하게 격차가 그대로다. **절대 점수 5점 척도로는 이 과업의 모델을 가를 수 없다**는 것이 확정됐다. claude가 2축 1위이나 자기 채점 편향이 섞여 있어 그대로 읽을 수 없다.

**실측은 확연히 갈린다 — 판정 근거가 여기 있다**

| 모델 | 품질 | 출력tok | 소요 | 비용 |
|---|---|---|---|---|
| `claude-opus-5` | 4.77 | 2,948 | **45s** | 0.1291$ |
| `gpt-5.5` | 4.72 | 3,701 | 56s | 0.1460$ |
| `gemini-3.1-pro-preview` | 4.75 | 3,003 | 158s | **0.0505$** |
| `qwen3.8-max` | 4.73 | 30,321 | **702s** | 0.1964$ |

**품질 폭 1%, 비용 폭 3.9배, 속도 폭 15.6배.**

---

#### 6차 — 평가셋·용어집 확장 후 재실행 (2026-08-20)

**변경 근거 — 문제는 표본 양이 아니라 난이도였다**

1차 평가셋을 분석한 결과:

| 지표 | 값 |
|---|---|
| 세그먼트 평균 길이 | **16.6자** (20자 이하 80%) |
| `length` 축 만점 비율 | **96.1%** |
| `terminology`·`regulation` 만점 | 약 80% |
| quality 평균 — 15자 이하 / 16자 이상 | 4.53 / **4.42** |

점수가 천장에 붙어 측정 기능을 잃었다. 다만 **긴 세그먼트에서 점수가 떨어지는 신호**가 있어, 양이 아니라 난이도를 올리는 방향으로 확장했다.

| 지표 | v1 | v2 |
|---|---|---|
| 세그먼트 | 64 | **89** |
| 평균 길이 | 16.6자 | **21.2자** |
| 40자 이상 | 0% | **9%** |
| 금지어 등장 | 8건 | **29건** |
| 용어집 항목 | 8 | **15** |

**용어집은 개수보다 함정 유형을 넓혔다** — v1은 함정이 사실상 1건(`치료` 고지문)이었는데 그 1건이 범용 LLM 3종을 걸러냈으므로, 변별력이 입증된 축을 확장했다.

| 유형 | 내용 |
|---|---|
| A | 주장/부인 양쪽에 등장 — 문맥에 따라 치환 여부가 갈린다 |
| B | 부분 일치 — 금지어가 더 긴 단어 안에 있다(`재생` vs `재생력`) |
| C | 제품명 안의 금지어 — **프롬프트의 "제품명 유지" 규칙과 충돌** |
| D | 인용문 안의 금지어 — 제3자 발화를 바꾸면 인용이 왜곡된다 |
| E | 한 문장에 금지어 2~3개 |
| F | 조사·어미 결합 — 치환하면 문법이 깨진다 |

> 캐시 버그 수정: 용어집이 바뀌면 시스템 프롬프트가 바뀌는데 캐시 키가 문서명뿐이라 **옛 용어집으로 만든 번역을 재사용**하게 돼 있었다. 프롬프트 지문(SHA-256)을 캐시에 넣어 불일치 시 무효화하도록 고쳤다. dry-run 집계도 지문을 확인하게 맞췄다.

**실측 (89세그먼트)**

| 모델 | 입력 | 출력 | 소요 | 비용 |
|---|---|---|---|---|
| **`gemini-3.1-pro-preview`** | 11,781 | 4,127 | 212s | **0.0731$** |
| `claude-opus-5` | 18,100 | 4,800 | **74s** | 0.2105$ |
| `gpt-5.5` | 11,383 | 6,788 | 94s | 0.2606$ |
| `qwen3.8-max` | 11,622 | **61,820** | **1,504s** | 0.3942$ |
| **합계** | | | | **0.9383$** |

**함정별 결과 — judge 없이 기계 점검만으로 변별이 나왔다**

| 유형 | claude | gpt | gemini | qwen |
|---|---|---|---|---|
| A 주장 | 정상 | 정상 | 정상 | 정상 |
| **A 부인** | **실패** | **실패** | **실패** | **실패** |
| B 부분일치 | 정상 | 정상 | 정상 | 정상 |
| **C 제품명 (명시)** | **실패** | **통과** | **실패** | **통과** |
| C 제품명 (암시) | 실패 | 실패 | 실패 | 실패 |
| **D 인용** | **실패** | **실패** | **실패** | **실패** |
| E 다중 | 정상 | 정상 | 정상 | 정상 |
| F 조사 | 정상 | 정상 | 정상 | 정상 |

**① A 부인 — 4종 전부 실패, 문장이 깨졌다**

| | `주름 개선 효과는 의학적으로 입증된 바 없습니다` |
|---|---|
| claude | Helps reduce the look of fine lines — **not dermatologist-informed proven** |
| gpt | ...has not been proven by **dermatologist-informed** testing |
| gemini | ...is **not dermatologist-informed proven** |

`의학적 → dermatologist-informed` 기계 치환으로 **영어 문장이 문법·의미 모두 성립하지 않는다.** 원문은 "효과가 입증되지 않았다"는 소극적 고지인데 뜻이 뭉개졌다.

**② D 인용 — 4종 전부 실패, 발언 왜곡**

| | `피부과 전문의 인터뷰 중: "이 성분은 치료 목적이 아닙니다"` |
|---|---|
| 4종 공통 | "This ingredient is not for **care** purposes" |

**전문의가 하지 않은 말로 바뀌었다.** 인용부호 안이라는 신호를 아무도 처리하지 못했다. 실서비스라면 발언 왜곡 소지가 있다.

**③ C 제품명 — 2:2로 갈렸다. 모델 간 처리 방침 차이가 처음으로 드러난 지점**

| | `루미에르 미백 앰플 30ml (제품명)` |
|---|---|
| **gpt / qwen** | **루미에르 미백 앰플 30ml** — 원표기 유지 |
| claude / gemini | Lumiere **Brightening** Ampoule 30ml |

프롬프트 규칙 3번("제품명은 번역하지 않고 원표기 유지")과 규제 표가 충돌하는 상황에서 **GPT·Qwen은 제품명 규칙을, Claude·Gemini는 규제 표를 우선했다.** 어느 쪽이 정답인지는 팀이 정할 사안이다.

> 단 `(제품명)` 표기가 없는 `안티에이징 라인`은 **4종 모두 치환**했다. 표시가 없으면 아무도 알아채지 못한다 → **실서비스에서는 `role` 정보로 제품명을 명시해 넘겨야 한다.** 4장 체크리스트의 `role` 항목과 직결된다.

**함정 정답률 — judge 점수보다 훨씬 잘 가른다**

치환 여부가 아니라 **기대 동작 대비 정오**로 집계했다. 부인·고지 문맥 / 인용문 / 제품명은 치환하지 않는 것이 정답이다.

| 대상 | 정답 | 오답 | 정답률 |
|---|---|---|---|
| `gpt-5.5` | 22 | 7 | **76%** |
| `qwen3.8-max` | 22 | 7 | **76%** |
| `qwen-mt-plus` *(참고)* | 22 | 7 | **76%** |
| `claude-opus-5` | 21 | 8 | 72% |
| `gemini-3.1-pro-preview` | 20 | 9 | 69% |

**폭이 7%p.** judge 절대 점수의 격차 0.05점(1%)과 대비된다. 함정 정답률이 이 과업의 변별 지표로 훨씬 유효하다.

> ⚠️ **집계 사고 기록 [2026-08-20]** — 처음 이 표를 냈을 때 `qwen-mt-plus`가 52%로 나왔고, "MT는 치환 자체를 잘 못 한다"고 잘못 해석했다. 실제 원인은 **확장 25건을 아직 실행하지 않은 것**이었고, 비교표가 미실행을 오답으로 세고 있었다.
>
> 조치: `compare.py`가 미실행을 `—`로 표시하고 정답률 분모에서 제외하도록 수정. 미실행이 있으면 경고를 표기한다.
>
> **이 사고 자체가 "조건이 다른 결과를 같은 표에 넣으면 오독된다"는 실례다.** 참고 측정을 분리 표기해야 하는 이유를 작성자 본인이 실증한 셈이다.

**종합 — 모델들은 치환은 잘하고 치환 억제를 못 한다**

공통 실패 3유형(A 부인 / C 제품명 암시 / D 인용)이 **전부 "치환하면 안 되는 자리"**다. 프롬프트가 "바꿔라"만 지시하고 "언제 바꾸지 말라"를 말하지 않았기 때문이며, 규제 표 스펙의 문제라는 진단을 한 번 더 확인한다.

**C 누적 지출: 약 2.44$ (약 3,400원)**

---

**실행 전 확정 필요 3건**

| 항목 | 담당 | 없으면 |
|---|---|---|
| `TARGET_LANG` 번역 목표 언어 | 팀 | **문서에 명시가 없음.** 번역 자체가 불가 |
| `JUDGE_SCALE` / `JUDGE_PASS` 척도·통과선 | 팀 논의 | 점수는 나오나 합격/불합격을 못 가림 |
| `REGULATION_TABLE` 규제 매핑 테이블 | 데이터 담당 합의 | `regulation` 축을 채점할 수 없음 (문서 2.3 선행 조건) |

추가로 `gpt`·`gemini`·`qwen`의 모델 ID·가격이 `config.MODELS`에 비어 있다. 키 확보 시점에 각 벤더 가격표를 확인해 채운다. 벤더 어댑터도 Claude만 구현했다 — 검증 없이 미리 적으면 틀린 코드가 남으므로 키가 생긴 뒤 추가한다.

**결과 기록**

| 후보 모델 | 번역 품질(judge) | 용어 정확도 | 규제 대체 적절성 | 구조화 출력 안정성 | 비용 | 비고 |
|---|---|---|---|---|---|---|
| 후보 모델 | 번역 품질(judge) | 용어 정확도 | 규제 대체 적절성 | 구조화 출력 안정성 | 비용 | 비고 |
|---|---|---|---|---|---|---|
| Claude (`claude-opus-5`) | 4.55 | 4.83 | 4.77 · 표 8건 중 7건 정확, 고지문 1건 기계 치환 | **8/8 성공** | **0.1291$** | ⚠️ 자기 채점 |
| GPT (`gpt-5.5`) | 4.41 | 4.75 | 4.80 · 〃 | **8/8 성공** | 0.1460$ | |
| Gemini | — | — | — | — | — | **미실행** — API 키 없음 |
| Qwen | — | — | — | — | — | **미실행** — API 키 없음 |

- judge 점수는 `claude-opus-5` 채점, 5점 척도. **전체 평균 격차가 0.05점으로 변별이 안 되며 자기 채점 편향이 섞여 있다** — 채택 근거로 쓸 수 없다(3차 실행 기록)
- `구조화 출력 안정성`·`비용`은 기계 집계 항목으로 실측 완료
- `규제 대체 적절성`은 표 설계 결함이 원인이므로 모델 변별에 쓰기 어렵다(2차 실행 기록)

**최종 결정**: _미정_

> **⏸ 2026-08-20 — C 검증을 중단하고 재설계하기로 결정 (예람님).**
>
> 후보 4종 번역·비교는 끝났으나 **judge 절대 점수가 모델을 가르지 못해**(2·3·4종 모두 폭 0.05) 채택 결정을 내릴 수 없었다. 판정 방식 자체를 다시 설계해야 한다.
>
> **새 세션에서 재계획해 진행한다.** 인계 문서: **`poc/C_translate/HANDOFF.md`**
>
> - 확정 사항(목표 언어·합성 평가셋·함정 6유형·후보 4종)과 코드 자산 1,765줄은 그대로 승계
> - 재설계 대상 4가지: 판정 방식 / 평가 축 / 통과선 / 표본 규모
> - 아래 실행 기록은 **참고용으로 보존**한다. ⚠️ **judge 점수는 전부 v1 평가셋(64세그먼트·용어집 8항목) 기준**이며 현재 v2(89세그먼트·용어집 15항목)와 비교할 수 없다. **judge v2는 미실행.**
> - 블로커는 재계획으로 풀리지 않는다 — **규제 매핑 테이블 실제 스펙이 여전히 미합의**이며, 현재 변별의 대부분이 규제 함정에서 나오므로 **가짜 표로 고른 모델이 진짜 표에서도 1등이라는 보장이 없다.** 결론은 실제 표 확보 전까지 잠정이다.

---

#### 재계획 실행 결과 [2026-08-25] — 3단계 검증 완료

**⏸ 재설계를 마치고 실행했다.** 실행 계획·전 과정 기록: `poc/C_translate/PLAN.md` (진행 기록 20건)

**무엇을 바꿨나**

| 항목 | 재설계 전 | 재설계 후 |
|---|---|---|
| 검증 구조 | 후보 4종 일괄 비교 | **3단계** — ① 프롬프트 구조 확정 ② 벤더 내 예선 ③ 결승 |
| 판정 | judge 5점 척도 (폭 0.05점, 변별 실패) | **1차 정량(기계) → 2차 judge(블라인드 분야별)** |
| 평가셋 | v2 89세그 / 함정 29쌍 | **v3.1 128세그 / 함정 72쌍** (`유지` 8 → 41쌍) |
| 대상 | 벤더당 플래그십 1종 | **벤더당 3티어 예선 → 대표 1종** |

---

##### 1단계 — 문제는 모델이 아니라 지시였다

프롬프트 규칙 하나만 바꿔 `gemini` 1종으로 대조했다.

| variant | 규칙 2 | 함정 정답률 |
|---|---|---|
| `v0_baseline` | "표에 있으면 반드시 바꾼다" (현행) | **33%** — 치환 15/15, **억제 0/30** |
| `v1_exception` | 바꾸지 말아야 할 문맥 **3종을 열거** | 75% — **열거한 3유형 30/30, 열거 안 한 3유형 1/19** |
| **`v6_principle`** | **열거 대신 원리** — "브랜드가 효능을 주장하는 자리에만 적용" | **89%** |

**`v0_baseline`이 만든 출력은 영어로 성립조차 하지 않았다** — `주름 개선 효과는 의학적으로 입증된 바 없습니다` → `not dermatologist-informed proven`.

**열거식은 답이 될 수 없다는 것이 실측으로 확정됐다.** 가르친 것만 하며, 실서비스에는 열거하지 않은 문맥이 계속 나온다. → **확정 구조 = `v6_principle`**

##### 2단계 — 예선 11종. 티어 낙폭이 벤더마다 다르다

| 벤더 | 상위 | 중위 | 하위 | 성격 |
|---|---|---|---|---|
| Anthropic | 92% | 64% | 53% | 단계적 하락 — **상위만 쓸 만함** |
| OpenAI | 91% | 90% | 86% | **평탄 — 하위까지 쓸 만함** |
| Google | 100%* | 93% | 45% | 중위까지 버티다 절벽 |
| Alibaba | *제외* | 82% | 58% | `qwen3.8-max`·`3.8-flash`는 **소요로 탈락** |

**"하위 티어로 충분한가"에 벤더 공통 답은 없다.**

`qwen3.8-flash`는 1문서 305초 / 출력 16,174토큰(전체 환산 86분)으로 **품질과 무관하게 서비스 부적합** 판정했다. `qwen3.8-max`도 같은 기준(문서당 2.7분)에 걸려 제외했다.

> **사고 모드 실측** — `qwen3.7-plus`의 사고 모드를 끄면 소요 14배·비용 6배가 개선되나
> **정답률이 70% → 55%로 떨어진다**(억제가 무너지고 치환만 남는다). Alibaba는 둘 다 가질 수 없다.

##### 3단계 결승 — 최종 결과

judge 2종(`claude-opus-5` × `gemini-3.1-pro-preview`) **블라인드 교차 채점**. 모델명·토큰량·소요를 채점자에게 넘기지 않는다.

| 모델 | GA | 함정 정답률 | `instruction` | `hallucination` | `quality` | 부당 | 양측 저점 | 일치율 | 중앙 tok/s | 변동 | 출력tok | 비용 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `gemini-3.1-pro-preview` | **preview** | **100%** | **9.97** | 9.91 | **9.36** | **0** | **1** | 88% | 11.9 | **117배** | 7,238 | 0.1294$ |
| **`gemini-3.8-flash`** | **GA** | **93%** | **9.77** | 9.87 | 9.02 | **5** | 8 | 89% | 62.7 | 19배 | 6,628 | **0.0408$** |
| **`claude-opus-5`** | GA | 92% | 9.74 | **9.96** | 9.30 | **5** | **1** | 85% | 72.3 | **2배** | 11,515 | 0.4534$ |
| `gpt-5.6-sol` | GA | 92% | 9.28 | 9.89 | 9.23 | 15 | 12 | 88% | 58.2 | 2배 | 7,965 | 0.2423$ |
| `gpt-5.6-terra` | GA | 90% | 9.43 | 9.89 | 8.95 | 9 | 8 | 80% | **79.1** | 3배 | **5,927** | 0.1126$ |
| `qwen3.7-plus` | GA | 82% | 8.99 | 9.72 | 9.19 | 20 | 13 | 86% | 57.0 | 14배 | 53,358 | 0.0934$ |

- 함정 정답률 = 기계 detector. 규제 표를 문맥에 맞게 적용했는가 (72쌍)
- `instruction`·`hallucination`·`quality` = judge 2종 평균, 1~10점
- `부당` = judge가 규제 적용을 잘못했다고 본 건 (양방향)
- `양측 저점` = 두 채점자가 **모두** 7점 이하로 본 건. **판정 반영분**
- 일치율 = 두 채점자의 저점 판정 일치. **70% 미만이면 judge 축 폐기** — 전원 통과
- 전 모델이 필수 통과 조건(JSON 100% / 세그먼트 누락 0 / 미번역 잔존 0 / 숫자 보존 100%)을 충족했다
- **육안 확정 완료** — 기계가 `?`로 보류한 13건을 전수 확인해 전부 `O`로 확정했다.
  11건은 직역형 사전 누락(`therapeutic`·`renewal`·`pore shrinkage` 등 정상 번역),
  2건은 「트러블」이 한국어권 조어라 `Blemish`가 자연 표기라는 판단이다.
  근거는 `poc/C_translate/review_confirm.py`에 건별로 기록했다. **`O`/`X` 판정은 덮어쓰지 않았다**

**기계 detector와 블라인드 LLM judge가 서로 다른 방법으로 같은 순서를 가리킨다.**

##### `gemini-3.1-pro-preview`는 채택 불가 [확정 2026-08-25 / 예람님]

정식 버전이 아니다. **Google의 3.x 세대 Pro는 전부 preview뿐이며 GA Pro는 2.5 세대가 마지막**이다.

preview를 빼면 Google 대표는 **`gemini-3.8-flash`**다. 예선 중위였던 `gemini-3.7-flash`(85%)를
같은 단가로 교체해 **93%**를 얻었다 — C 제품명(명시)을 0/5 → 5/5로 회복했다. 세대 차였다.

> **조사 누락 기록** — 2단계 예선 모델 선정 시 `gemini-3.8-flash`가 조사 목록에서 빠졌다.
> 3.7-flash와 단가가 같으므로 처음부터 이쪽이 중위 후보였어야 했다.

**GA 모델은 전부 C 제품명(암시) 0/5다.** `(제품명)` 표기 없이 제품명을 알아본 것은 `pro-preview`뿐이다.
→ 실서비스에서는 **상류(B/A)가 `role` 정보를 넘기면 해소된다.** 4장 체크리스트 `role` 항목과 직결.

##### 소요 시간 — 지표를 총 소요에서 중앙 생성 속도로 바꿨다

총 소요는 이상치에 지배되어 판정에 쓸 수 없다. `gemini-3.1-pro-preview`는 같은 모델·같은 17문서를
3회 실행해 **601s / 752s / 2,208s**로 흔들렸고, 가장 느린 문서는 **197토큰에 635초**(0.31 tok/s)였다 — 생성이 아니라 대기다.

**중앙 생성 속도와 변동폭은 노이즈에 강하며, 둘 다 모델 특성이다.**

| 모델 | 중앙 tok/s | 변동폭 |
|---|---|---|
| `gpt-5.6-terra` | **79.1** | 3배 |
| `claude-opus-5` | 72.3 | **2배** |
| `gemini-3.8-flash` | 62.7 | 19배 |
| `gpt-5.6-sol` | 58.2 | 2배 |
| `qwen3.7-plus` | 57.0 | 14배 |
| `gemini-3.1-pro-preview` | **11.9** | **117배** |

`pro-preview`는 3회 실행 모두 중앙 속도 최하위였다. **총 소요만 흔들린 것이 아니라 실제로 느리다.**

##### 규제 표 스펙 요구안 — 데이터 담당 합의 입력물

검증 과정에서 **현재 더미 표의 구조적 결함 3종**이 드러났다. 실제 표 작성 시 반영이 필요하다.

| # | 요구 | 근거 |
|---|---|---|
| 1 | `제외문맥`을 **열거하는 방식은 한계가 있다** | 열거식 프롬프트가 미열거 문맥에서 1/19 |
| 2 | **적용 범위**(자사 표시면 포함 여부)를 명시할 것 | UI 라벨 판정이 세 번 뒤집혔고 그때마다 순위가 바뀌었다 |
| 3 | 대체표현은 금지표현의 **직역과 어휘적으로 구별**될 것 | `재생 → renewing`은 직역과 구별 불가 → **판정 자체가 불가** |
| 4 | 서로 다른 금지표현이 **같은 대체표현으로 수렴하지 말 것** | `노화 방지`·`안티에이징` → 둘 다 `age-defying` → `Age-defying and age-defying` |
| 5 | 대체표현이 원문의 **숫자·고유명사를 지우지 말 것** | `100% → pure` → 수치 소실 |

**결과 기록**

| 후보 모델 | 함정 정답률 | judge 3축 | 구조화 출력 | 비용 | 중앙 tok/s | 비고 |
|---|---|---|---|---|---|---|
| **Google** `gemini-3.8-flash` | **93%** | 9.77 / 9.87 / 9.02 | 17/17 | **0.0408$** | 62.7 | GA. 비용 최저권 |
| **Anthropic** `claude-opus-5` | 92% | 9.74 / 9.96 / 9.30 | 17/17 | 0.4534$ | 72.3 | 품질·안정성 최고 |
| **OpenAI** `gpt-5.6-sol` | 92% | 9.28 / 9.89 / 9.23 | 17/17 | 0.2423$ | 58.2 | |
| **Alibaba** `qwen3.7-plus` | 82% | 8.99 / 9.72 / 9.19 | 17/17 | 0.0934$ | 57.0 | 출력 53,358tok |
| *(참고)* `gemini-3.1-pro-preview` | *100%* | *9.97 / 9.91 / 9.36* | *17/17* | *0.1294$* | *11.9* | **preview — 채택 불가** |

**C 누적 지출: 약 14.2$**

**최종 결정**: _미정_ — **확정하지 않는다.** 사유 3가지.

| # | 미해결 |
|---|---|
| 1 | **규제 매핑 테이블이 테스트용 더미다.** 위 결과 전체가 실제 표 확보 전까지 잠정이며, 변별의 대부분이 규제 함정에서 나오므로 순위가 바뀔 수 있다 |
| 2 | **채택 우선 축이 미정.** 규제 처리 1위(`gemini-3.8-flash`) · 번역 품질 1위(`claude-opus-5`) · 속도 1위(`gpt-5.6-terra`) · 비용 1위(`gemini-3.8-flash`)가 갈린다. **비용은 11배 차이** |
| 3 | **육안 확정 일부 완료** — 함정 불명 13건은 확정했으나 **번역 품질 A/B/C 등급이 미착수**다. 작업지: `poc/C_translate/REVIEW.md` |

---

### 2.4 E2. 번역문 배치 (렌더링) — **PoC 제외**

**결정**: **HTML/CSS 배치 + 헤드리스 브라우저 캡처로 확정.** 기술 후보 비교를 수행하지 않는다.
- 결정일: 2026-08-20 / 결정 주체: 예람님
- 근거: 줄바꿈·정렬·다국어 처리를 브라우저가 담당, 검수 UI(웹)와 렌더링 스택 공유 가능

**역할 경계**: 렌더링 구현·검증은 **프론트엔드·백엔드 담당** 소관. AI 담당은 입력 데이터(`module_coords`·`font_size`·`font_color`·`role`)를 규격대로 공급하는 데까지 책임진다.

⚠️ 개발계획문서 6.5는 "PoC에서 결정, 미확정"으로 기재 — 결과 정리 시 함께 수정 필요.
⚠️ **인수인계 필요 항목**: 6.5에 정의된 좌표 정합·폰트 매핑·길이 자동조정(`auto_shrunk` 신호)·출력 후처리(2MB·RGB)는 프론트·백엔드에 전달돼야 함. **전달 여부 미확인.**

---

### 2.5 F. 배경 가공 (후순위 — 여력 시)

> 개발계획문서 6.7 기준 MVP 제외·최종 단계 과업. 본 PoC에서는 실현 가능성 탐색 수준으로 다룬다.

**목적**: 원문 지운 자국·조각 이음새·색 불연속을 보정할 수 있는지 확인.

**선행 조건**: E1 결과물 필요 (지운 자국이 있어야 보정 대상이 생김). **E1 검증 완료 후 착수.**

**후보 기술**
- E1의 LaMa 인페인팅을 다른 입력(이음새·여백·잔여 자국)에 재적용 — 6.7의 "E1 재활용" 방침
- OpenCV 고전 색보정·블렌딩 (색상·톤 정합용)

**판정 기준**: E1과 동일한 A/B/C 육안 등급. 단 F는 완성도 개선 과업이라 Go/No-Go 판정 대상이 아니며 개선 폭 확인 위주.

**용어 정정 [2026-08-20]**: 원안의 "조각 이음새"는 조각끼리 맞닿은 선이 아니라 **조각과 캔버스가 만나는 경계, 즉 여백**을 뜻한다. 처리 목표는 선을 지우는 것이 아니라 **여백을 배경으로 채우고 경계 색을 자연스럽게 잇는 것**이다.

**결과 기록 (2026-08-20)**

| 처리 유형 | 실행 | 대상 | 개선 여부(육안) | 비고 |
|---|---|---|---|---|
| 지운 자국 보정 | `ring_lama` (LaMa 재적용, 경계 링 마스크) | E1 결과 12장 | | **판정 미완** |
| 색상·톤 정합 | `seamless` (OpenCV 포아송 블렌딩) | E1 결과 12장 | | **판정 미완** |
| 경계 이음 | `feather` (마스크 경계 블러·알파 혼합) | E1 결과 12장 | | **판정 미완** |
| **여백 채우기** | 6종 (아래) | `sample_1~3` | | 측정치는 아래 표 |

판정표: `poc/F_background/summary.md` (E1 잔여 자국 3종, 36행 미기입)

---

#### 여백 채우기 실측

**마스크 설계**: 흰 캔버스 영역 + **조각 안쪽으로 8px 침투**. 흰 부분만 정확히 채우면 조각 가장자리 선이 그대로 남아 딱딱해지므로, 경계선 자체를 마스크에 넣어 다시 그리게 한다. 흰 옷·흰 종이가 캔버스로 오인되지 않도록 **화면 가장자리에 닿는 큰 덩어리**만 캔버스로 잡는다.

| 이미지 | 흰 여백 | 마스크(침투 후) |
|---|---|---|
| sample_1 | 48.2% | 49.3% |
| sample_2 | 34.3% | 38.9% |
| sample_3 | 55.6% | 58.9% |

| 기법 | 계열 | 3장 소요 | 결과 |
|---|---|---|---|
| `gap_telea` | OpenCV 고전 | 3.0s | **가장 깨끗함** |
| `gap_ns` | OpenCV 고전 | 2.7s | telea와 비슷 |
| `gap_extend` | 최근접 화소 확장 + 블러 | **0.25s** | 매끄러우나 밋밋함 |
| `gap_lama` | LaMa | 17.7s | **실패** — 회색 얼룩 |
| `gap_pp_context` | PowerPaint context-aware | 54.5s | **실패** — 여백에 관중 인파 생성 |
| `gap_sd15` | SD1.5 | 231.3s | **실패** — 낙서 무늬 도배, 가짜 한글 생성 |

부수 효과 — `sample_2`의 조각 내부 이음새(y=498, 샘플 제작 시의 실수)를 마스크에 포함시킨 결과. 행 평균 밝기 편차로 측정.

| | 편차 | 감소율 |
|---|---|---|
| 원본 | +25.1 | — |
| `gap_telea` | **-0.17** | **99%** |
| `gap_lama` | -1.03 | 96% |
| `gap_ns` | -1.63 | 94% |
| `gap_extend` | +3.04 | 88% |

> 얇은 띠만 좁게 잡아 지웠을 때(`seam_blur` 83%)보다 **여백과 함께 넓게 처리한 쪽이 깨끗했다.**

---

#### 확인된 사실 (판정과 무관하게 확정)

**1. 과업마다 유리한 기법 계열이 다르다**

| 과업 | 필요한 일 | 유리한 계열 |
|---|---|---|
| E1 원문 지우기 | 가려진 **질감 복원** | **LaMa** (83%) — 고전은 17~25% |
| F 여백 채우기 | 가장자리 배경의 **바깥 연장** | **OpenCV 고전** — LaMa는 실패 |

E1에서 압승한 LaMa가 여백 채우기에서는 무너졌다. "무엇을 채우는가"가 다르기 때문이며, 파이프라인 단계마다 기법을 따로 골라야 한다.

**2. LaMa는 마스크 면적에 취약하다**

E1에서 실패한 2장이 마스크 47%·32%였고, 여백 채우기 마스크는 49~59%였다. 2.2 "마스크 팽창 폭"의 면적 상한 규칙이 F에도 그대로 적용된다.

**3. SD 계열은 파이프라인 전반에 부적합 — 두 과업에서 반대 방향으로 실패**

| 과업 | 마스크 주변 | 생성된 것 |
|---|---|---|
| E1 | 온통 글자·라벨 | 가짜 영문·라벨 |
| F | 민민한 배경뿐, 단서 없음 | **관중 인파·낙서 무늬** |

E1 예상대로 여백에서는 가짜 글자가 거의 없었다. 대신 **채울 단서가 없으니 학습 데이터의 일반적인 사진으로 채웠다.** 공통 원인은 하나 — SD는 내용을 만들어 넣는 모델인데, 이 파이프라인이 필요로 하는 것은 **아무것도 새로 만들지 않는 것**이다.

> ⚠️ 제품 관점 위험: 상용 상세페이지 여백에 생성 모델이 없던 이미지를 넣으면 **상품 정보를 오도할 소지**가 있다. 품질과 별개로 채택 시 검토 필요.

**4. 이음새·여백 좌표는 검출 대상이 아니라 입력이어야 한다**

자동 검출을 3가지 기준으로 시도해 모두 실패했다.

| 기준 | 실패 원인 |
|---|---|
| 행·열 불연속 강도 | 머리카락·글자 경계가 같이 잡힘 |
| 조각 폭 전체를 가로지르는 불연속 비율 | **가로 글자 줄도 폭 전체를 가로지름.** `sample_1`에서 14개 오검출, 전부 텍스트 |
| 위/아래 색 계단 | 진짜 이음새 6.7 < 글자 줄 48.7. 두 조각이 같은 색으로 이어져 계단이 안 생김 |

단 **흰 캔버스 기준 조각 사각형 추출은 안정적으로 동작**했다. 실서비스에서는 A(리플로우)가 조각을 배치하므로 좌표를 이미 알고 있다 → **A→F 인터페이스에 조각 좌표·여백 영역을 넘겨야 한다.**

**최종 결정**: _미정_ — **확정하지 않는다.** F는 개발계획문서 6.7 기준 MVP 제외 과업이며 본 PoC의 목표도 실현 가능성 탐색까지다. 위 측정치는 실서비스 설계 시 출발점으로만 쓴다.

---

### 2.6 A. 초안 배치 / 리플로우 (후순위 — 여력 시)

> 개발계획문서 6.6 기준 MVP 제외·고도화 대상. "연구 수준 난이도"로 명시돼 있어, 본 PoC에서는 **방법 탐색과 실현 가능성 판단**까지만 목표로 한다.

**목적**: 세로형 상세페이지 조각을 가로/정사각 규격으로 재배열 가능한지 확인.

**핵심 선행 과제**: 6.6이 지적한 대로 실질 난관은 배치가 아니라 **조각의 의미 분류**(제품컷/설명/표/로고 등). 이것부터 확인해야 배치 규칙 적용이 가능하다.

**후보 기술**
- MLLM 레이아웃 생성 (VFLM 계열, arXiv 2603.22187 — 렌더링 결과를 시각 피드백으로 반복 개선)
- 규칙 기반 배치 + 조각 분류 모델 조합

**판정 기준**: 배치 규칙 4종(결합·순서·우선순위·규격 적합)이 지켜지는지 육안 확인.

**결과 기록**

| 후보 방식 | 조각 의미 분류 가능? | 배치 규칙 준수(육안) | 비고 |
|---|---|---|---|
| MLLM 레이아웃 생성 | | | |
| 규칙 기반 + 분류 모델 | | | |

**최종 결정**: _미정_

---

### 2.7 G. 제품컷 누끼 (즉시 착수)

> **PoC 진행 중 추가된 과업. 개발계획문서에 없다** — 4장 체크리스트 반영 대상.
> 기존 우선순위 체계(1~3순위·후순위) 밖이다.

**목적**: 상세페이지에서 **제품컷을 배경과 분리**할 수 있는지 확인.

**대상 [확정 2026-08-20 / 예람님]**: **제품컷만** — 화장품 용기·패키지. 인물·소품·그래픽 요소는 대상이 아니다.

**선행 조건**: 없음. `data/images/` 12장을 그대로 쓴다.

> ⚠️ 표본은 B·E1용으로 고른 것이라 **텍스트 위주**다. 제품컷이 뚜렷한 것은 `5.jpg`·`7.jpg`·`9.jpg`·`11.jpg`·`12.jpg` 정도. 제품이 작게 나온 이미지에서 등급이 낮게 나올 수 있으며, 이는 기술 한계가 아니라 표본 특성일 수 있다. 판정 시 감안할 것.

**후보 기술** — 전부 로컬 실행, API 비용 0

| 계열 | 후보 | 비고 |
|---|---|---|
| 범용 배경제거 | `rembg` (U²-Net) | 가장 널리 쓰임. 제품 사진에 강함 |
| | `transparent-background` (InSPyReNet) | 경계 품질이 좋다고 알려짐 |
| | `BiRefNet` | 최신 계열. GPU 유리 |
| 프롬프트 기반 분할 | `SAM 2` | 점·박스로 대상 지정. **제품만 고르는 데 유리할 수 있음** |
| 고전 | OpenCV GrabCut | 대조군 |

> 상세페이지에는 제품 외에 인물·소품·그래픽이 함께 있다. 범용 배경제거는 **전경 전체**를 따내므로 제품만 고르려면 후처리가 필요할 수 있다. SAM 2는 대상을 지정할 수 있으나 지정 좌표를 누가 주느냐가 문제다(B의 bbox는 텍스트용이라 제품 위치를 모른다).

**출력 형식 [확정 2026-08-20 / 예람님]**: **투명 PNG와 마스크 PNG를 모두 저장**한다.
- 투명 PNG(알파 채널) — 재배치·합성에 바로 사용
- 마스크 PNG(흑백) — 다른 단계 입력으로 전달

시각화는 투명 PNG를 체크무늬 배경에 얹어 경계 품질이 드러나게 한다.

**판정 기준 [제안]** — E1의 A/B/C를 누끼에 맞게 옮긴 것. 팀 확인 필요

| 등급 | 기준 |
|---|---|
| A | 제품 윤곽이 정확히 따짐. 배경 잔여·제품 잘림 없음 |
| B | 경계가 약간 거칠거나 미세한 잔여가 있으나 **후처리로 흡수 가능** |
| C | 윤곽이 크게 어긋나거나 제품 일부가 잘림 — 사용 불가 |

**판정 단위 [제안]**: `이미지 × variant`. 한 이미지에 제품이 여러 개인 경우가 많아 영역 단위로 가면 분량이 커진다(B에서 같은 이유로 이미지 단위를 택했다).

**결과 기록 (2026-08-20) — 판정 미완**

환경: `.venv-g` 별도 생성(rembg가 `onnxruntime`·`opencv-python-headless`를 끌어와 기존 환경을 깨뜨릴 수 있음). CPU 실행, 비용 0.

| variant | 모델 | 파일 크기 | 이미지 | 장당 소요 | 등급 |
|---|---|---|---|---|---|
| `rembg_u2net` | `u2net` | 167.8 MB | 1장 | 0.47s | _판정 미완_ |
| `rembg_isnet` | `isnet-general-use` | 170.4 MB | 1장 | 0.87s | _판정 미완_ |
| `rembg_birefnet` | `birefnet-general` | **927.6 MB** | **12장** | 9.7s | _판정 미완_ |
| `rembg_birefnet_dis` | `birefnet-dis` | 약 900 MB | 6장 | 10.3s | _판정 미완_ |

출력은 투명 PNG·마스크·체크무늬 시각화 3종. 위치는 `poc/G_cutout/results/{variant}/{rgba,mask,vis}/`

---

#### 확인된 사실 — 네 모델 모두 "제품인지"를 판단하지 못한다

차이는 **무엇을 전경으로 보느냐**일 뿐이다.

| 모델 | 전경으로 보는 것 |
|---|---|
| `u2net` | 가장 두드러진 하나 — **인물만**. 제품을 통째로 놓침 |
| `isnet-general-use` | 인물 + 큰 텍스트. 제품 누락 |
| **`birefnet-general`** | 인물 + **제품** + 텍스트 일부 |
| `birefnet-dis` | 인물 + 제품 + **텍스트 대부분** |

`u2net`은 salient object detection 계열로 "가장 눈에 띄는 물체 하나"를 찾도록 학습됐다. **상세페이지는 인물·제품·그래픽·텍스트가 함께 있는 합성 레이아웃이라 "단일 주제 사진"을 전제한 모델과 맞지 않는다.**

**`birefnet-dis`는 기대와 반대로 나왔다.** "사물 특화라 인물을 덜 잡을 것"으로 예상했으나, DIS(dichotomous image segmentation)의 "사물"은 *인물이 아닌 것*이 아니라 *경계가 복잡한 대상*이었다. 인물을 그대로 잡으면서 **글자 획까지 추가로 분리**해 덩어리 수가 일관되게 늘었다(5.jpg 4→17, 9.jpg 5→22, 8.jpg 176→247).

**현재 최선은 `birefnet-general`.**

#### `birefnet-general` 12장 — 이미지 유형별로 결과가 갈린다

| 유형 | 이미지 | 결과 |
|---|---|---|
| **제품만, 인물 없음** | `5` `7` `9` `11` | **우수** — 제품컷이 정확히 분리됨. 과업 목표 달성 |
| 인물 + 제품 | `1` `3` `10` `12` | 인물이 함께 따짐. 제품만 고르려면 후처리 필요 |
| **텍스트 위주, 제품 없음** | `4` `6` `8` | **실패** — 텍스트·아이콘을 전경으로 인식 |

`4.jpg`는 `My First Collagen` 글자와 종이 조각을, `8.jpg`는 아이콘·글자 파편 176개를 따냈다.

> **핵심 — 이것은 선별 문제가 아니라 분류 문제다.** 범용 배경제거는 "전경 vs 배경"까지만 하고, **전경 안에서 제품만 고르는 것은 별도 단계**가 필요하다.
>
> 이는 문서 2.6이 A(초안 배치)의 진짜 난관으로 지목한 **"조각의 의미 분류"**와 같은 벽이다. G와 A가 동일한 문제에 부딪혔다. 4장 체크리스트의 `role` 산출 과업과도 직결된다.

#### 후속 단서 — 덩어리 수가 판별에 쓰일 수 있다

| 이미지 | 덩어리 수 | 실제 |
|---|---|---|
| `5.jpg` | 4 | 제품만, 우수 |
| `9.jpg` | 5 | 제품 위주 |
| `7.jpg` | 149 | 텍스트 파편 다수 |
| `8.jpg` | 176 | 텍스트만, 실패 |

**덩어리가 적고 크면 제품, 많고 잘면 텍스트**라는 경향이 보인다. 면적·종횡비와 함께 후처리 규칙의 단서가 될 수 있다. 미검증.

**최종 결정**: _미정_ — 판정 미완. 후처리 규칙 또는 대상 지정 방식(SAM) 검토가 남았다.

---

## 3. 진행 로그

시간순으로 실제 작업 내역을 기록. (표 형식으로 두되, 상세 메모는 아래 자유 기록란에)

| 날짜 | 과업 | 내용 | 담당 |
|---|---|---|---|
| ~~2026-08-19~~ | ~~E1~~ | ~~bbox/background_type 수동 라벨링 도구 제작 완료~~ — **무효**. 저장소에 실물 없음, 8/20 라벨링 폐기로 불필요해짐 | Claude |
| 2026-08-20 | B | PaddleOCR 샘플 실행 — 정확도 미흡 확인. 전처리 개선 검토 착수 | 예람님 |
| 2026-08-20 | 전체 | 후보 기술 간추림 — B는 PaddleOCR 단독(전처리 비교 중심), E2는 HTML+헤드리스로 확정해 PoC 제외. 우선순위 B→E1→C로 변경, F·A를 PoC 범위에 포함 | 예람님 |
| 2026-08-20 | 전체 | 평가 방식 확정 — 정량 지표 폐기, 전 과업 육안 A/B/C 등급으로 통일. OCR 전처리는 3단계 게이트 방식 | 예람님 |
| 2026-08-20 | 전체 | **수동 라벨링 폐기** — 육안 판정이라 정답 데이터 불필요. E1 마스크는 B의 OCR bbox 출력을 사용. 개발계획문서 v2.2도 참조 대상에서 제외 | 예람님 |
| 2026-08-20 | 전체 | **배경유형을 영역 단위로 변경** — 표본 이미지가 한 장에 단색·사진 구간을 함께 가져 이미지 단위 분류 불가. `data/test_images/` 평면 배치, 배경유형은 등급표에 영역별 기입. 2.1.2 3단계(배경유형 분기) 폐기 | 예람님 |
| 2026-08-20 | 전체 | CLAUDE.md 디렉토리 규칙 개정 — 과업별 폴더(`poc/B_ocr/` 등 5종) 착수 시점 생성, 과업 간 코드 비공유 | Claude |
| 2026-08-20 | B | 환경 구축 — `.venv`, paddlepaddle-gpu 3.3.1(cu126), paddleocr 3.7.0. RTX 4060 8GB에서 GPU 구동 확인 | Claude |
| 2026-08-20 | B | `poc/B_ocr/` 착수 — `run.py`(variant 실행·bbox 시각화), `compare.py`(비교표 생성). 표본 12장 | Claude |
| 2026-08-20 | B | **1단계 3 variant 실행 완료.** 등급 판정 대기 — 아래 표 참조 | Claude |
| 2026-08-20 | 전체 | 1장 실행 주체 표 갱신 — Claude가 예람님 PC의 RTX 4060을 직접 쓸 수 있음이 확인돼 로컬 GPU 과업(PaddleOCR·LaMa·SD·F)을 Claude로 이관. API 키 과업만 예람님 유지 | 예람님 |
| 2026-08-20 | B·E1 | 판정 단위 확정 — B는 이미지 단위(등급 4열), E1은 영역 단위. 분량 때문에 갈림 | 예람님 |
| 2026-08-20 | B | **1단계 후보 2종 제외** — `det_side_1280`(baseline과 결과 동일), `vl_spotting`(속도 300배). baseline 12행만 판정. 1단계에 남은 모델·파라미터 카드 없음 | 예람님 |
| 2026-08-20 | 전체 | **정량 지표 미도입 재확인** — B는 라벨링 반나절이면 가능하나 n=12에서 변별력 낮고, E1·F·A는 정답 이미지가 원리적으로 없어 합성 데이터가 필요(측정 대상이 바뀜). 육안 A/B/C 유지 | 예람님 |
| 2026-08-20 | B | `PP-OCRv5_server_rec` 실측 — 한글 미지원 확인(빈 문자열). 한국어 인식 모델 상향 불가 확정. 라틴은 baseline보다 정확해 `rec_hybrid`를 보류 카드로 기록 | Claude |
| 2026-08-20 | B | **진행 방침 확정** — baseline 70% 미달 시 1단계 추가 실험 없이 2단계 이미지 전처리로 진행 | 예람님 |
| 2026-08-20 | **B 종료** | **PaddleOCR 기본 설정 채택 확정.** 육안 확인 결과 대부분 A로 70% 통과 판단. 전처리 미적용, 2단계 생략. 개별 등급표는 미기입 상태 | 예람님 |
| 2026-08-20 | B | 2.1.1 상용 OCR 참조 측정 **생략 확정** — 채택이 끝나 비교 목적 소멸. 유료 호출 집행 0 | 예람님 |
| 2026-08-20 | E1 | `poc/E1_inpaint/` 착수 — `make_mask.py`(B poly를 글자 높이 15% 팽창), `run.py`, `compare.py` | Claude |
| 2026-08-20 | E1 | `data/test_images/` → `data/images/` 폴더명 변경 반영 (CLAUDE.md·B·E1 경로 수정) | 예람님 |
| 2026-08-20 | E1 | **대표 4장(1·5·9·11)에 3종 실행 완료** — OpenCV Telea/NS, LaMa. 판정 대기 | Claude |
| 2026-08-20 | E1 | 12장 전체 확대 실행 후 **이미지 단위 판정 완료** — LaMa 83%, Telea 25%, NS 17%. OpenCV 2종 제외 | 예람님 |
| 2026-08-20 | E1 | SD 계열 검증 — iopaint 배치 경로의 `disable_nsfw` 버그로 PowerPaint 실행 불가, `_iopaint_driver.py`로 우회 | Claude |
| 2026-08-20 | E1 | **SD 계열 탈락 확정** — 설정 5종 시도 전부 가짜 생성. 대조군 `1.jpg`(LaMa A) 파괴가 결정적. `hd_strategy`가 SD에 적용 안 됨을 코드로 확인해 문맥 가설 기각 | 예람님 |
| 2026-08-20 | E1 | 판정표 `대상` 열(번역대상/제품고유) **미도입 결정** — PoC 범위 밖. 대신 `role` 산출 과업 누락을 4장 체크리스트에 등재 | 예람님 |
| 2026-08-20 | E1 | 마스크 팽창 폭 d10 비교 실행 — 잔글씨는 두 설정이 동일(최소 3px), 차이는 큰 글자에서만 발생함을 확인. **상수가 아닌 규칙으로 설계해야 한다는 결론**, 4장 체크리스트 등재 | 예람님 |
| 2026-08-20 | E1 | LaMa GPU/CPU 소요 실측 — 장당 추론 11.3s→2.9s(약 3.9배). 단 고정비가 CPU 13s·GPU 19s라 1장만 돌리면 이득 없음. 서버 상주 시 유효 | Claude |
| 2026-08-20 | **E1 종료** | **Go 확정 — 사진 배경도 MVP 포함.** 채택 기술 LaMa. 사진·그라데이션 영역을 함께 확인한 육안 판정에 근거. 문서 4.3·5.1의 "9월 이전 MVP 범위 결정" 해소 | 예람님 |
| 2026-08-20 | F | `poc/F_background/` 착수 — E1 잔여 자국 보정 3종(`ring_lama`·`seamless`·`feather`) 12장 실행. 판정 미완 | Claude |
| 2026-08-20 | F | 조각 합성 테스트 이미지 `sample_1~3` 추가. **"조각 이음새" 용어 정정** — 조각끼리 맞닿은 선이 아니라 조각-캔버스 경계, 즉 여백 채우기가 목표 | 예람님 |
| 2026-08-20 | F | 이음새 자동 검출 3가지 기준 전부 실패(가로 글자 줄과 구분 불가). **좌표는 A가 제공해야 할 입력**으로 정리, 수동 지정 경로 추가 | Claude |
| 2026-08-20 | F | 여백 채우기 6종 실행 — **고전 기법(telea) 최선, LaMa·SD 계열 실패.** E1에서 압승한 LaMa가 여기서 무너져 과업별 기법 선택이 필요함을 확인 | Claude |
| 2026-08-20 | F | **확정 없이 종료** — MVP 제외 과업이라 최종 결정을 내리지 않고 측정치만 기록 | 예람님 |
| 2026-08-20 | C | `poc/C_translate/` 착수 — 평가셋(문서 10/세그먼트 88), 프롬프트, 비용 추정기, 실행기 구축. **합의 3건 대기로 실행은 막아둠** | Claude |
| 2026-08-20 | C | 설정 확정 — **목표 언어 한국어→영어**, judge 5점 척도. 통과선은 결과 보고 결정. 규제 표는 테스트용 더미로 대체 | 예람님 |
| 2026-08-20 | C | **세션 예비 실행**(비용 0) — 프롬프트·출력형식 배선 검증. 문제 2건 발견: OCR 오류가 번역 품질과 섞임(88건 중 17건 추정 필요), 규제 표현 0건 등장으로 regulation 축 검증 불가 | Claude |
| 2026-08-20 | C | **평가셋 방식 변경** — 원문 OCR이 완벽하다는 가정으로 합성 데이터 채택. 문서 8/세그먼트 64, 규제 표현 8종 전부 포함. 1차 평가셋은 `evalset_ocr.json`으로 보존 | 예람님 |
| 2026-08-20 | C | 합성 평가셋 세션 예비 실행 + `judge.py` 작성. 규제 표를 문맥 무시하고 적용하면 법정 고지문이 깨진다는 가설 도출 | Claude |
| 2026-08-20 | C | OpenAI 키 확보. 후보 모델 `gpt-5.5` 선정(claude-opus-5와 급 맞춤), 가격 공식 문서 확인 | 예람님 |
| 2026-08-20 | C | **1차 API 실행 — `gpt-5.5` 번역 8회, 실측 0.1460$.** 구조화 출력 안정성 첫 검증(JSON 파싱 실패 0건). **규제 표 가설이 실증됨** — 8건 중 7건 정확 적용, 법정 고지문 1건에서 기계 치환. 4장 체크리스트 등재 | Claude |
| 2026-08-20 | C | **2차 API 실행 — `claude-opus-5` 번역 8회, 실측 0.1291$** (추정 대비 101%). 2종 누적 0.2751$. **규제 표 진단 확정** — 서로 다른 벤더 플래그십 2종이 동일하게 기계 치환 → 모델 문제가 아니라 표 설계 결함 | Claude |
| 2026-08-20 | C | judge 캐시 키 수정 — `judge_{채점자}_s{척도}_{대상}`. 채점자·척도가 바뀌면 캐시 무효화되도록 | Claude |
| 2026-08-20 | C | **3차 API 실행 — judge 채점 16회, 실측 0.4864$.** 2종 전체 평균 4.72 vs 4.77. 배선 통과(파싱 실패 0건). **점수 격차 0.05점으로 변별 불가 + 자기 채점 편향** → 채택 근거로 못 씀 | Claude |
| 2026-08-20 | C | **통과선 결정 보류** — 후보 4종이 모여 변별이 생긴 뒤 정하기로. 지금 정하면 임의값 | 예람님 |
| 2026-08-20 | C | `qwen3.8-max` 번역 — **추정 543% 초과.** 출력의 91%가 추론 토큰임을 `usage`로 확인. 사고 모드 유지 결정. 새 모델은 1건 선실측 후 추정하는 절차 도입 | 예람님 |
| 2026-08-20 | C | `qwen-mt-plus` **참고 측정** — 번역 전용 모델. 시스템 프롬프트·JSON 불가로 벤치마크 제외하되, 법정 고지문을 유일하게 통과. 파이프라인 분리 검토 근거로 기록 | 예람님 |
| 2026-08-20 | C | **`gemini-3.1-pro-preview` 추가 — 후보 4종 완료.** judge 격차가 2·3·4종 모두 0.05로 동일 → **절대 점수 척도로는 변별 불가 확정** | Claude |
| 2026-08-20 | C | **평가셋·용어집 확장** — 문제가 표본 양이 아니라 난이도임을 실측으로 확인(20자 이하 80%, length 96% 만점). 89세그먼트·용어집 15항목·함정 6유형으로 확장 | 예람님 |
| 2026-08-20 | C | 번역 캐시 버그 수정 — 프롬프트 지문(SHA-256)을 키에 반영. 용어집 변경 시 옛 결과 재사용을 막음 | Claude |
| 2026-08-20 | C | **4종 재번역 0.9383$.** 함정 점검에서 **judge 없이 변별 확보** — C 제품명 함정에서 2:2 분기(GPT·Qwen 통과), A 부인·D 인용은 4종 전부 실패. 체크리스트 2건 등재 | Claude |
| 2026-08-20 | C | `compare.py` 개편 — 규제 점검을 "치환 여부"에서 **"기대 동작 대비 정오"**로. 함정 정의(`TRAPS`)를 평가셋 원본에 배치. judge 점수 절 추가 | Claude |
| 2026-08-20 | C | **집계 사고 + 정정** — 미실행 세그먼트를 오답으로 세어 `qwen-mt-plus` 정답률을 52%로 오독. 확장 25건 실행 후 **76%(상위권 동률)**로 정정. `compare.py`가 미실행을 `—`로 구분하도록 수정. `run_mt.py`에 용어집 지문 캐시·레이트 리밋 재시도 추가 | Claude |
| 2026-08-20 | **C 중단** | **판정 방식 재설계를 위해 검증 중단.** judge 절대 점수가 후보 수와 무관하게 폭 0.05로 변별 불가. 확정 사항과 코드는 승계하고 **새 세션에서 재계획**. 인계 문서 `poc/C_translate/HANDOFF.md` 작성. 누적 지출 약 2.51$ | 예람님 |
| 2026-08-20 | E1 | 나노바나나(Gemini 이미지 편집) 추가 검증 — 방법 A(전체+지시) 2모델, 방법 C(크롭+되붙이기) 1모델 | Claude |
| 2026-08-20 | **E1 추가검증 종료** | **나노바나나 부적합 판정. LaMa 채택 유지.** 방법 A는 출력 크기 1024 고정·전체 재생성으로 좌표계 보장 불가(모델 등급 무관). 방법 C는 그 둘을 해결했으나 **저작권 필터로 영역 거부**·가짜 글자 생성이 남음. 지출 0.641$ | 예람님 |
| 2026-08-20 | F | 나노바나나 여백 채우기 시도 — flash v1은 여백 미채움, **pro v2는 여백을 채웠으나 캔버스 전체를 새 이미지로 재생성**(가짜 한글·가짜 제품 대량). 지시가 강할수록 재생성이 심해짐. `gap_telea` 유지. 지출 0.173$ | Claude |
| 2026-08-20 | **G 신설** | **제품컷 누끼 과업 추가.** 개발계획문서에 없는 PoC 자체 과업. 대상은 제품컷만, 출력은 투명 PNG+마스크 둘 다, 표본은 기존 12장 그대로. 2.7 절 신설·4장 체크리스트 등재 | 예람님 |
| 2026-08-20 | G | `poc/G_cutout/` 착수 — `.venv-g` 별도 생성, `run.py` 작성. rembg 4개 모델 실행(u2net·isnet·birefnet-general·birefnet-dis). 전부 로컬, 비용 0 | Claude |
| 2026-08-20 | G | **결과 기록 — 판정 미완.** `birefnet-general`이 최선이나 **네 모델 모두 "제품인지"를 판단하지 못함.** 제품만 있는 이미지는 우수, 인물 있으면 섞이고, 제품 없으면 텍스트를 따냄. **A의 "조각 의미 분류"와 같은 벽.** 덩어리 수가 후처리 단서로 관측됨(미검증) | Claude |
| 2026-08-20 | E1 | 인페인팅 **API** 추가 검증 — OpenAI `images.edit`(`gpt-image-2`). 마스크 기반이라 나노바나나와 범주가 다름. LaMa 실패 2장 대상 | Claude |
| 2026-08-20 | E1 | **결과 — 조건부 개선.** `4.jpg`(분홍 단색)는 개선, `9.jpg`(다색·복잡)는 **흰색으로 뭉갬**. **약점 축이 LaMa(마스크 면적)와 다름 — API는 배경 복잡도.** 하이브리드 근거 불충분 | Claude |
| 2026-08-20 | E1 | `quality=high` 2장 추가 측정 — 복잡한 배경(`9.jpg`)에서만 개선, 단색(`4.jpg`)은 변화 미미. **비용 3.5배 대비 효용 낮음.** 두 경우 모두 마스크 모양이 결과에 드러나 **판정 기준상 A가 아닌 B 수준.** 총 지출 0.6085$ | Claude |
| 2026-09-08 | 전체 | **기능명세서 개정 반영** — 보완 개발계획서 검증 항목을 완료/결정대기/미착수로 분류. 미착수 6과업 계획을 `PoC_추가검증_계획.md`로 분리 신설. **과업 코드 미배정 결정** — 과업은 이름으로 부름 | 예람님 |
| 2026-09-08 | C | **번역 모델 `gemini-3.8-flash` 확정.** 채택 우선 축을 **규제 처리·비용**으로 결정 — 함정 정답률 1위(93%)·비용 최저. **상태는 `잠정` 유지** — 규제 매핑 표가 더미라 실제 표 확정 시 재검증 필요 | 예람님 |
| 2026-09-08 | 추가검증 | **줄·문단 병합 + 역할 분류 종결.** 휴리스틱 2종 + `llm_assist`(gemini-3.8-flash) 12장 판정. 세 variant 모두 게이트 통과, **`llm_assist` 채택**(병합 92%·역할 92%). 지출 $0.0231. 결과는 `PoC_추가검증_결과.md` | 예람님 |
| 2026-09-09 | 추가검증 | **제품 라벨 판정 종결.** 로컬 3종 + LLM 3종 실행. **미탐 0은 `vlm_relation`(gemini-3.8-flash + 이미지) 하나뿐**이라 채택. 이미지 없는 `role_ext`는 미탐 7(전부 11.jpg 겹친 패키지). `claude-opus-5`는 판정 100% 동일해 모델 상향 실익 없음. 지출 $0.2568 | 예람님 |
| | | | |

**1단계 실행 실측** (2026-08-20, 표본 12장. 등급 아님 — 판정 전 측정치)

| variant | 검출 영역 | 12장 인식 시간 | 비고 |
|---|---|---|---|
| baseline | 280 | 3.5s | PP-OCRv5_server_det + korean_PP-OCRv5_mobile_rec |
| det_side_1280 | 280 | 3.1s | **baseline과 인식 결과 완전 동일 — 변별력 없음** |
| vl_spotting | 239 | 1042s | PaddleOCR-VL-1.6, spotting 모드 |

기술적 확인 사항 (판정과 무관하게 확정된 사실)

- **`det_limit_side_len` 조정은 이 표본에서 무효.** 기본값 960/max가 1000~1280px 표본을 축소시키는 건 맞으나, 인식은 축소본이 아니라 원본에서 크롭해 수행되므로 결과가 바뀌지 않음. 1단계 항목으로는 음성 결과로 종결
- **한국어 인식 모델은 mobile 등급뿐.** 설치본에 존재하는 건 `korean_PP-OCRv5_mobile_rec`·`korean_PP-OCRv3_mobile_rec` 2종. server 등급 한국어 rec 없음 → 기본 조합이 server_det + mobile_rec 비대칭. 인식 단계 상향 카드가 없음
- **PaddleOCR-VL 기본 모드는 상세페이지에 사용 불가.** 레이아웃 분류기가 사진 구간을 image 블록으로 통째 묶어 내부 글자의 선 단위 좌표가 소실됨(`1.jpg`에서 989×776 영역이 블록 1개). `use_layout_detection=False` + `prompt_label="spotting"`으로 우회해야 선마다 4점 좌표가 나옴. 문서가 인용한 OmniDocBench 96.33%는 문서 이미지 기준이라 상세페이지에 그대로 적용되지 않음
- **VL은 baseline 대비 약 300배 느림**(12장 3.5s vs 1042s). VRAM 7.3/8.1GB 점유로 8GB에 겨우 적재됨. PoC 판정과는 무관하나 채택 시 비용 요인
- VL은 영역별 신뢰도를 제공하지 않음 → 출력 `score`가 `null`. E1은 좌표만 쓰므로 무관
- 표본 12장이 모두 1000~1280px 정사각형에 가까움. 세로로 긴 상세페이지 원본이 아니라 조각 단위임

### 자유 기록

-

---

## 4. 결과 확정 후 개발계획문서에 반영할 사항

PoC 완료 시점에 개발계획문서(v2.2)를 수정할 항목. **결과가 모두 나온 뒤 일괄 처리.**

- [ ] 4.3 PoC 우선순위: E1 1순위 → **B 1순위**로 순서 수정
- [ ] 6.1 OCR 엔진: "Clova vs Google Vision 비교 후 선정" → **PaddleOCR 기본 설정(전처리 없음)**으로 수정. `PP-OCRv5_server_det` + `korean_PP-OCRv5_mobile_rec`
- [ ] 6.5 렌더링 방식: "PoC에서 결정, 미확정" → **HTML+헤드리스 캡처 확정**, 담당을 프론트·백엔드로 명시
- [ ] 6.6·6.7: A·F가 PoC 범위에 포함됐음을 반영
- [ ] **G(제품컷 누끼) 과업 신설** — PoC 진행 중 추가된 과업으로 개발계획문서에 없음(2.7). 파이프라인 어느 단계에 놓을지, A(초안 배치)와의 관계를 정해 반영할 것
- [ ] **규제 표에 "치환 금지 조건"을 명시** — 후보 4종이 **치환은 잘하는데 치환 억제를 못 한다**(2.3 6차 실행). 공통 실패 3유형이 전부 치환하면 안 되는 자리였다. 최소한 ① 부인·고지 문맥(`~보장하지 않습니다`, `~입증된 바 없습니다`) ② 인용부호 안의 제3자 발화 ③ 제품명 세 가지를 금지 조건으로 스펙에 넣을 것
- [ ] **프롬프트 규칙 우선순위 명시 — 제품명 유지 vs 규제 치환** — 둘이 충돌할 때 GPT·Qwen은 제품명을, Claude·Gemini는 규제 표를 우선했다(2:2로 갈림). 어느 쪽이 정답인지 정해 프롬프트에 못박아야 모델 간 편차가 사라진다
- [ ] **규제 규칙 전달 경로 재검토 — 프롬프트 지시 vs 용어집 파라미터** — 범용 LLM 3종(Claude·GPT·Qwen)이 프롬프트 경로에서 법정 고지문을 모두 오역했으나, `qwen-mt-plus`의 용어집 경로는 주장/부인 문맥을 구분했다(2.3 참고 측정). 표본 1문항이라 일반화 불가하나 **전달 경로가 결과를 바꾼다는 신호**다
- [ ] **C 파이프라인 분리 검토 (D 통합 재고)** — 번역은 전용 MT 모델이 담당(26초·출력 604토큰·비용 1/7), 규제 판단은 문맥을 읽는 범용 LLM이 별도 단계로. 문서 6.2의 "D를 C에 통합" 결정을 되돌리는 사안이라 PoC 범위 밖이지만 실측 근거가 확보됐다
- [ ] **규제 매핑 테이블에 문맥 조건 추가** — 단순 문자열 치환표로 두면 법정 고지문이 깨짐. `gpt-5.5` 실행에서 실증됨(2.3): "질환의 **치료**를 목적으로 사용할 수 없습니다"의 `치료`를 `care`로 치환해 고지 기능이 약해짐. **주장 문맥은 치환 / 부인·고지 문맥은 치환 금지**를 스펙에 명시할 것. 데이터 담당 합의 항목
- [ ] **성분 목록 구간은 자동 번역에서 제외하거나 사람 검수 필수** — C 예비 실행에서 성분명 19종이 OCR로 훼손됐고(`페녹시에탄음`→Phenoxyethanol 등) 문맥 추정으로 복원해야 했음. **알레르기 유발 성분 목록이라 추정하면 안전 문제**로 이어짐. `role` 산출 과업(아래)에서 "성분 목록" 유형을 별도로 구분할 것
- [ ] **A→F 인터페이스에 조각 좌표·여백 영역 추가** — F가 여백을 채우려면 어디가 여백인지 알아야 하는데, 자동 검출은 가로 글자 줄과 구분이 안 돼 실패했음(2.5). 조각을 배치하는 A가 좌표를 이미 가지고 있으므로 규격으로 넘길 것
- [ ] **E1 마스크 팽창 규칙 반영** — 상수가 아니라 규칙으로 설계해야 함(2.2 "마스크 팽창 폭"). 비율+상한, 병합 감지, 마스크 면적 상한, 배경 유형별 차등, 검수 플래그 5개 항목
- [ ] **`role` 산출 과업 누락 보완** — 6.5는 AI 담당이 E2에 `role`을 공급하도록 규정하나, `role`을 산출하는 과업이 파이프라인에 없음. B는 bbox·텍스트만 뽑고 A의 조각 분류는 레이아웃용이라 결이 다름
  - 실제 관측: `5.jpg`는 검출 38개 영역이 거의 전부 제품 인쇄 글자였음. 전부 지우면 제품이 뭉개짐 → **"어느 텍스트를 번역·제거할 것인가"를 판단하는 단계가 반드시 필요**
  - PoC 범위 밖(2026-08-20 결정, `대상` 열 미도입)이나 **개발계획문서에는 과업으로 명시돼야 함**
- [ ] 7장 체크리스트 갱신
- [x] CLAUDE.md 수정 — CER/IoU 계산 정의·jiwer 의존성 제거, 육안 등급 기록 방식으로 교체 *(2026-08-20 완료)*

> ⚠️ 개발계획문서 v2.2는 이 저장소에 없음. 위 항목은 문서를 확보한 뒤에야 반영 가능 — **원본 소재 확인 필요.**

---

*이 문서는 PoC 진행에 따라 지속 업데이트. 최종 기술 채택 결과는 각 섹션 "최종 결정" + 3장 진행 로그에 근거를 남길 것.*
