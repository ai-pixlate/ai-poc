# PoC 추가 검증 계획

> 작성 2026-09-08. 근거: 보완 개발계획서 검증 항목표 × 기존 PoC 기록 대조
> 참조: `PoC_검증_계획_및_기록.md` (기존 과업 계획·기록) · `README.md` (완료분 결과 요약)
> **이 문서는 계획만 담음.** 실행 결과·판정은 `PoC_추가검증_결과.md`에 남김.
> 대상은 기능명세서 개정으로 **추가된 미착수 6개 과업**. 완료분(텍스트 인식 · 인페인팅)과 결정 대기분(규제·현지 부적합 판정)은 위 두 문서에 있음.
> **과업 코드를 쓰지 않음** — 과업은 이름으로 부름.

---

## 1. 검증 대상

| 과업 | 후보 기술 | 핵심 검증 질문 | 판정 기준 | 성격 | 선행 조건 |
|---|---|---|---|---|---|
| ~~**줄·문단 병합 + 역할 분류**~~ **종결 2026-09-08 — `llm_assist` 채택** | 좌표 휴리스틱 / 휴리스틱 + LLM 보정 | 좌표 규칙만으로 블록 1건 = 문단 1개가 성립하는가. 제목·본문·캡션·가격·주의문구 5종을 가르는가 | 과분할·과병합 비율, 역할 오분류율 | PoC | 없음 — 즉시 착수 가능 |
| ~~**제품 라벨 판정**~~ **종결 2026-09-09 — `vlm_relation` 채택** | 역할 분류 확장 / 객체-텍스트 관계 판단 | 제품 용기·패키지 인쇄 글자를 배경 위 텍스트와 구분하는가 | **미탐 0 우선.** 오탐은 검수 복구 가능, 미탐은 복구 불가 → 보수적 판정으로 치우칠 것 | PoC | 줄·문단 병합 + 역할 분류 |
| **섹션 분해 + 절단 지점 산출** | 여백·레이아웃 규칙 / VLM 판단 | 의미 단위 경계가 텍스트 블록을 가르지 않는가. 경계마다 절단 가능한 여백 구간이 실재하는가 | 블록 관통 사례 수, 여백 좌표 산출 성공률. 실패 시 단일 섹션 폴백 동작 확인 | PoC | 줄·문단 병합 + 역할 분류 · **초장축 표본 확보** |
| ~~**번역 길이 팽창률**~~ **종결 2026-09-09 — 조판 단독 채택** | — (측정) | 번역 후 원본 박스 폭을 넘는 블록이 몇 %인가 | 초과율이 높으면 "길이 변화는 조판에서 흡수한다" 원칙 자체를 재검토 | 측정 | `evalset_ocr.json`(텍스트 인식 bbox 기반 88세그먼트) 존재. 단 **bbox가 붙은 번역 출력은 없음** → 번역 1회 추가 실행 필요 |
| ~~**스타일 추출**~~ **종결 2026-09-09 — `otsu_border` 채택** | 지배색 추출·크기 역산·정렬 판정 | 배경 대비 글자색 분리·정렬 판정이 되는가 | 육안 대조 오차. 굵기 추정·폰트 패밀리 식별은 범위 밖(12월) | 소규모 확인 | 텍스트 인식 bbox |
| **브랜드 로고 제외** | 템플릿 매칭 / 특징점 매칭 | 등록 로고 파일로 원본에서 위치를 찾는가 | 매칭 실패율. 실패는 확인 필요 신호로 노출되므로 완전할 필요 없음 | 소규모 확인 | 등록 로고 파일 확보 |

> **제품 라벨 판정 방식은 미확정.** 개발계획서 v3.4 §2.1은 "역할 분류로 판정한다"로 서술돼 있으나 이는 **검증 전 서술**이며, 본 PoC에서 `role_ext` · `vlm_relation` 두 방식을 비교해 확정함. `vlm_relation` 채택 시 라벨 판정이 역할 분류와 **별개 단계**가 되므로 v3.4 파이프라인 서술 개정이 함께 필요함.

### 현재 상태 — 왜 미착수인가

| 과업 | 상태 |
|---|---|
| 줄·문단 병합 + 역할 분류 | 텍스트 인식은 bbox·텍스트만 산출함. 병합·역할 단계가 파이프라인에 없음 |
| 제품 라벨 판정 | 인페인팅에서 제품 인쇄 글자를 **판정 제외**로 회피했을 뿐, 판정 로직은 없음 |
| 섹션 분해 + 절단 지점 산출 | 초안 배치가 후순위로 보류돼 착수 안 함 |
| 번역 길이 팽창률 | **요구 형태로 미측정.** 로컬라이징 번역 1차에 `length` 축(1~5점)이 있었으나 96% 만점으로 변별력을 잃어 폐기함. **박스 폭 초과율 %는 산출된 적 없음.** 결승 번역은 **합성 문서 10건 기준이라 bbox가 없음** — 좌표와 번역문이 이어진 산출물이 존재하지 않음 |
| 스타일 추출 | 미착수 |
| 브랜드 로고 제외 | 미착수 |

---

## 2. 항목별 실행 방법

### 공통 규칙

| 항목 | 내용 |
|---|---|
| 폴더 | 과업별 신설. **코드를 붙이지 않고 이름 기반으로 지음.** 착수 시점에 생성 |
| 진입점 | `poc/{과업}/run.py` |
| 결과 | `poc/{과업}/results/{variant}/` · 시각화는 `vis/` 하위 |
| 요약 | `poc/{과업}/summary.md` — **등급 칸은 빈 표 틀만 생성** |
| 코드 | 과업 간 공유하지 않음. 필요하면 복사 |
| 실행 | 전 과업 로컬. **유료 API는 번역 길이 팽창률 1회뿐** |
| 판정 | 사람이 육안 A/B/C. 코드는 비교하기 쉽게 만드는 데까지 |

입력 자원 현황 — `data/images/` **12장**, 텍스트 인식 `baseline` 인식 영역 **총 280건**.

폴더명 제안 — 확정 아님.

| 과업 | 폴더 |
|---|---|
| 줄·문단 병합 + 역할 분류 | `poc/block_role/` |
| 제품 라벨 판정 | `poc/product_label/` |
| 섹션 분해 + 절단 지점 산출 | `poc/section_split/` |
| 번역 길이 팽창률 | `poc/length_expansion/` |
| 스타일 추출 | `poc/style_extract/` |
| 브랜드 로고 제외 | `poc/logo_match/` |

### 줄·문단 병합 + 역할 분류

| 항목 | 내용 |
|---|---|
| 표본 | 12장 전량 · region 280건 |
| 입력 | `poc/B_ocr/results/baseline/regions/*.json` |
| variant 후보 | `heuristic_v1` (y겹침·x간격 임계로 행→문단 병합) / `heuristic_v2` (임계 튜닝 + 다단 열 분리) / `llm_assist` (휴리스틱 결과를 LLM이 보정) |
| 산출물 | `results/{variant}/blocks/{image}.json` — 블록 bbox, 병합된 텍스트, `role`, 소속 region id 목록<br>`vis/` — 블록 박스 + role 색상 구분, 원본 인식 영역은 회색<br>`summary.md` — **보조 지표 3종(기계)** + 과분할·과병합·오분류 건수 칸(빈 표) |
| 판정 | 이미지 단위 A/B/C. 사람이 `vis/`를 보고 과분할·과병합·오분류 건수를 직접 기록 |
| 정답 라벨 | **만들지 않음 (2026-09-08 결정).** 문단 경계·역할 판단이 사람 머릿속에 있으므로 육안 판정으로 대체 |
| 제외 관례 | **제품 인쇄 글자(라벨) 블록은 역할 오분류 계수에서 제외** (2026-09-08 결정). 판정표 `라벨` 칸에 건수를 적으면 **역할 오분류율 분모가 `블록 수 − 라벨`로 줄어듦**. 병합 오류는 제외하지 않고 셈. 인페인팅 판정 관례와 동일하며, 해당 블록은 **제품 라벨 판정 과업에서 판정함** |
| 등급 기준 | **병합** — 가중 오류율(과분할 + 과병합×2 ÷ 블록 수) ≤10% & 과병합 0이면 A, ≤25% B, 그 외 C. 블록 10개 미만은 1건까지 A<br>**역할** — 오분류율 ≤10% A, ≤25% B, 그 외 C. **주의문구 미탐 1건이면 최대 B, 2건 이상 C**<br>**채택 게이트 — 12장 A+B 70%** (텍스트 인식·인페인팅과 동일) |

**보조 지표 3종** — 정답 없이 계산되는 값. 정오가 아니라 **볼 순서를 고르는 용도**임.

| 지표 | 정의 | 한계 |
|---|---|---|
| 겹침 | 블록 bbox가 서로 겹치는 쌍 수 | 과병합에서도 늘지만 원래 겹쳐 배치된 레이아웃에서도 늚 |
| 이질 | 한 블록 안 글자 높이가 1.5배 넘게 벌어진 블록 수 | `h_ratio`가 직접 누르는 값이라 variant 간 차이는 설계상 당연 |
| 일치율 | variant 간 블록 경계가 같은 비율 | 임계 민감도만 말함. 어느 쪽이 옳은지는 말하지 않음 |

`role` 5종 — 제목 / 본문 / 캡션 / 가격 / 주의문구. **성분 목록을 별도 유형으로 둘지 결정 필요**(로컬라이징 번역에서 성분명 OCR 훼손 → 문맥 추정이 안전 문제로 이어짐, `PoC_검증_계획_및_기록.md` 4장).

### 제품 라벨 판정

| 항목 | 내용 |
|---|---|
| 표본 | 12장 중 제품 인쇄 글자가 있는 이미지. **장수 _미정_** — 착수 시 육안 선별 |
| 입력 | 줄·문단 병합 + 역할 분류의 블록 JSON + 원본 이미지 |
| variant 후보 | `role_ext` (역할 분류에 `label` 클래스 추가) / `vlm_relation` (전체 이미지 + 블록 크롭을 VLM에 주고 객체-텍스트 관계 판단) |
| 산출물 | 블록 JSON에 `is_product_label` 필드 추가 (**PoC 내부 이름**). 계약 승격 시 스키마 계약 v3.1 §2.1의 `is_excluded`로 매핑 — 블록 제외 필드는 단일화돼 있으므로 새 필드를 만들지 않음<br>`vis/` — 라벨 판정분을 별색으로 표시<br>`summary.md` — 이미지별 미탐·오탐 건수 칸 |
| 판정 | **미탐 우선.** 미탐 1건이라도 나오면 해당 variant 탈락으로 볼지 여부 _미정_ |

인페인팅 판정에서 제품 인쇄 글자를 제외했던 기준을 그대로 쓰면 됨(`PoC_검증_계획_및_기록.md` 2.2).

### 섹션 분해 + 절단 지점 산출

| 항목 | 내용 |
|---|---|
| 표본 | **현 12장으로는 검증 불가.** 종횡비 최대 1.2(세로/가로)로 **초장축 상세페이지가 표본에 없음** → 초장축 이미지 확보가 선행 |
| 입력 | 줄·문단 병합 + 역할 분류의 블록 JSON + 원본 이미지 |
| variant 후보 | `whitespace_rule` (행 투영 프로파일로 여백 구간 검출) / `whitespace_rule_blockaware` (블록 경계 침범 금지 제약 추가) / `vlm_section` (VLM이 의미 경계 판단) |
| 산출물 | `results/{variant}/sections/{image}.json` — 절단 y좌표 목록, 섹션 범위, **`top_offset`(원본 기준 섹션 시작 y)**, 폴백 발동 여부<br>**블록 좌표 재기준화 확인** — 상류 블록 bbox를 섹션 로컬 좌표로 변환하고, `top_offset + bbox.y`로 원본 위치가 복원되는지 검산<br>`vis/` — 절단선을 그린 원본<br>`summary.md` — 블록 관통 건수, 여백 산출 성공률 칸 |
| 판정 | 블록 관통 건수 + 여백 좌표 산출 성공률. **단일 섹션 폴백이 실제로 도는지 확인 포함** |

> 이 표본 공백은 텍스트 인식의 "초장축 타일 경계 누락" 미검증과 **같은 원인**임. 초장축 이미지를 확보하면 두 건을 같은 표본으로 함께 검증 가능.

### 번역 길이 팽창률

| 항목 | 내용 |
|---|---|
| 표본 | `poc/C_translate/evalset_ocr.json` — **10장 · 88세그먼트** (텍스트 인식 baseline bbox 동반) |
| 모델 | **`gemini-3.8-flash` 확정.** variant는 모델 축이 아니라 폭 추정 조건 축으로 둠 |
| variant 후보 | `nowrap` (한 줄 가정 — 원본 폭 초과 여부만) / `wrap_allowed` (줄바꿈 허용, 블록 높이 초과로 판정) / `wrap_and_shrink` (줄바꿈 + **폰트 축소 하한 적용**, 하한값은 파라미터) |
| 실행 | 번역 1회 → 세그먼트별 번역문 폭을 렌더 측정(PIL 등)해 원본 bbox와 대조 |
| 산출물 | 세그먼트별 `(원문 폭 px, 번역문 추정 폭 px, 초과 여부, 초과 배율)` 표<br>`summary.md` — 전체 초과율 %, 초과 배율 분포, **폰트 축소율 구간별 누적 흡수율(100%·95%·90%·85%·80%)**, role별 분해(줄·문단 병합 완료 시) |
| 판정 | **조판 흡수 후 잔여 초과율 %.** 개발계획서 §4.3이 묻는 값은 흡수 전 초과율이 아니라 **조정 상한으로도 흡수되지 않는 비율**임. 통과 임계 _미정_ |
| 비용 | 유료 API 1회. **약 0.04$ 예상 — 실행 전 건수·비용 재보고 필요** |

⚠️ **재현 조건을 결과 문서에 그대로 남길 것** — ① 폭 추정에 쓴 폰트·자간·렌더 방식 ② 번역에 쓴 **프롬프트 전문**. 초과율은 폰트 가정만큼이나 **프롬프트의 길이 제약 지시 유무**에 흔들림. 둘 중 하나라도 빠지면 수치가 재현되지 않음.

### 스타일 추출

| 항목 | 내용 |
|---|---|
| 표본 | 12장 전량 · region 280건 |
| 입력 | 텍스트 인식 baseline bbox + 원본 이미지 |
| variant 후보 | `dominant_color` (bbox 내 최빈색·k-means) / `contrast_split` (대비로 전경 화소를 먼저 가른 뒤 색 추출) / 정렬은 `align_rule` (블록 내 행 x좌표 분산) |
| 산출물 | region별 `{font_color, est_font_size, align}` JSON<br>`vis/` — 추출한 색 스와치를 해당 bbox 옆에 붙인 대조 이미지<br>`summary.md` — 이미지 단위 A/B/C 칸 |
| 판정 | 육안 대조 오차. **굵기 추정·폰트 패밀리 식별은 범위 밖(12월)** |

크기 역산은 bbox 높이 기반. 세로쓰기·장식체에서 어긋날 수 있어 별도 칸으로 기록.

### 브랜드 로고 제외

| 항목 | 내용 |
|---|---|
| 표본 | **등록 로고 파일 확보분 × 해당 로고가 있는 이미지. 둘 다 _미정_** |
| variant 후보 | `template_match` (다중 스케일 템플릿 매칭) / `feature_match` (특징점 + 호모그래피) |
| 산출물 | 로고별 매칭 좌표 JSON + 신뢰도<br>`vis/` — 매칭 박스, 신뢰도 미달은 별색<br>`summary.md` — 로고별 매칭 실패율 칸 |
| 판정 | 매칭 실패율. **실패는 "확인 필요" 신호로 노출되므로 완전할 필요 없음** — 오탐(엉뚱한 위치 매칭)이 실패보다 나쁨 |

---

## 3. 검증 순서

순서 기준은 **선행 조건 충족 순 → 그 안에서 위험도 순**임. 개발계획서 §3의 "위험한 것부터 검증" 원칙상 **제품 라벨 판정**(오판 시 복구 불가)이 가장 앞서야 하나, 블록·역할 산출물 없이는 착수할 수 없어 3순위에 둠.

| 순위 | 과업 | 근거 |
|---|---|---|
| ~~1~~ | ~~**줄·문단 병합 + 역할 분류**~~ | **종결** — 결과는 `PoC_추가검증_결과.md` 1장 |
| ~~2~~ | ~~**번역 길이 팽창률**~~ **종결** | 준비된 `evalset_ocr.json`에 **번역 1회(약 0.04$ 예상) + 폭 추정 3조건(로컬)**이면 산출됨. variant 3종이 같은 번역 출력을 공유하므로 API 비용은 그대로임. 원칙 재검토 트리거라 조기 확인 이득이 큼 |
| ~~3~~ | ~~제품 라벨 판정~~ **종결** → 섹션 분해 + 절단 지점 산출 | 블록·역할 산출물 위에서 확장 |
| 4 | ~~스타일 추출~~ **종결** · 브랜드 로고 제외 | 독립. 여력 시 |

### 줄·문단 병합 + 역할 분류를 1순위에 두는 이유

`role` 산출이 **3개 과업의 공통 전제**임. 서로 다른 과업이 같은 벽에 부딪힌 것이 실측으로 확인됨.

| 과업 | 막힌 지점 | 기록 |
|---|---|---|
| 제품컷 누끼 | 전경 중 무엇이 제품인가 — 범용 배경제거 4종 모두 판단 못 함 | `README.md` 4장 |
| 로컬라이징 번역 | `미백 앰플`이 제품명인가 효능 주장인가 — 정식 모델 전부 0/5 | `PoC_검증_계획_및_기록.md` 2.3 |
| 초안 배치 | 조각의 의미 분류(제품컷/설명/표/로고) | `PoC_검증_계획_및_기록.md` 2.6 |

→ 상류가 `role`을 산출해 넘기면 셋 다 해소됨. `PoC_검증_계획_및_기록.md` 4장 체크리스트 `role` 항목과 동일 사안.

---

## 4. 미결 사항

| # | 내용 | 담당 |
|---|---|---|
| 1 | 새 과업 폴더명 확정 — 2장 제안안 채택 여부 | _미정_ |
| 2 | 브랜드 로고 제외용 등록 로고 파일 확보 경로 | _미정_ |
| 3 | ~~줄·문단 병합 A/B/C 등급 기준~~ — **2026-09-08 확정**, 2장에 기재 | 종결 |
| 3-1 | ~~줄·문단 병합 과업 자체~~ — **2026-09-08 종결**. 표본 한계로 **조건부** — 골든 샘플 확보 시 재실행 | 종결 |
| 4 | ~~결과 문서 파일명·분리 단위~~ — **`PoC_추가검증_결과.md` 일괄**로 확정 | 종결 |
| 5 | **초장축 상세페이지 표본 확보** — 현 12장 종횡비 최대 1.2. 데이터 수집 계획서 v3 §4-1 골든 샘플 조건 ⑥(초장축 1장·섹션 15~20개)이 같은 요건이며 **9/10 PM 확정 예정** → 별도 수집 불필요, **확정 결과 수령 대기**. 섹션 분해 착수 전제이자 텍스트 인식 타일 경계 미검증 해소 조건 | _미정_ |
| 6 | ~~번역 길이 팽창률의 폭 추정 기준 폰트·자간~~ — Arial·자간 0으로 측정. **실물 폰트 확정 시 재측정**(결과 문서 6장 12번) | 이관 |
| 7 | `role`에 성분 목록 유형을 포함할지 여부 — **개발 이관**(결과 문서 4장) | 이관 |
| 8 | ~~제품 라벨 판정에서 미탐 발생 시 variant 즉시 탈락 처리 여부~~ — **판단 불필요**. 미탐 0인 variant가 하나뿐이라 기준 없이 갈림 | 종결 |
| 9 | **골든 샘플 조건에 "제품 인쇄 글자(라벨) 포함" 축 추가 요청** — 현 조건 ①~⑥에 배경 유형(단색/사진)은 있으나 **라벨 축이 없어** 제품 라벨 판정 표본이 확보되지 않음. **9/10 확정 전** 데이터 담당에 역제안 | AI→데이터 |
| 10 | **브랜드 로고 오탐 신호의 스키마 자리** — 계약 v3.1 §3에 `logo_match_failed`(실패)만 있고 **오탐용 신호 없음**. 오탐 영역은 번역·인페인팅에서 통째로 빠지나 사용자가 알 수 없음. PoC에서 오탐률이 유의미하면 신호 추가 검토 | AI→BE |
| 11 | ~~번역 길이 팽창률 폰트 축소 하한값~~ — **80% 유지 권고**. 60·50%로 열어도 흡수 12·30%뿐이고 가독성만 잃음 | 종결 |
