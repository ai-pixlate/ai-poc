# PoC 골든 샘플 재실행 계획

> 작성 2026-09-15. **확정 기술 전부를 골든 샘플로 다시 돌려** 12장 표본에서 낸 결론이 유지되는지 확인함.
> 참조: `PoC_추가검증_결과.md` (추가 6과업 결과) · `PoC_검증_계획_및_기록.md` (원 과업 판정 기준) · `PoC_추가검증_계획.md` (추가 6과업 판정 기준)
> **계획만 담음.** 판정표는 과업별 `summary_golden.md`, 결론은 결과 문서에 반영.

---

## 0. 요약

| 항목 | 내용 |
|---|---|
| 목적 | 12장·10장 표본으로 확정한 기술이 **실제 상세페이지에서도 게이트를 넘는지** 확인. 조건부 종결 2과업(줄·문단 병합 · 제품 라벨) 종결 |
| 표본 | 골든 샘플 3상품 34장 → `color_snap_vlm2` **섹션 102개** · 글자 영역 2,280개(12장의 8.1배 · 4,000px 초과 섹션 분할 전 수치 — 단계 1 뒤 갱신) |
| 대상 | 확정 기술 8종 중 **7종** — 새로 판정 6 + 재확인 1. 섹션 분해는 골든 샘플로 확정돼 제외 |
| 처리 단위 | **섹션** — 4,000px 초과 섹션은 OCR 전 추가 분할(결과 문서 10장 27번) |
| 판정 | **Claude 1차 판정 → 예람님 검토** |
| 비용 | 유료 3단계 **약 $0.65** (추정 — 단계마다 실행 전 재보고) |
| 제외 | 초안 배치/리플로우(기능 제외) · 로컬라이징 번역 품질(잠정 — 규제 표 확정 후) · 제품컷 누끼(잠정) |

---

## 1. 대상

| 확정 기술 | 기존 표본 | 골든 샘플 현황 | 이번 작업 |
|---|---|---|---|
| 텍스트 추출 — PaddleOCR | 12장 | 섹션 OCR 있음(2,280영역). **poly 미저장 · 4,000px 초과 섹션 미분할 · 페이지 좌표 저장 · 판정 안 함** | `poc/B_ocr`에서 poly 포함 · 27번 분할 적용 재생성 + 판정 |
| 원문 지우기 — LaMa | 12장 | 누끼 검증용 지운 결과 있음. **bbox 마스크 · 라벨·로고 미제외 · 판정 안 함** | 재실행 + 판정 |
| 줄·문단 병합 + 역할 분류 — `llm_assist` | 12장 (조건부) | 없음 | 실행 + 판정 |
| 제품 라벨 판정 — `vlm_relation` | 12장 (조건부) | 없음 | 실행 + 판정 |
| 스타일 추출 — `otsu_border` | 12장 | 없음 | 실행 + 판정 |
| 번역 길이 팽창률 — 조판 단독 | 10장 · 88세그먼트 | 없음 | 번역 1회 + 폭 측정 |
| 브랜드 로고 제외 — `block_exact` | 골든 34장 | 완료. **병합 1단계(`heuristic_v2`) 블록 기준** | `llm_assist` 블록으로 재확인 |
| 섹션 분해 — `color_snap_vlm2` | 골든 34장 | 완료 | 없음 — 섹션 입력으로 재사용 |

---

## 2. 공통 원칙

| 항목 | 내용 |
|---|---|
| 처리 단위 | **섹션 102개.** 원본을 통째로 넣지 않음 — 최대 1000×37,665px라 VLM 입력(긴 변 1024px)이 무의미하고, OCR 검출기 긴 변 상한이 4,000px. 확정 파이프라인 순서(섹션 분해 → OCR)와도 맞음 |
| 긴 섹션 | **4,000px 초과 섹션은 OCR 전에 추가 분할** — 여백 우선, 없으면 300px 겹침 띠(결과 문서 10장 27번). 해당 섹션 1개(4,491px) — 기존 `section_vlm2`는 미분할로 축소돼 34/37만 찾음. 분할은 OCR 입력에만 적용, 섹션 단위 판정·VLM 크롭은 섹션 그대로 |
| 알고리즘·파라미터 | **확정 조건 그대로.** 입력만 바꿈 |
| 좌표 | **섹션 로컬 좌표로 저장·처리.** 분할 조각 결과는 섹션 로컬로 합침. 원본 위치는 `top_offset + y`로 복원(`top_offset` = 섹션 분해 `range` 시작, 섹션 분해 검산과 같음). 기존 `ocr_split` 결과는 페이지 좌표 · 페이지 단위 파일이라 재사용 안 함 |
| 코드 | 각 과업 폴더에 `--sample golden` 추가 — 텍스트 추출은 **`poc/B_ocr`**. 섹션 입력 읽기 · 분할 로직은 기존 코드(`poc/ocr_split` 등)에서 **폴더마다 복사**(과업 간 공유 금지) |
| 결과 위치 | `poc/{과업}/results/golden/{variant}/` · 판정표 `poc/{과업}/summary_golden.md`. **12장 결과는 건드리지 않음.** CLAUDE.md 결과 위치 규칙(`results/{variant}/` · `summary.md`)의 예외 — 12장 결과 보존 목적 |
| 라벨·로고 제외 기준 | **실행 입력은 예측** — 단계 5 마스크 · 단계 7 번역 입력은 단계 3·4 출력 그대로(파이프라인 조건). **판정 대상은 정답** — 단계 2 역할 · 단계 6 판정에서 빼는 라벨은 단계 3 검토 확정 정답, 로고는 기존 정답(페이지 로고 7개). 예측 오탐 블록이 판정에서 조용히 빠지는 것 방지. 12장은 예측(채택 후보)이 곧 정답지라 차이 없었음 |
| 판정 | 섹션 단위 A/B/C · 게이트 **A+B 70%**(기존과 같음). **글자 없는 섹션은 분모 제외** |
| 판정 주체 | Claude 1차 판정(비고에 "Claude 육안 판정") → 예람님 검토 후 확정 |
| 유료 호출 | 단계마다 드라이런으로 호출 건수·입력 크기를 센 뒤 비용 보고 → 확인 후 실행. 응답은 프롬프트 지문 포함 캐시 |

---

## 3. 실행 순서

앞 단계 산출물이 다음 단계 입력임.

| 단계 | 과업 | 입력 | 비용 | 소요 (추정) |
|---|---|---|---|---|
| 0 | 섹션 분해 | 완료분 재사용 | 0 | — |
| 1 | **텍스트 추출** — 섹션 OCR 재생성(poly 포함 · 4,000px 초과 분할) | 섹션 이미지 | 0 | 약 1분 |
| 2 | **줄·문단 병합 + 역할 분류** | 1 | **약 $0.25** | 약 10분 |
| 3 | **제품 라벨 판정** | 2 + 섹션 이미지 | **약 $0.20** | 약 6분 |
| 4 | **브랜드 로고 제외 재확인** | 2 | 0 | 수 초 |
| 5 | **원문 지우기** — variant 2종 | 1 · 3 · 4 | 0 | 약 6분 × 2 |
| 6 | **스타일 추출** | 1 · 2 · 3 · 4 | 0 | 수 초 |
| 7 | **번역 길이 팽창률** | 3 · 4 | **약 $0.20** | 수 분 |

> 소요는 골든 섹션 실측(OCR 0.27s · LaMa 3.29s/섹션)과 12장 실측(`llm_assist` 6.7s · `vlm_relation` 3.5s/장)으로 잡음.

---

## 4. 단계별 실행 방법

### 단계 1 — 텍스트 추출

| 항목 | 내용 |
|---|---|
| 현황 | `poc/ocr_split` `section_vlm2` 결과는 **bbox만 저장** — 확정 마스크(poly 채움 + 팽창)에 poly가 필요함. 4,000px 초과 섹션을 **나누지 않고** 넣어 축소됨 · 페이지 단위 파일에 페이지 좌표로 저장 |
| 실행 | `poc/B_ocr`에 `--sample golden` 추가. 섹션 OCR 재실행 — **4,000px 초과 섹션은 27번 규칙으로 분할**(여백 우선, 없으면 300px 겹침 띠), **poly 저장 추가**. 모델은 확정 조합 `PP-OCRv5_server_det` + `korean_PP-OCRv5_mobile_rec` |
| 산출물 | 섹션별 영역 JSON(섹션 로컬 `bbox` · `poly` · `text` · `score` + `top_offset` · 분할 조각) · `vis/` bbox + 인식 텍스트 대지 |
| 판정 | 섹션 × **등급 4칸** — `텍스트` / `bbox` / `사진영역 텍스트` / `사진영역 bbox` (원 과업 기준 [확정]) |
| 확인 | 신뢰도 0.5 미만 109 · 빈 텍스트 40영역이 무엇을 잡았는지(결과 문서 8장 ⑫ — 분할 전 수치라 재집계) · 분할 섹션의 이음선 글자 누락·중복 · 영역 수 변화(2,280 대비) |

### 단계 2 — 줄·문단 병합 + 역할 분류

| 항목 | 내용 |
|---|---|
| 실행 | `heuristic_v2` → `llm_assist`(`gemini-3.8-flash` · `temperature=0` · 분할 안 시킴). 섹션별 호출 |
| 입력 | 단계 1 영역 (이미지는 넣지 않음 — 확정 조건) |
| 산출물 | `blocks/{섹션}.json` · `vis/` 블록 박스 + role 색상 |
| 판정 | 병합·역할 A/B/C — 기존 등급 기준 그대로(가중 오류율 · **주의문구 미탐 1건이면 최대 B**). 라벨 블록은 역할 오분류에서 제외 — 제외 기준은 **단계 3 검토 확정 정답**이라 **단계 3 검토 뒤 판정** |
| 확인 | **가격·주의문구 건수** — 12장에 각 1건뿐이던 한계가 풀리는지 · 역할 분포(12장에서 본문 1 · 캡션 80으로 쏠림) |
| 비용 근거 | 12장 입력 17,479 / 출력 2,656토큰 × 영역 8.1배 + 호출 수 증가분(프롬프트 반복). 단계 1 영역 수 갱신 후 드라이런에서 재산정 |

### 단계 3 — 제품 라벨 판정

| 항목 | 내용 |
|---|---|
| 실행 | `vlm_relation` — **섹션 크롭(긴 변 1024px)** + 단계 2 블록. "애매하면 라벨" 편향 프롬프트 그대로 |
| 산출물 | 블록별 `is_product_label` · `vis/` 라벨 판정분 별색 |
| 판정 | 블록 단위 **미탐 · 오탐**. 정답은 Claude 1차 전수 확인 → 예람님 검토 — 12장은 채택 후보를 정답지로 삼았으나 이번엔 사람 검토로 교차. 확정 정답은 단계 2 역할 · 단계 6 판정의 라벨 제외 기준으로도 씀 |
| 확인 | 겹친 패키지 컷 · 라벨 없는 컷이 표본에 있는지(조건부 종결 사유) |
| 위험 | 4,000px대 섹션은 크롭 폭이 **약 230px**로 줄어듦(OCR 분할과 무관 — VLM 입력은 섹션 그대로). 확정 조건 그대로 돌리고, 미탐이 긴 섹션에 몰리면 원인으로 기록 |
| 비용 근거 | 12장 입력 25,710 / 출력 2,066토큰 × 블록 약 8배 + 섹션 이미지 102장 |

### 단계 4 — 브랜드 로고 제외 재확인

| 항목 | 내용 |
|---|---|
| 실행 | `block_exact` — 단계 2 `llm_assist` 블록에 적용. 브랜드명 사전은 기존 그대로 |
| 판정 | 기존 정답(페이지 로고 7개 · 5장)과 **기계 대조** — 찾음 · 놓침 · 오탐. 섹션 좌표는 `top_offset`으로 원본 좌표로 바꿔 대조 |
| 근거 | 결과 문서 9장 브랜드 로고 한계 3 · 10장 19번 — LLM 보정이 로고를 옆 글자와 묶으면 놓침이 늘 수 있음 |

### 단계 5 — 원문 지우기

| 항목 | 내용 |
|---|---|
| 마스크 | 확정 조건 — **poly 채움 + 글자 높이 15% 팽창**. **라벨 블록(단계 3 예측) · 로고 블록(단계 4 예측)은 마스크에서 뺌** |
| variant | `erase_all` — 확정 조건(모든 OCR 영역) / `erase_s50` — 인식 신뢰도 0.5 이상 · 텍스트 있는 영역만 |
| 산출물 | 섹션별 대지 — 원본 \| 마스크 \| `erase_all` \| `erase_s50` |
| 판정 | 섹션 A/B/C(원 과업 기준 [확정]) + 사진·그라데이션 위 글자 칸. 두 variant 나란히 |
| 확인 | `erase_all`에서 **얼굴·물방울이 지워지는 섹션 수**(결과 문서 8장 ⑫) — 확정 조건을 `erase_s50`으로 바꿀지 근거 · 라벨·로고가 남는지 |

### 단계 6 — 스타일 추출

| 항목 | 내용 |
|---|---|
| 실행 | `otsu_border` — 테두리에 많이 닿는 쪽을 배경으로 봄. 크기 em ×1.35 · 정렬 허용 오차 블록 폭 12%. 전 영역에 실행하고 라벨·로고 여부는 표시만 함 |
| 입력 | 단계 1 영역 + 단계 2·3·4 블록(정렬 · 역할 · 라벨 · 로고) + 섹션 원본 이미지 |
| 산출물 | 영역별 `font_color` · `bg_color` · `est_font_px`, 블록별 `align` · `vis/` 색 스와치 대지 |
| 판정 | 조판 대상(**라벨·로고 제외** — 단계 3 검토 확정 정답 · 기존 로고 정답 기준) 섹션 단위 **색 · 크기 · 정렬** A/B/C |
| 확인 | 사진 배경 위 글자 · 아웃라인·배지 테두리 글자(12장 색 실패 유형, 결과 문서 8장 ⑧) |

### 단계 7 — 번역 길이 팽창률

| 항목 | 내용 |
|---|---|
| 실행 | 번역 1회 — `gemini-3.8-flash` · 프롬프트 `default`(기존 전문 그대로) → 폭 측정 4조건 `nowrap` / `wrap_allowed` / `wrap_and_shrink` / `wrap_grow_20` |
| 입력 | 단계 3 라벨 · 단계 4 로고(모두 예측)를 뺀 `llm_assist` 블록(`block` 입력만 — 채택 파이프라인 단위) |
| 측정 조건 | 기존과 같음 — Arial · 자간 0 · em = 줄 높이 × 1.35 · 축소 상한 80% · 규제 표 더미 15항목 |
| 판정 | 기계 집계 — 초과율 · 조정 구간별 흡수율 · role별 · 폭 배율 분포 |
| 확인 | 결론 "조판으로 흡수 불가(초과율 94%)"가 유지되는지 · 긴 본문 섹션에서 달라지는지(결과 문서 9장 한계 4) |
| 비용 근거 | 10장 `block` 33세그먼트 $0.0138 × 블록 수 비율 |

---

## 5. 비용

| 단계 | 모델 | 비용 (추정) |
|---|---|---|
| 2 — `llm_assist` | `gemini-3.8-flash` | 약 $0.25 |
| 3 — `vlm_relation` | `gemini-3.8-flash` | 약 $0.20 |
| 7 — 번역 | `gemini-3.8-flash` | 약 $0.20 |
| **합계** | | **약 $0.65** |

> 12장 실측 토큰을 영역 수 비율로 늘린 추정임. 영역 수는 분할 전 2,280 기준이라 단계 1 뒤 달라질 수 있음. **단계마다 드라이런으로 건수를 센 뒤 재보고하고 확인받음.**

---

## 6. 판정 절차

| 순서 | 내용 |
|---|---|
| 1 | 코드가 섹션별 대지 + **빈 판정표** 생성 |
| 2 | Claude 1차 판정 — 비고에 "Claude 육안 판정" |
| 3 | 예람님 검토 — 수정분 반영 |
| 4 | 게이트 집계 → 결과 문서 반영 |

> 단계 2 역할 · 단계 6 판정은 라벨 제외 기준이 단계 3 확정 정답이라 **단계 3 검토가 끝난 뒤** 진행함.

| 단계 | 판정 분량 |
|---|---|
| 1 텍스트 추출 | 글자 있는 섹션 × 4칸 |
| 2 줄·문단 병합 + 역할 | 글자 있는 섹션 × 2칸(병합 · 역할) |
| 3 제품 라벨 | 블록 전수 — 미탐 · 오탐 |
| 5 원문 지우기 | 글자 있는 섹션 × variant 2종 |
| 6 스타일 추출 | 조판 대상 있는 섹션 × 3칸 |

---

## 7. 종결 기준

기존 게이트를 그대로 씀.

| 과업 | 통과 |
|---|---|
| 텍스트 추출 | 섹션 A+B 70% (텍스트 · bbox) |
| 원문 지우기 | 섹션 A+B 70% — 사진·그라데이션 위 글자 포함 |
| 줄·문단 병합 + 역할 분류 | 병합 · 역할 각 A+B 70% |
| 제품 라벨 판정 | **미탐 0** |
| 스타일 추출 | 색 · 크기 · 정렬 각 A+B 70% |
| 번역 길이 팽창률 | 통과선 없음 — 결론 유지 여부만 확인 |
| 브랜드 로고 제외 | 놓침 · 오탐이 기존(놓침 1 · 오탐 0)보다 늘지 않음 |

> 미달 시 대응은 _미정_ — 결과 보고 후 결정.

---

## 8. 결정 사항 (2026-09-15 / 예람님)

| # | 결정 |
|---|---|
| 1 | **초안 배치/리플로우** — 기획서 개정으로 기능에서 제외 → **기술검증에서도 제외** |
| 2 | **확정 기술 전부 골든 샘플 재실행** |
| 3 | 처리 단위 **섹션 102개** |
| 4 | 원문 지우기 **확정 조건(`erase_all`)과 신뢰도 0.5 하한(`erase_s50`) 둘 다 판정** |
| 5 | **Claude 1차 판정 → 예람님 검토** |
| 6 | 4,000px 초과 섹션은 **27번 규칙(OCR 전 추가 분할) 적용** — 기존 `section_vlm2` 미분할 결과 재사용 안 함 |

---

## 9. 미결 사항

| # | 내용 | 담당 |
|---|---|---|
| 1 | **제품컷 누끼 목적 재확인** — "세로 → 가로 상세페이지 재배치"용으로 검증했으나 초안 배치가 기능에서 빠짐. 누끼를 기능에 남길지, 남기면 용도가 무엇인지 | 기획 · _미정_ |
| 2 | 골든 샘플에 가격 · 주의문구 · 겹친 패키지 컷이 부족할 때 추가 표본 요청 여부 | _미정_ |
| 3 | 게이트 미달 과업의 대응 | _미정_ |
| 4 | 결과 기록 위치 — `PoC_추가검증_결과.md`에 장 추가 / 별도 결과 문서 | _미정_ |
