# PoC 검증 결과 요약

> 작성 2026-08-25. 근거 문서: `PoC_검증_계획_및_기록.md` (전 과업) · `poc/C_translate/PLAN.md` (로컬라이징 번역 상세)
> 이 문서는 **결과만 모은 것임**. 판단 경위·실패 기록은 위 두 문서에 있음.

---

## 0. 전체 현황

| 과업 | 채택 기술 | 결과 | 상태 |
|---|---|---|---|
| **텍스트 추출** | **PaddleOCR** (전처리 없음) | 육안 대부분 A, 70% 게이트 통과 | **확정** |
| **원문 지우기** | **LaMa** (IOPaint) | 12장 중 10장 A+B (**83%**) | **확정 — Go** |
| **로컬라이징 번역** | _미정_ | 6종 결승 완료. `gemini-3.8-flash` 93% 최상위 | **잠정** |
| 제품컷 누끼 | _미정_ | `birefnet-general`이 최선이나 **분류 단계 부재** | 판정 미완 |

---

## 1. 텍스트 추출 — **확정**

### 결과

| 항목 | 결정 |
|---|---|
| 엔진 | **PaddleOCR** |
| 모델 | `PP-OCRv5_server_det` + `korean_PP-OCRv5_mobile_rec` |
| 전처리 | **적용 안 함** |

| variant | 판정 | 사유 |
|---|---|---|
| `baseline` | **채택** | 육안 대부분 A. 70% 게이트 통과 |
| `det_side_1280` | 제외 | 12장 전부 baseline과 **인식 결과 완전 동일** |
| `vl_spotting` (PaddleOCR-VL) | 제외 | 12장 1,042초 — baseline의 **약 300배** |

### 검증 방법

이미지 12장 · **이미지 단위** A/B/C 육안 판정. 단계적 게이트 — A+B 70% 이상이면 다음 단계 생략.

### 과정

baseline이 게이트를 통과해 전처리 단계로 넘어가지 않음. 한국어 인식 모델 상향을 시도했으나 **server 등급 인식 모델은 전부 중국어·영어·일본어용**이고 한국어는 `mobile`뿐이라 구조적으로 불가함을 실측으로 확인함.

> ⚠️ **개별 등급이 기록되지 않음.** `poc/B_ocr/summary.md` 판정표 12행이 빈 칸이며 근거는 총평뿐임. 나중에 전처리를 붙이면 **비교할 baseline 등급이 없음.**

---

## 2. 원문 지우기 — **확정 (Go)**

### 결과

| variant | 후보 | A | B | C | **A+B** | 소요 |
|---|---|---|---|---|---|---|
| **`lama`** | **LaMa (IOPaint, CPU)** | 8 | 2 | 2 | **10/12 (83%)** | 149s |
| `telea` | OpenCV Telea | 1 | 2 | 9 | 3/12 (25%) | 1.5s |
| `ns` | OpenCV Navier-Stokes | 1 | 1 | 10 | 2/12 (17%) | 1.5s |
| SD 계열 5종 | SD1.5 / PowerPaint | — | — | — | **탈락** | — |

**→ Go. 사진 배경도 MVP에 포함함.**

### 검증 방법

표본 12장 · A/B/C 육안 판정. 마스크는 텍스트 추출의 bbox를 **글자 높이 15% 팽창**. 제품 인쇄 글자는 판정에서 제외.

### 과정

SD 계열은 설정 3축(guidance·steps·네거티브)을 바꿔도 **지운 자리에 가짜 글자·라벨을 생성함**. 결정적으로 LaMa가 A를 받은 쉬운 이미지에 적용했더니 **멀쩡한 부분까지 가짜 무늬로 채움**.

> **구조적 원인** — SD는 마스크 주변을 보고 채울 내용을 **생성함**. 상세페이지는 주변이 온통 글자라 "여기도 글자"가 가장 그럴듯한 답이 됨. LaMa는 주변 화소를 확장·복원해 실패해도 흐려질 뿐 **없던 것이 생기지 않음.**

LaMa가 C를 받은 2장은 마스크 면적 1·2위(47%·32%)로, **기술 한계가 아니라 마스크 과대**가 원인임.

---

## 3. 로컬라이징 번역 — **잠정**

### 결과 — 결승 6종

| 모델 | 정식 | 함정 정답률 | `instruction` | `hallucination` | `quality` | 부당 | 양측 저점 | 일치율 | 중앙 tok/s | 변동 | 비용 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `gemini-3.1-pro-preview` | **preview** | **100%** | **9.97** | 9.91 | **9.36** | **0** | **1** | 88% | 11.9 | **117배** | 0.1294$ |
| **`gemini-3.8-flash`** | GA | **93%** | **9.77** | 9.87 | 9.02 | **5** | 8 | 89% | 62.7 | 19배 | **0.0408$** |
| **`claude-opus-5`** | GA | 92% | 9.74 | **9.96** | 9.30 | **5** | **1** | 85% | 72.3 | **2배** | 0.4534$ |
| `gpt-5.6-sol` | GA | 92% | 9.28 | 9.89 | 9.23 | 15 | 12 | 88% | 58.2 | 2배 | 0.2423$ |
| `gpt-5.6-terra` | GA | 90% | 9.43 | 9.89 | 8.95 | 9 | 8 | 80% | **79.1** | 3배 | 0.1126$ |
| `qwen3.7-plus` | GA | 82% | 8.99 | 9.72 | 9.19 | 20 | 13 | 86% | 57.0 | 14배 | 0.0934$ |

**`gemini-3.1-pro-preview`는 정식 버전이 아니어서 채택 불가.** Google의 3.x 세대 Pro는 전부 preview뿐임.

### 결과 — 벤더별 대표

| 벤더 | 대표 | 정답률 | 비용 |
|---|---|---|---|
| Google | **`gemini-3.8-flash`** | **93%** | **0.0408$** |
| Anthropic | `claude-opus-5` | 92% | 0.4534$ |
| OpenAI | `gpt-5.6-sol` | 92% | 0.2423$ |
| Alibaba | `qwen3.7-plus` | 82% | 0.0934$ |

**축마다 1위가 다름** — 규제 처리·비용은 `gemini-3.8-flash`, 번역 품질·안정성은 `claude-opus-5`, 속도는 `gpt-5.6-terra`. **비용은 11배 차이.**

### 검증 방법

**5축**을 3단계로 나눠 측정함. 전부 기계 또는 LLM이 채점함.

| 축 | 측정 |
|---|---|
| 토큰량 · 시간 | 기계 — API 응답 실측 |
| 금지표현 (지시 준수) | 기계 — **함정 72쌍**, 규제 표를 문맥에 맞게 적용했는가 |
| 환각 (숫자·성분/인증명) | 기계 — 원문 숫자 대조 + `expect_term` 정답 사전 |
| 환각 (원문에 없는 정보) | **LLM judge** |
| 번역 퀄리티 (현지화) | **LLM judge** |

**함정 72쌍** — 규제 표의 금지표현이 등장하는 자리를 기대 동작별로 분류함.

| 기대 | 쌍 | 유형 |
|---|---|---|
| **유지** (바꾸면 안 됨) | 41 | 효능 부인·고지 / 제품명 / 제3자 인용 / 법령·고시 인용 / 수상 부문명 |
| **치환** (바꿔야 함) | 23 | 효능 주장 / 부분일치 / 조사 결합 / 강한 주장 |
| **양립** (둘 다 정답) | 8 | UI 라벨·카테고리 — 사람·모델·judge가 모두 갈려 한쪽으로 정하지 않음 |

**LLM judge** — `claude-opus-5` × `gemini-3.1-pro-preview` 2종 **블라인드 교차**. 어느 모델의 번역인지, 토큰량·소요가 얼마인지 채점자에게 알려주지 않음. 분야별 1~10점만 받고 **평균·가중·순위는 전부 로컬에서** 냄.

- **양측 저점** = 두 채점자가 **모두** 7점 이하로 본 건. 판정 반영분
- **일치율** 70% 미만이면 judge 축 폐기 — 전원 통과
- **`부당`** = 규제 적용을 잘못한 건. 미적용·과잉 적용 양방향

### 과정

**1단계 — 문제는 모델이 아니라 지시였음.** 프롬프트 규칙 하나만 바꿔 대조함.

| variant | 규칙 2 | 정답률 |
|---|---|---|
| `v0_baseline` | "표에 있으면 반드시 바꾼다" | **33%** — 치환 15/15, 억제 0/30 |
| `v1_exception` | 바꾸지 말아야 할 문맥 **3종 열거** | 75% — 열거한 3유형 30/30, **열거 안 한 3유형 1/19** |
| **`v6_principle`** | **열거 대신 원리** | **89%** |

`v0_baseline`의 출력은 영어로 성립조차 하지 않음 — `의학적으로 입증된 바 없습니다` → `not dermatologist-informed proven`. **열거식은 가르친 것만 한다**는 것이 실측으로 확정됨.

**2단계 — 예선 12종.** 티어 낙폭이 벤더마다 달랐음(Anthropic −39%p / OpenAI −6%p / Google −55%p). `qwen3.8-flash`는 1문서 305초·출력 16,174토큰으로 **품질과 무관하게 소요 탈락함**.

**3단계 — 결승.** judge 블라인드 교차 채점. **기계 detector와 LLM judge가 서로 다른 방법으로 같은 순서를 가리킴.**

### 미해결 2건 — 최종 결정을 내리지 않은 이유

| # | |
|---|---|
| 1 | **규제 매핑 테이블이 테스트용 더미임.** 변별의 대부분이 규제 함정에서 나오므로 실제 표에서 순위가 바뀔 수 있음 |
| 2 | **채택 우선 축이 미정.** 규제 처리 / 품질 / 속도 / 비용의 1위가 갈리고 비용은 11배 차이 |

### 규제 표 스펙 요구안 — 데이터 담당 합의 입력물

검증 과정에서 현재 더미 표의 **구조적 결함**이 드러남.

| # | 요구 | 근거 |
|---|---|---|
| 1 | `제외문맥`을 **열거하는 방식은 한계가 있음** | 열거식 프롬프트가 미열거 문맥에서 1/19 |
| 2 | **적용 범위**(자사 표시면 포함 여부) 명시 필요 | UI 라벨 판정이 세 번 뒤집혔고 그때마다 순위가 바뀜 |
| 3 | 대체표현은 금지표현의 **직역과 구별**될 것 | `재생 → renewing`은 직역과 구별 불가 → **판정 자체가 불가** |
| 4 | 서로 다른 금지표현이 **같은 대체표현으로 수렴 금지** | `노화 방지`·`안티에이징` → 둘 다 `age-defying` |
| 5 | 대체표현이 원문의 **숫자·고유명사를 지우지 말 것** | `100% → pure` → 수치 소실 |

---

## 4. 제품컷 누끼 — **판정 미완**

### 결과

| variant | 모델 | 크기 | 이미지 | 장당 소요 |
|---|---|---|---|---|
| `rembg_u2net` | `u2net` | 167.8 MB | 1장 | 0.47s |
| `rembg_isnet` | `isnet-general-use` | 170.4 MB | 1장 | 0.87s |
| **`rembg_birefnet`** | `birefnet-general` | 927.6 MB | **12장** | 9.7s |
| `rembg_birefnet_dis` | `birefnet-dis` | 약 900 MB | 6장 | 10.3s |

`birefnet-general` 12장을 유형별로 보면 결과가 갈림.

| 유형 | 이미지 | 결과 |
|---|---|---|
| **제품만, 인물 없음** | `5` `7` `9` `11` | **우수** — 목표 달성 |
| 인물 + 제품 | `1` `3` `10` `12` | 인물이 함께 따짐 |
| **텍스트 위주** | `4` `6` `8` | **실패** — 텍스트·아이콘을 전경으로 인식 |

### 검증 방법

`이미지 × variant` 단위 A/B/C 육안 판정(제안). 출력은 투명 PNG·마스크·체크무늬 시각화 3종. CPU 실행, 비용 0.

### 과정

**네 모델 모두 "제품인지"를 판단하지 못함.** 차이는 무엇을 전경으로 보느냐일 뿐임 — `u2net`은 인물만, `birefnet-dis`는 글자 획까지 분리함.

> **이것은 선별 문제가 아니라 분류 문제임.** 범용 배경제거는 "전경 vs 배경"까지만 하고, **전경 안에서 제품만 고르는 것은 별도 단계**가 필요함.

후속 단서 — **덩어리가 적고 크면 제품, 많고 잘면 텍스트**라는 경향이 보임(`5.jpg` 4개 우수 / `8.jpg` 176개 실패).

---

## 5. 과업을 가로지르는 발견

**① 번역과 누끼가 같은 벽에 부딪힘 — 대상의 의미 분류**

| 과업 | 막힌 지점 |
|---|---|
| 로컬라이징 번역 | `미백 앰플`이 제품명인가 효능 주장인가 — 정식 모델 전부 0/5 |
| 제품컷 누끼 | 전경 중 무엇이 제품인가 |

→ **상류가 `role` 정보를 산출해 넘기면 둘 다 해소됨.**

**② 생성 모델(SD 계열)은 이 파이프라인에 부적합함**

설정을 어떻게 바꿔도 **지운 자리에 없던 것을 만들어 넣음**. 이 파이프라인이 필요로 하는 것은 **아무것도 새로 만들지 않는 것**인데 생성 모델의 본질과 어긋남. 상세페이지에 없던 내용이 들어가면 **상품 정보 오도 소지**도 있음.

**③ 측정 도구 자체가 검증 대상임**

번역 검증에서 판정기 결함을 세 번 발견함 — detector 오탐 5항목, judge 축 역전, judge 캐시 지문 누락. 셋 다 **선실측(0.2$)이 본 실행(3$) 전에 잡아냄.**

---

## 6. 지출

| 과업 | 실비 |
|---|---|
| 텍스트 추출 · 원문 지우기 · 제품컷 누끼 | **0$** (전부 로컬 실행) |
| **로컬라이징 번역** | **약 14.2$** |

번역 내역 — 재계획 이전 2.5$ / 1단계 0.3$ / 2단계 예선 2.4$ / 3단계 judge 9.0$

---

## 7. 남은 작업

| # | 과업 | 작업 | 담당 |
|---|---|---|---|
| 1 | **로컬라이징 번역** | **규제 매핑 테이블 실제 스펙 합의** — 3장 요구안 5항목 | 데이터 담당 |
| 2 | 로컬라이징 번역 | 채택 우선 축 결정 (규제 / 품질 / 속도 / 비용) | 팀 |
| 3 | 제품컷 누끼 | 후처리 규칙 또는 대상 지정 방식(SAM) 검토 | _미정_ |
| 4 | 공통 | **`role` 산출 과업** — 번역·누끼 공통 전제 | 팀 |

**1번이 풀려야 번역의 결론이 잠정에서 확정으로 바뀜.**
