# CLAUDE.md

기술 PoC 검증용 저장소. 서비스 코드가 아니라 후보 기술을 실행해보고 결과를 기록하는 코드다.
전 과업 로컬 실행.

## 디렉토리 규칙

**과업별로 폴더를 만든다.**

- 과업 폴더는 착수 시점에 생성한다. 미착수 과업의 빈 폴더는 미리 만들지 않는다.
- 과업 간 코드를 공유하지 않는다. 필요하면 복사한다. PoC라 중복보다 격리가 우선.

과업 폴더 내부:
- `poc/{과업}/run.py` — 실행 진입점
- `poc/{과업}/results/{variant}/` — 변형별 결과. `vis/` 하위에 bbox 시각화 이미지
- `poc/{과업}/summary.md` — PoC 문서에 옮겨붙일 요약표

variant는 실험 조건명으로 짓는다 (baseline, vl_model, upscale_2x, clahe 등).

데이터:
- `data/images/` — 테스트 이미지 원본. 하위 폴더로 나누지 않는다.

## API 비용
- 키는 `.env`에서만 읽는다. 하드코딩 금지. `.gitignore` 포함 여부 확인.
- **유료 API 호출 전 예상 건수·비용을 보고하고 확인받는다.** 현재 유료 대상은 ① 상용 OCR 참조 측정(1회) ② C 번역 LLM 벤치마크 두 곳. PaddleOCR·전처리·인페인팅은 로컬 실행이라 해당 없음.
- API 응답은 로컬에 캐싱한다. 재실행 시 캐시 우선.

## 작업
- 검증 작업 전, 검증 방식에 대해 요약 설명한다.
- 생성 파일이 어떤 역할을 하는지 표로 설명한다.

## 평가
- 정량적으로 평가하기 쉬운 검증은 정량적으로 평가한다.
- 정량적으로 평가하기 어려운 검증은 육안으로 평가한다.
코드의 역할은 사람이 비교하기 쉽게 만드는 것까지다:
- 변형별 인식 텍스트를 나란히 볼 수 있게 출력
- bbox는 원본에 그려 시각화 이미지로 저장
- 등급 칸은 비워둔 표 틀만 생성

## 문서 작성
- 개조식 한국어. `~함`, `~됨`, `~필요` 형태로 끝낸다. 경어체·설명체 금지.
- 팀 내부용이므로 배경 설명 없이 결론부터.
- 표를 우선한다.
- 확정되지 않은 항목은 `_미정_`으로 표시하고 임의로 채우지 않는다.

## 금지
- `docs/` 문서는 명시적 요청 없이 수정하지 않는다.
- 수집 데이터를 재배포·외부 공유하는 코드를 작성하지 않는다.
- 결과 등급을 임의로 채워넣거나 요약하지 않는다.