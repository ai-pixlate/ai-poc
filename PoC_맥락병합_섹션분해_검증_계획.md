# 섹션 분해 — 처리 단위 OCR → 맥락 병합 검증 계획

> **2026-09-17 보류** — 단계 1까지 실행. 사유·재개 조건은 `poc/context_merge/summary.md` 상단. 섹션 분해는 `color_snap_vlm2` 유지, 맥락은 규제·현지 부적합 판정 입력에서 보강(`PoC_추가검증_계획.md`).

## 요약

| 항목 | 내용 |
|---|---|
| 목적 | 텍스트 맥락으로 섹션을 묶는 방식이 `color_snap_vlm2`보다 맥락 경계를 잘 잡는지, **파이프라인 소요를 늘리지 않는지** 확인 |
| 배경 | 현행은 경계 대부분(배경색 전환 54건)을 픽셀 신호로만 정함. 텍스트는 3,000px 초과 무변화 구간에서 VLM이 이미지로 읽을 때만 반영됨 |
| 방식 | 여백 처리 단위(`ws_std` 293개)로 OCR → 단위별 블록 → **LLM이 단위 사이 경계 번호만 골라** 섹션으로 묶음 |
| 표본 | 골든 샘플 3상품 34장 (현행 확정과 같은 표본) |
| 비교 기준 | `color_snap_vlm2` 결과 102섹션 — 재실행 안 함 |
| 과업 폴더 | `poc/context_merge/` — 착수 시 생성 |
| 비용 | 약 $0.3 이내 (추정 — 실행 전 재보고) |
| 게이트 | 시간 게이트 → 품질 게이트 순. **시간 게이트 미달 시 품질 판정 전에 중단** |

---

## 1. 소요시간 — 순증은 맥락 병합 LLM 호출뿐

### 구간 비교

| 구간 | 현행 (`color_snap_vlm2` → OCR) | 후보 (처리 단위 OCR → 맥락 병합) |
|---|---|---|
| 분해 (로컬) | ①② 색 전환·스냅 수 초 | `ws_std` 여백 절단 2.67s |
| 분해 (API) | ③ VLM 11호출 **80.6s** | — |
| OCR | 섹션 102개 28.43s + 4,000px 초과 분할 | 처리 단위 293개 **28.95s** (`poc/ocr_split` 실측). 추가 분할 불필요 — 단위 최대 2,112px |
| 맥락 병합 (API) | — | **측정 대상** — 페이지당 1호출, 34호출 |
| 섹션 좌표로 옮기기 | — | 단위 OCR 결과를 `top_offset` 차이만큼 이동. 재OCR 없음. `heuristic_v2` 섹션 단위 재실행 1.3s |

- OCR은 **추가가 아니라 순서 이동**임. 하류(줄·문단 병합)는 옮긴 OCR 결과를 그대로 씀
- 따라서 비교는 **VLM 80.6s vs 맥락 병합 호출 시간** 한 쌍으로 좁혀짐
- 입력이 텍스트뿐이라 이미지 입력(VLM 11호출 in 46,541)보다 호출당 가벼울 것으로 예상 — 미측정

### 참고 — 현행 파이프라인 API 소요 (34장, 전부 순차 호출)

| 단계 | 소요 |
|---|---|
| 섹션 분해 ③ | 80.6s |
| 줄·문단 병합 `llm_assist` | 472.6s |
| 제품 라벨 `vlm_relation` | 440.2s |
| 번역 | 466.5s |

- 기존 코드는 전부 **순차 호출**. 동시 호출 시 소요 변화는 미측정 → 이번에 함께 측정

### 시간 측정 방법

| 항목 | 방법 |
|---|---|
| 캐시 | 시간 측정 실행은 **캐시 미사용**(`--no-cache`). 품질 판정은 캐시 응답 사용 |
| 반복 | variant별 3회 — 중앙값 보고 |
| 모드 | 순차 / 동시 8 두 가지 |
| 지표 | 34장 합계 · 페이지당 p50·p95 · **최장 페이지(`219554_002`, 37,665px) 단건** |
| 기록 | `results/{variant}/timing.json` — 호출별 시작·종료·토큰 |

### 시간 게이트

| 항목 | 기준 |
|---|---|
| 34장 합계 (순차) | **현행 ③ 80.6s 이하** — 제안값, 확정 필요 |
| 최장 페이지 단건 | _미정_ — 상품 1건 처리 대기 허용치 필요 |
| 선행 차단 | 착수 첫 단계에서 최장 페이지 1건만 호출. 단건이 기준을 크게 넘으면 전체 실행 없이 중단 |

---

## 2. 방식

### 처리 흐름

| 단계 | 내용 |
|---|---|
| ⓐ 처리 단위 | `section_split/results/ws_std/sections/*.json` 복사해 사용 (293단위, 강제 분할 0) |
| ⓑ OCR | 단위별 PaddleOCR `baseline` — 확정 조건 그대로 |
| ⓒ 단위 블록 | 단위별 `heuristic_v2` |
| ⓓ 맥락 병합 | 페이지 하나를 1호출로. 단위 순서대로 `#번호 · 높이 · 블록 텍스트(글자 높이 px)`를 줌. **새 섹션이 시작되는 단위 번호만** 고르게 함 |
| ⓔ 섹션 조립 | 고른 번호로 단위를 이어 붙여 섹션 이미지 · `top_offset` · `range` 산출. OCR 결과는 좌표 이동 |
| 폴백 | 응답 파싱 실패 → 그 페이지는 ①② 배경색 경계로 대체. 폴백 건수 기록 |

- 경계는 **단위 사이에서만** 고를 수 있음 → 글자 관통·스냅 실패가 구조상 없음 (v2 번호 선택과 같은 원리)
- 프롬프트 경계 규칙은 `color_snap_vlm2` v2를 그대로 옮김 — 소제목 등장·대상 전환·역할 전환에서 나누고, 소제목–설명 · 항목 나열 · 시험 결과–각주 사이는 나누지 않음
- 모델 `gemini-3.8-flash` · `temperature=0` — 파이프라인 다른 단계와 같음

### variant

| variant | 입력 | 의도 |
|---|---|---|
| `ctx_text` | 단위별 블록 텍스트 · 글자 높이만 | 텍스트 맥락 단독 성능 |
| `ctx_hint` | `ctx_text` + 단위 사이 **배경색 전환 표시**(①) · 글자 없는 단위 표시 | 색 신호를 참고로 주고 최종 판단은 LLM. 사진만 있는 단위의 소속 결정 보조 |
| (기준) `color_snap_vlm2` | 기존 결과 | 재실행 안 함 |

- 글자 없는 단위(사진만)는 `[글자 없음 · 높이 N px]`로 넘김. `ctx_text`에서 소속이 흔들릴 것으로 예상 — `ctx_hint`와 비교 포인트

---

## 3. 품질 평가

### 정량 (자동)

| 지표 | 방법 | 통과 기준 |
|---|---|---|
| 블록 관통 | `section_split/check_cut.py` 복사 — 전체 OCR 캐시 대조 | 0 |
| `top_offset` 검산 | 연속성 · 시작점 · 크롭 높이 · 좌표 왕복 복원 | 오류 0 |
| 섹션 분포 | 섹션 수 · 중앙 높이 · 최대 · 5,000px 초과 수 | 기록만 — 기준 _미정_ |
| 현행과 경계 일치 | 현행 절단 68개 대비 ±100px 안 일치 · 후보만 있음 · 현행만 있음 | 기록만 |
| 배경색 전환 처리 | ① 54건 중 LLM이 유지한 수 / 합친 수 | 기록만 |

### 육안 (판정표)

전수 정답지는 만들지 않음. **두 방식이 다른 경계만** 판정함.

| 판정 대상 | 판정 값 |
|---|---|
| 후보만 자른 경계 | 맞음 / 틀림 / 둘 다 가능 |
| 현행만 자른 경계 | 맞음 / 틀림 / 둘 다 가능 |
| 알려진 사례 `213548_018` y629 | `RECOMMEND` – `이런 분들에게 추천해요` 분리 해소 여부 |

- `compare.py`가 경계 차이 사례마다 앞뒤 단위 크롭 + 블록 텍스트를 나란히 그린 대지를 만들고, 등급 칸은 비운 표 틀만 생성
- 판정 — Claude 1차 → 예람님 검토
- 필요 시 기존 동료 투표 4장과 같은 방식 재시행 — _미정_

### 품질 게이트

| 항목 | 기준 |
|---|---|
| 블록 관통 · 검산 오류 | 0 |
| 차이 경계 판정 | 후보가 맞은 수 > 현행이 맞은 수 — 제안값, 확정 필요 |
| 폴백 발동 | _미정_ |

---

## 4. 실행 순서

| 순서 | 작업 | 비용 | 중단 조건 |
|---|---|---|---|
| 0 | 처리 단위 복사 · 단위 OCR · 단위 블록 | 0 | — |
| 1 | **최장 페이지 1건 맥락 병합 호출** (두 variant) | 소액 | 단건 소요가 게이트를 크게 넘음 |
| 2 | 34장 전체 · 순차 3회 · 동시 8 3회 | 대부분 | 시간 게이트 미달 → 품질 판정 생략하고 종결 |
| 3 | 관통 · 검산 · 분포 · 경계 일치 집계 | 0 | 관통 1건 이상 → 원인 확인 후 판단 |
| 4 | 차이 경계 대지 생성 → 육안 판정 | 0 | — |
| 5 | 결과 정리 → `summary.md` | 0 | — |

---

## 5. 비용 (추정)

| 항목 | 산식 | 추정 |
|---|---|---|
| 1회 실행 (variant 1개 · 34호출) | 입력 텍스트만 — 줄·문단 병합 골든 실측(in 140k · $0.176)보다 입력 작음 | $0.02~0.05 |
| 시간 측정 반복 | 2 variant × (순차 3 + 동시 3) = 12회 | $0.24~0.6 |
| 합계 | | **$0.3 안팎** — 반복 횟수 줄이면 감소. 실행 전 재보고 |

---

## 6. 생성 파일 (착수 시)

| 파일 | 역할 |
|---|---|
| `poc/context_merge/run.py` | 진입점 — 단위 OCR · 블록 · 맥락 병합 · 섹션 조립. `--variant` `--images` `--no-cache` `--concurrency` |
| `poc/context_merge/check_cut.py` | 블록 관통 · `top_offset` 검산 (`section_split`에서 복사) |
| `poc/context_merge/compare.py` | 현행 대비 경계 차이 집계 · 대지 생성 · 판정표 틀 |
| `poc/context_merge/cache/` | LLM 응답 캐시 — 키에 프롬프트 지문 포함 |
| `poc/context_merge/results/{variant}/sections/` | 섹션 json (`top_offset` · `range` · 구성 단위 · 섹션 이름) |
| `poc/context_merge/results/{variant}/vis/` | 페이지별 경계 시각화 — 현행 경계와 겹쳐 그림 |
| `poc/context_merge/results/{variant}/timing.json` | 호출별 소요 · 토큰 |
| `poc/context_merge/summary.md` | 요약표 · 판정표 (등급 칸 비움) |

---

## 7. 한계 (예상)

| # | 한계 |
|---|---|
| 1 | 맥락 경계 정답지가 없음 — 차이 경계만 판정하므로 두 방식이 **같이 틀린 경계**는 안 잡힘 |
| 2 | OCR 오독이 맥락 판단에 그대로 들어감 (저신뢰 영역 4.9%) |
| 3 | 섹션 길이 상한 없음 — 하류 VLM 크롭(긴 변 1024px) 축소는 별도 확인 필요 |
| 4 | API 지연은 시점·부하에 따라 변동 — 3회 중앙값으로만 완화 |
