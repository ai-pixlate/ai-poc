# AGENTS.md

기술 PoC 검증용 저장소. 서비스 코드가 아니라 후보 기술을 실행해보고 결과를 기록하는 코드다.
전 과업 로컬 실행.

## 디렉토리 규칙

**과업별로 폴더를 만든다.**

- 과업 폴더는 착수 시점에 생성한다. 미착수 과업의 빈 폴더는 미리 만들지 않는다.
- 과업 간 코드를 공유하지 않는다. 필요하면 복사한다. PoC라 중복보다 격리가 우선.

과업 폴더 내부:
- `poc/{과업}/run.py` — 실행 진입점
- `poc/{과업}/results/{variant}/` — 변형별 결과. `vis/` 하위에 bbox 시각화 이미지
- `poc/{과업}/summary.md` — PoC 문서에 옮겨붙일 요약표

variant는 실험 조건명으로 짓는다 (baseline, vl_model, upscale_2x, clahe 등).

골든 샘플 재실행 (`PoC_골든샘플_재실행_계획.md`):
- `poc/golden/{단계}_{과업}/` — 단계별 결과 모음. `results/{variant}/` + `summary.md`. 단계 번호는 계획 3장 실행 순서
- 코드는 과업 폴더에 둔다(`--sample golden`). **출력 경로는 바꾸지 않는다** — 과업 폴더 `results/golden/` · `summary_golden.md`로 나온 뒤 `python poc/golden/move.py {단계}`로 이동
- 재실행 시 `move.py {단계} --restore`로 되돌린 뒤 실행하고 다시 이동한다. 채운 등급을 compare가 원위치 판정표에서 회수하기 때문
- 다음 단계가 읽는 앞 단계 결과는 `poc/golden/` 이동 위치를 입력으로 쓴다
- 단계 폴더는 이동 시점에 생긴다. `poc/golden/README.md`에 단계별 현황·판정표 링크

데이터:
- `data/images/` — 초기 테스트 이미지 원본(12장 표본 `.jpg` + `.PNG` 3개). 하위 폴더로 나누지 않는다.
- `data/golden_sample/images_{상품코드}/` — 골든 샘플 원본(3상품 34장). 상품별 하위 폴더 구조를 그대로 둔다. 코드는 `rglob`으로 읽는다.
- `data/`는 재배포 금지 대상이라 git에 올리지 않는다(`.gitignore`).

## API 비용
- 키는 `.env`에서만 읽는다. 하드코딩 금지. `.gitignore` 포함 여부 확인.
- **유료 API 호출 전 예상 건수·비용을 보고하고 확인받는다.** LLM·VLM을 부르는 스크립트는 전부 해당 — 대개 `run_llm.py` · `run_vlm*.py` 이름이며, 번역 벤치마크(`C_translate`) · 줄·문단 병합 · 제품 라벨 · 번역 길이 · 섹션 분해 VLM · 누끼 요소 선별 · 섹션 판정 등. `--dry-run`이 있으면 먼저 돌려 추정치를 보고한다. PaddleOCR·LaMa·누끼 모델 등 로컬 실행은 해당 없음.
- API 응답은 로컬에 캐싱한다. 재실행 시 캐시 우선.

## 작업
- 검증 작업 전, 검증 방식에 대해 요약 설명한다.
- 생성 파일이 어떤 역할을 하는지 표로 설명한다.

## 평가
- 정량적으로 평가하기 쉬운 검증은 정량적으로 평가한다.
- 정량적으로 평가하기 어려운 검증은 육안으로 평가한다.
코드의 역할은 사람이 비교하기 쉽게 만드는 것까지다:
- 변형별 인식 텍스트를 나란히 볼 수 있게 출력
- bbox는 원본에 그려 시각화 이미지로 저장
- 등급 칸은 비워둔 표 틀만 생성

## 문서 작성
- 개조식 한국어. `~함`, `~됨`, `~필요` 형태로 끝낸다. 경어체·설명체 금지.
- 팀 내부용이므로 배경 설명 없이 결론부터.
- 표를 우선한다.
- 확정되지 않은 항목은 `_미정_`으로 표시하고 임의로 채우지 않는다.

## 금지
- `docs/` 문서는 명시적 요청 없이 수정하지 않는다.
- 수집 데이터를 재배포·외부 공유하는 코드를 작성하지 않는다.
- 결과 등급을 임의로 채워넣거나 요약하지 않는다.