# 섹션 분해 — 처리 단위 OCR → 맥락 병합

> 계획: `PoC_맥락병합_섹션분해_검증_계획.md`. 시간 게이트 34장 순차 합계 **80.6s 이하**(현행 `color_snap_vlm2` ③ 실측).

## 상태 — **보류** (2026-09-17)

단계 1(최장 페이지 단건)까지 실행 후 보류. 단계 2(34장 측정) 미실행. 지출 **$0.0504**.

| # | 보류 사유 |
|---|---|
| 1 | **착수 목적이 섹션 분해 자체가 아님.** 섹션 단위 규제·현지 부적합 판정에 맥락이 필요하다는 판단에서 출발함. 맥락은 **판정 입력(앞뒤 섹션 텍스트 · 페이지 개요)으로 보강 가능** — 섹션 경계를 바꿀 필요가 확인되지 않음 |
| 2 | **입도가 현행보다 굵음.** 최장 페이지 현행 23섹션 · 최대 3,188px 대비 `ctx_hint2` 14섹션 · 최대 5,292px, `ctx_text2` 13섹션 · 최대 6,846px. 판정 VLM 입력(긴 변 1024px)에서 긴 섹션은 글자 판독 불리 |
| 3 | **텍스트 단독으로 입도 조절 안 됨.** 입도 규칙(v2)은 `ctx_hint2`(배경색 힌트 포함)에서만 효과 — 결국 현행 신호에 기댐 |
| 4 | **교체 비용이 큼.** 골든 재실행 7단계가 `color_snap_vlm2` 102섹션 위에서 종결됨. 섹션 분해 교체 시 전 구간 재실행 필요 |
| 5 | 확인된 것은 1페이지뿐 — 34장 일반화 안 됨. 우열 판정이 아니라 **착수 근거 소멸**에 따른 보류임 |

**재개 조건** — 규제·현지 부적합 판정 검증(`PoC_추가검증_계획.md`)에서 **섹션 경계 기인 오판**이 앞뒤 맥락 보강으로도 해소되지 않고 반복될 때.

**재사용 가능한 것** — 처리 단위 OCR 결과(`results/units/`) · 섹션 이름 붙이는 프롬프트(판정용 페이지 개요 생성에 활용 가능) · 단위 사전 병합 기준(간격 ≤ 큰 글자 높이 × 0.5 → 두 줄 제목·라벨 보호).

## 단계 0 — 처리 단위 OCR (34장)

| 항목 | 값 |
|---|---|
| 처리 단위 | `ws_std` 293개 |
| region | 2,257 (`poc/ocr_split` `unit_ws_std`와 같음) |
| OCR | 31.13s · 비용 0 |
| 단위 블록 `heuristic_v2` | 0.06s |

## 단계 1 — 최장 페이지 단건 (`A000000219554_002`, 1000×37,665px · 단위 68)

캐시 미사용 · 순차 · 3회.

| variant | 소요 (3회) | 중앙값 | 토큰 in / out | 비용 (3회 합) | 섹션 |
|---|---|---|---|---|---|
| `ctx_text` | 5.03 · 2.39 · 2.27s | **2.39s** | 7,259 / 192~214 | $0.0186 | 13 (3회 동일) |
| `ctx_hint` | 5.70 · 3.43 · 3.80s | **3.80s** | 7,381 / 207~214 | $0.0190 | 13 (3회 동일) |
| (현행) `color_snap_vlm2` | 이 페이지 VLM 2호출 — 호출별 소요 미기록. 11호출 평균 7.3s 기준 약 15s (추정) | | | | 23 |

- 파싱 실패 · 폴백 · 재시도 0
- **시간 게이트 단건 통과** — 선행 차단 조건 해당 없음. 34장 순차 합계는 단계 2에서 측정

### 경계 관찰 (판정 아님 — 단계 4 대상)

| 항목 | 내용 |
|---|---|
| 섹션 입도 | 현행 23 → 13. 최대 6,846px(`[15457, 22303]` — POINT 02 · 저자극 테스트 · 쿨링 · 발림성 합쳐짐). 현행 v1 과소분할과 비슷한 양상 |
| y8462 절단 | 두 variant 모두 자름. **현행 결과 문서의 `color_snap_vlm` 두 줄 제목 분리 사례와 같은 자리** — `오직, 징크 성분으로 피부에 순한`이 단위 #15 끝, `민감피부 / 저자극 무기자차`가 단위 #16. 처리 단위 경계가 이미 제목을 가르고 있어 LLM은 #16·#17 중에서만 고를 수 있음 |
| `ctx_hint` 차이 | 하단(y25284 이후) 경계를 배경색 전환 쪽으로 옮김 — 사용 팁 · 라인업 · 친환경 패키지 경계가 현행과 일치 |

## 보완 — v2 (`ctx_text2` · `ctx_hint2`)

| 보완 | 내용 |
|---|---|
| 단위 사전 병합 | 앞 단위 마지막 블록–뒤 단위 첫 블록 간격 ≤ 큰 쪽 글자 높이 × **0.5**이면 한 단위로 봄. 골든 251경계 중 이 구간은 대부분 "리드 문구·라벨 → 큰 제목"(y8462 0.25 · `RECOMMEND` 0.47). 34장 **16경계 병합**(293 → 277단위). OCR 재실행 없음 |
| 입도 규칙 | 섹션 보통 단위 2~6개 · 1,000~3,000px. 3,000px 초과 시 소제목 재확인. 머리말에 페이지 글자 높이 중앙값 · 상위 25% 표시. 시험·테스트 결과는 별도 논점 |
| 라벨 규칙 | 소제목 위 작은 라벨(RECOMMEND · POINT 01)은 아래 소제목과 같은 섹션 — v1부터 포함 |
| vis 범례 | 상단에 variant명 · 섹션 수 · 선 색 범례. 오른쪽 가장자리 회색 눈금 = 고를 수 있던 단위 경계 |

### 최장 페이지 단건 (캐시 미사용 · 1회)

| variant | 단위 | 소요 | 토큰 in / out | 비용 | 섹션 | 최대 높이 |
|---|---|---|---|---|---|---|
| `ctx_text` | 68 | 2.39s (3회 중앙값) | 7,259 / 192 | $0.0062 | 13 | 6,846 |
| `ctx_hint` | 68 | 3.80s (3회 중앙값) | 7,381 / 214 | $0.0063 | 13 | 5,292 |
| `ctx_text2` | 65 | 3.79s | 7,396 / 217 | $0.0064 | 13 | **6,846** |
| `ctx_hint2` | 65 | 8.72s | 7,518 / 234 | $0.0065 | **14** | **5,292** |
| (현행) `color_snap_vlm2` | — | 약 15s (추정) | — | — | 23 | 3,188 |

- **y8462 두 줄 제목 분리 해소** — v2 두 variant 모두 y8808에서 자름(현행과 같은 자리)
- **과소분할은 일부만 해소** — `ctx_hint2`만 POINT 02 구간(6,846px)을 `[15457, 19350]` · `[19350, 22303]` 둘로 나눔. `ctx_text2`는 그대로
- 남은 긴 섹션 — `ctx_hint2` `[26353, 31645]` 5,292px 라인업. 현행은 이 구간을 색 전환으로 4개(879 · 825 · 763px 포함)로 나눔
- `ctx_hint2` 8.72s는 1회 측정 — 변동인지 단계 2 반복 측정으로 확인 필요
- 판정 아님 — 단계 4 대상
