# PoC 추가 검증 결과

> 작성 2026-09-08. 계획 문서: `PoC_추가검증_계획.md` · 실행 기록: `poc/{과업}/summary.md`
> **결과만 담음.** 판정 기준·실행 방법은 계획 문서에 있음.

---

## 0. 현황

| 과업 | 채택 | 결과 | 상태 |
|---|---|---|---|
| **줄·문단 병합 + 역할 분류** | **`llm_assist`** (휴리스틱 + `gemini-3.8-flash`) | 병합 92% · 역할 92% | **확정** |
| **제품 라벨 판정** | **`vlm_relation`** (`gemini-3.8-flash` + 원본 이미지) | 미탐 0 · 오탐 0 | **확정** |
| **섹션 분해 + 절단 지점 산출** | **`color_snap_vlm2`** (배경색 전환 + VLM 보조) | 동료 투표 67% · 블록 관통 0 · 최대 4,491px | **확정** |
| **번역 길이 팽창률** | **조판 단독** (번역 압축 없음) | **초과율 94%** — 조판으로 흡수 불가 | **확정** |
| **스타일 추출** | **`otsu_border`** (Otsu + 테두리 규칙) | 색 91% · 크기 100% · 정렬 91% | **확정** |
| **브랜드 로고 제외** | **`block_exact`** (블록 텍스트 == 브랜드명) | 찾음 6 · 놓침 1 · 오탐 0 | **확정** |

---

## 1. 줄·문단 병합 + 역할 분류 — **확정**

### 결과

| 항목 | 결정 |
|---|---|
| 구성 | **휴리스틱 + LLM 보정 2단계** |
| 1단계 | 좌표 휴리스틱 `heuristic_v2` — `line_gap=0.7` `para_gap=0.6` `h_ratio=1.5` `overlap=0.5` `gutter=on` |
| 2단계 | **`gemini-3.8-flash`** · `temperature=0` — 추가 병합 + 역할 재판정. **분할은 시키지 않음** |
| 입력 | 텍스트 인식 `baseline` region. 이미지는 넣지 않음 |

### 판정

이미지 12장 · variant별 12행 육안 판정. **게이트 A+B 70%.**

| variant | 병합 | 역할 |
|---|---|---|
| `heuristic_v1` | A9 B2 C1 · **92%** | A8 B1 C2 · **82%** |
| `heuristic_v2` | A10 B1 C1 · **92%** | A8 B1 C2 · **82%** |
| **`llm_assist`** | A11 B0 C1 · **92%** | A10 B1 C1 · **92%** |

**세 variant 모두 게이트 통과.** 병합은 동률이고 `llm_assist`가 **역할에서 10%p 우위**임.

### 건수 합계

| variant | 블록 | 과분할 | 과병합 | 오분류 | 라벨 |
|---|---|---|---|---|---|
| `heuristic_v1` | 159 | 3 | 2 | 4 | 111 |
| `heuristic_v2` | 176 | 3 | 1 | 4 | 123 |
| **`llm_assist`** | **110** | **1** | 1 | 4 | 미계수 |

> ⚠️ **`llm_assist` 행은 라벨을 세지 않음.** 역할 오분류율 분모가 heuristic 행(블록 − 라벨)과 달라 등급 직접 비교 시 llm 쪽이 유리함. 오분류 절대 건수는 세 variant 모두 4건으로 같음.

### 채택 근거

| # | 근거 |
|---|---|
| 1 | **주의문구 검출 1건 → 4건.** 휴리스틱 키워드 매칭이 놓친 것을 문맥으로 잡음. **미탐은 복구 불가 축** |
| 2 | 휴리스틱 공통 실패였던 `4.jpg` 과분할 2건을 해소 (5블록 → 3블록) |
| 3 | 과분할 3 → 1건 |

비용은 이미지당 **약 $0.002**, 소요 **6.7초/장**. 휴리스틱 단독은 0.01초/장·무료임.

### 역할 분포 (기계 집계 — 정오 아님)

| variant | 제목 | 본문 | 캡션 | 가격 | 주의문구 |
|---|---|---|---|---|---|
| `heuristic_v2` | 50 | 85 | 39 | 1 | 1 |
| `llm_assist` | 24 | **1** | **80** | 1 | **4** |

`본문`이 1건으로 줄고 `캡션`으로 몰림. 표본이 제품컷 위주라 실제로 캡션이 맞을 수 있으나 **분포가 통째로 바뀐 것**이라 표본이 넓어지면 재확인 필요함.

### 실측 — 등급 아님

| 항목 | `heuristic_v2` | `llm_assist` |
|---|---|---|
| 블록 | 176 | 110 |
| 겹침(보조) | 38 | 6 |
| 이질(보조) | 4 | 29 |
| 소요 | 0.13s / 12장 | 79.9s / 12장 |
| 토큰 | — | in 17,479 / out 2,656 |
| 비용 | 0 | **$0.0231** |

---

## 2. 제품 라벨 판정 — **확정**

### 결과

| 항목 | 결정 |
|---|---|
| 채택 | **`vlm_relation`** — `gemini-3.8-flash` + **원본 이미지 동봉**(긴 변 1024px) |
| 입력 | `block_role`의 `llm_assist` 블록 110개 |
| 출력 | 블록별 `is_product_label` 참/거짓 |
| 프롬프트 | "애매하면 라벨 쪽으로 답한다"를 명시해 **미탐 회피로 편향** |

### 판정

**정답지 = `vlm_relation`.** 예람님이 12장 전량을 육안 확인해 **미탐 0 · 오탐 0**임을 판정했고, 이를 기준으로 나머지 4종의 미탐·오탐을 블록 단위 대조로 산출함.

> ⚠️ **채택 후보를 정답지로 삼은 구조임.** `vlm_relation` 자체의 정확도는 사람 확인에만 의존하며, 다른 variant와 같은 방식으로 교차 검증되지 않았음. 먼저 손으로 판정한 21행(`1` `2` `4` `5`)과 기계 산출이 **불일치 0건**인 것이 방식의 근거임.

| variant | 미탐 | 오탐 | 라벨 판정 | 오탐률 | 비용 |
|---|---|---|---|---|---|
| **`vlm_relation`** | **0** | **0** | 60 | 0% | $0.027 |
| `role_ext` | 7 | 0 | 53 | 0% | $0.017 |
| `mask_overlap` | 6 | 18 | 72 | 25% | 0 |
| `bg_texture` | 9 | 39 | 90 | 43% | 0 |
| `poly_skew` | 12 | 7 | 55 | 13% | 0 |

**미탐 0은 `vlm_relation` 하나뿐.** 판정 기준이 미탐 0 우선이므로 나머지 4종은 전부 탈락임.

### 후보별 성격

| variant | 방식 | 왜 떨어졌나 |
|---|---|---|
| `role_ext` | 같은 모델·프롬프트, **이미지 없음** | 미탐 7 |
| `mask_overlap` | 제품컷 누끼 마스크와 bbox 교차 | 마스크가 인물·큰 그래픽 문자도 전경으로 잡음 |
| `bg_texture` | 블록 주변 밝기 표준편차 | 그라데이션 배경에서 무너짐. 오탐률 43% |
| `poly_skew` | 인식 poly의 기울기·사다리꼴 | 신호가 가장 약함. 미탐 12 |

로컬 3종은 **한쪽으로 치우친 게 아니라 미탐·오탐이 동시에 남음** — 신호 자체가 약함.

### 실측

| variant | 토큰 | 소요 | 비용 |
|---|---|---|---|
| `role_ext` | in 12,700 / out 1,997 | 59.0s | $0.0170 |
| `vlm_relation` | in 25,710 / out 2,066 | 41.9s | $0.0270 |
| 로컬 3종 | — | 2.3s | 0 |

이미지당 **약 $0.002 · 3.5초**.

---

## 3. 번역 길이 팽창률 — **확정**

### 결과

| 항목 | 결정 |
|---|---|
| 채택 | **조판 단독.** 번역 단계 길이 압축은 쓰지 않음 |
| 번역 | `gemini-3.8-flash` · 프롬프트 `default`(C 과업 `v6_principle` 사본, 규칙 4는 "길이를 원문과 비슷하게") |
| 조정 | 줄바꿈 + 폰트 축소 **80% 상한** |
| **판정** | **"길이 변화는 조판에서 흡수한다" 원칙은 성립하지 않음** |

**흡수되지 않는 나머지는 검수 단계에서 사람이 문구를 고치는 것으로 처리함**(2026-09-09 결정). 자동으로 해결할 방법이 없다는 것이 이 측정의 결론임.

### 측정 조건

| 항목 | 값 |
|---|---|
| 대상 언어 | 영어 (ko → en) |
| 폰트 | **Arial** · 자간 0 |
| em 크기 | 줄 높이 × **1.35** (OCR bbox는 글자 획 높이라 em보다 작음) |
| 줄 높이 | 원본 = bbox 높이 ÷ 원문 줄 수 · 렌더 = em × 1.20 |
| 규제 표 | **테스트용 더미 15항목** — 대체표현 길이가 결과에 섞임 |

가정이 바뀌면 수치가 통째로 움직임. **민감도를 함께 쟀고, 결론은 가정에 의존하지 않음**(아래).

### 초과율

| variant | 조판이 허용하는 것 | `block` (33) | `region` (88) |
|---|---|---|---|
| `nowrap` | 없음 | 100% | 98% |
| `wrap_allowed` | 줄바꿈 | 100% | 100% |
| **`wrap_and_shrink`** | **줄바꿈 + 축소 80%** | **94%** | **100%** |
| `wrap_grow_20` | 줄바꿈 + 높이 2배 | 94% | 98% |

입력 2종 — `block`은 채택 파이프라인(라벨 제외 블록), `region`은 인식 영역 단위. **단위를 바꿔도 결론이 같음.**

### 얼마나 조정해야 들어가는가

| 조정 | 흡수율 (`block`) |
|---|---|
| 폰트 축소 80%까지 | **6%** |
| 축소 60%까지 | 12% |
| 축소 50%까지 | 30% |
| 높이 2배까지 | 6% |
| 높이 5배까지 | 33% |

폭 배율 중앙 **2.2배**, 최대 4.33배. 필요 높이 배수 중앙 **4.85배**.

> **높이 확장이 안 듣는 이유** — 박스가 가로로 좁음. 172px 폭 박스에 `First-Served!` 한 단어가 안 들어가 **줄바꿈 자체가 불가능**한 사례가 나옴. 세로로만 늘리는 건 비효율이며 폭·배치를 바꾸는 축은 초안 배치 과업 소관임.

### 가정 민감도

| em 배수 | 축소 80% 흡수 | 높이 2배 흡수 |
|---|---|---|
| 1.0 (가장 관대) | 15% | 18% |
| 1.15 | 6% | 6% |
| 1.35 (현행) | 6% | 6% |

**가장 관대한 가정에서도 흡수는 15% 남짓.** 결론이 가정에 흔들리지 않음.

### role별 (`block`)

| role | 세그먼트 | 초과 | 폭 배율 중앙 |
|---|---|---|---|
| 제목 | 15 | 13 (87%) | 2.5배 |
| 캡션 | 13 | 13 (100%) | 1.94배 |
| 주의문구 | 3 | 3 (100%) | 2.5배 |

**캡션이 전멸.** 박스가 작아 여유가 없음.

### 탈락 — 길이 압축 프롬프트

규칙 4를 **세그먼트별 글자 수 상한**으로 바꿔 시험함(`block_compress`, $0.0168).

| 지표 | `default` | `compress` |
|---|---|---|
| 폭 배율 중앙 | 2.2배 | **1.04배** |
| 축소 60%까지 흡수 | 12% | **67%** |
| 축소 80% 기준 초과율 | 94% | 94% |

팽창은 확실히 줄었으나 **문구가 훼손됨.**

| 원문 | `default` | `compress` |
|---|---|---|
| `키캡키링 선착순!` | Keycap Keyring: First-Come, First-Served! | **FCFS Keycap** |
| `리센느 원이 오이쉬스` | RESCENE WONI Oishis | **RESCENE Woni** |

`FCFS`는 소비자에게 통하지 않는 축약이고 제품명 일부가 통째로 빠짐. 프롬프트에서 **"의미·성분·수치·인증명 훼손 금지"를 명시했는데도 발생함** — 지시로 막지 못함.

→ **채택하지 않음 (2026-09-09 결정).** 검수 단계에 사람이 문구를 수정하는 경로가 있으므로, 번역 품질을 깎아 길이를 맞추는 것보다 낫다고 판단함.

### 실측

| 입력 | 세그먼트 | 토큰 | 비용 |
|---|---|---|---|
| `block` | 33 | in 8,988 / out 1,889 | $0.0138 |
| `region` | 88 | in 10,422 / out 3,535 | $0.0211 |
| `block_compress` | 33 | in 10,579 / out 2,371 | $0.0168 |

폭 측정은 전부 로컬, 비용 0.

---

## 4. 스타일 추출 — **확정**

### 결과

| 항목 | 결정 |
|---|---|
| 채택 | **`otsu_border`** — Otsu로 가르되 **테두리에 많이 닿는 쪽을 배경**으로 봄 |
| 입력 | 텍스트 인식 `baseline` 영역 280건 + `vlm_relation` 블록(정렬·역할·라벨) |
| 출력 | 영역별 `font_color`·`bg_color`·`est_font_px`, 블록별 `align` |
| 판정 대상 | 제품 라벨을 뺀 **조판 대상 110영역** |
| 비용 | **0** — 전부 로컬, 12장 0.6초 |

### 판정

이미지 단위 육안 A/B/C. `3.jpg`는 조판 대상 0이라 분모에서 제외(11장).

| variant | 색 | 크기 | 정렬 |
|---|---|---|---|
| `dominant_color` | 82% | 100% | 91% |
| `contrast_split` | 82% | 100% | 91% |
| **`otsu_border`** | **91%** | **100%** | **91%** |

세 축 모두 게이트(70%) 통과. **색에서 `otsu_border`가 9%p 우위**임.

`dominant_color`(k-means)와 `contrast_split`(Otsu)는 **사실상 같은 답을 냄** — 글자색 채널 최대차 중앙 1, 눈에 보일 차이(16 초과)는 280건 중 5건뿐. 색 추출 방식 자체는 변별력이 없었고, **갈린 것은 글자·배경을 무엇으로 가르느냐**였음.

### 크기·정렬 규칙

| 축 | 규칙 |
|---|---|
| 크기 | bbox 높이 = 글자 획 높이. em 환산 × 1.35 (길이 팽창률 과업과 같은 가정) |
| 정렬 | 블록 안 **줄들**의 좌·중앙·우 좌표 분산 중 가장 고른 축. 허용 오차 블록 폭의 12% |

정렬은 판정 결과를 5종으로 냄 — `left` 3 / `center` 18 / `right` 2 / **`단일행` 44** / **`무관` 43**. `무관`은 줄 폭이 거의 같아 **어느 정렬로 그려도 결과가 같은** 경우임.

---

## 5. 섹션 분해 + 절단 지점 산출 — **확정**

### 결과

| 항목 | 결정 |
|---|---|
| 채택 | **`color_snap_vlm2`** — 배경색 전환으로 자르고, 색이 안 바뀌는 긴 구간만 VLM이 문맥 경계를 고름 |
| 표본 | **골든 샘플** — 실제 이커머스 상세페이지 3상품 34장 · 140.7M 화소 · 초장축(세로/가로 4 이상) 10장 · 최대 **1000×37,665** |
| 비용 | **$0.0375** (VLM 11호출) |
| 판정 | **동료 투표** 5명 × 4장 — 결과가 갈린 3장에서 **10/15(67%)**, 3장 모두 과반 |
| 블록 관통 | 절단 68개 중 **0** · `top_offset` 검산 오류 **0** |
| 입도 | **소제목 단위** — 동료 투표가 소제목 기준인 프롬프트 v2를 고름 |

### 두 층을 분리함

| 층 | 목적 | 크기 |
|---|---|---|
| 처리 단위 | 텍스트 인식에 넣을 크기. 1000×37,665를 그대로 넣으면 검출기가 25배 축소해 글자가 사라짐 | 여백마다 잘게 |
| **의미 섹션** | 하류(조판·검수)가 쓰는 문맥 단위 — 제품 소개·고민·효과·근거·사용법 등 | 굵직하게 |

처음에 여백마다 잘라 293섹션을 냈다가 **의미 섹션이 아니라 처리 단위**임을 확인하고 분리함.

### 절차 — `color_snap_vlm2`

| 단계 | 내용 |
|---|---|
| ① 배경색 전환 검출 | 행마다 **최빈색**(평균색은 글자·사진에 끌림)을 뽑아 같은 색이 60px 이상 이어지면 배경 구간. 인접 구간 색 차이 40 이상이면 변화 지점 |
| ② 여백 스냅 | 변화 지점을 300px 안의 **여백 한가운데**로 옮김. 없으면 뚜렷한 색 경계선으로. 둘 다 없으면 **자르지 않음** |
| ③ 보조 분해 | 색 변화 없이 **3,000px 넘는 구간**만 VLM에 넘김. 여백 후보선에 번호를 그려 **문맥이 바뀌는 번호를 고르게** 함 |

①의 보완 — 두 배경 구간 사이가 200px 넘게 벌어지면 **사이에 사진이 끼어 있다**고 보고 가운데 대신 **양 가장자리**를 후보로 삼음. 사진 가운데를 자르면 사진 위 글자를 관통함. 이 보완으로 **스냅 실패 6건 → 1건**.

### variant 비교 (34장)

| variant | 방식 | 섹션 | 중앙 높이 | 최대 | 5,000px 초과 | 비용 |
|---|---|---|---|---|---|---|
| `ws_std` | 처리 단위 — 여백마다 | 293 | — | 2,112 | 0 | 0 |
| `ws_edge` | 처리 단위 — 엣지 밀도 | 366 | — | 1,685 | 0 | 0 |
| `gap_major` | 큰 여백 또는 여백 앞뒤 색 차이 | 75 | 1,899 | 4,753 | 0 | 0 |
| `color_snap_ws` | ①② + ③ **후보 1**(여백+병합) | 94 | 1,563 | 3,085 | 0 | 0 |
| `color_snap_vlm` | ①② + ③ **후보 4**(VLM) · 프롬프트 v1 | 86 | 1,342 | **7,521** | **5** | $0.0355 |
| **`color_snap_vlm2`** | ①② + ③ 후보 4 · **프롬프트 v2** | **102** | 1,360 | **4,491** | **0** | $0.0375 |

### 단계별 판단

| 단계 | 실측 | 판단 |
|---|---|---|
| ① 배경색 전환 | 34장에서 **54건** | **강함** — 한국형 상세페이지는 섹션마다 배경을 갈아끼운다는 전제가 맞음 |
| ② 스냅 | 여백 **51** · 경계선 2 · **실패 1** | **강함**. 실패는 사진 배경 섹션 |
| ③ 후보 1 (여백+병합) | 11구간 → 23절단 | **자리가 틀림** — 길이(최대 3,000px)로 병합해 헤드라인과 설문 도입이 섞임 |
| ③ 후보 4 (VLM) v1 | 11구간 → 15절단 | **자리는 맞고 수가 적음** — 7,521px 구간을 통째로 둠 |
| ③ 후보 4 (VLM) **v2** | 11구간 → **31절단** | **소제목 단위로 갈림** — 5,000px 초과 섹션 없음 |

**37,665px 페이지 상단 22,303px**(같은 톤 배경이라 색 신호가 없는 구간)가 v2에서 12섹션으로 갈렸고, VLM이 붙인 이름이 실제 소제목과 일치함 — `제품 소개` · `파우더룸 설문 결과` · `자외선 차단 효과 시험` · `핵심 포인트 요약` · `비건 인증` · `POINT 01 진정 징크` · `피부 진정 개선 테스트` · `논-나노 무기자차` · `POINT 02 산뜻 보송 제형` · `저자극 테스트` · `10종 히알루쿨링` · `발림성·밀착력`.

### 동료 평가

5명이 4장에서 3 variant 중 가장 나은 것을 하나씩 고름. `poc/section_split/summary.md`.

| 이미지 | `color_snap_vlm` | `color_snap_vlm2` | `color_snap_ws` |
|---|---|---|---|
| `219554_002` 무기자차 선크림 | 1 | **3** | 1 |
| `250199_014` 레티날 샷 타이트닝 부스터 | 1 | **3** | 1 |
| `213548_002` 미백 바디로션 | 3 (vlm·vlm2 결과 동일) | ← | 2 |
| `250199_008` 비타민A 레티날 | 0 | **4** | 1 |
| **결과가 갈린 3장** | 2 (13%) | **10 (67%)** | 3 (20%) |

### 블록 관통 검사

섹션별 OCR 대신 **브랜드 로고 제외 과업의 전체 OCR 캐시**(2,000px 띠 · 300px 겹침)를 읽고 `heuristic_v2`로 블록을 만들어 절단선과 대조함. 박스가 절단선 위아래로 각 3px 이상 걸칠 때 관통. 비용 0.

| variant | 절단 | 글자 관통 (region) | 문단 관통 (block) | `top_offset` 오류 |
|---|---|---|---|---|
| `color_snap_vlm` | 52 | 0 | 1 | 0 |
| **`color_snap_vlm2`** | 68 | **0** | **0** | **0** |
| `color_snap_ws` | 60 | 0 | 0 | 0 |
| `gap_major` | 41 | 0 | 1 | 0 |
| `ws_std` | 259 | 1 | 2 | 0 |
| `ws_edge` | 332 | 9 | 10 | 0 |

- `color_snap_vlm` 1건 — `219554_002` y8462. 두 줄 제목 `오직, 징크 성분으로 피부에 순한` / `민감피부 저자극 무기자차` 사이를 잘라 **한 제목이 두 섹션으로 갈림**. v2는 y8808로 피함
- **관통 검사로 안 잡히는 의미 분리 1건** — `213548_018` y629(VLM 절단, v1·v2 동일). 소제목 라벨 `RECOMMEND`와 제목 `이런 분들에게 추천해요`가 다른 섹션으로 갈림. 절단선–제목 8px로 글자는 안 자르나, 글자 크기 차이로 한 블록이 아니라 검사에 안 걸림

### 섹션 단위 OCR 입력 — 이음선 확인

`poc/ocr_split/` · 34장 · 비용 0. 분할 방식별로 OCR을 돌려 페이지 좌표로 합치고, 이음선 글자를 **그 자리가 조각 한가운데인 다른 방식** 결과와 대조함. 별도 과업으로 두지 않음.

| OCR 입력 | 최대 조각 | 잘림 | 누락 | 비고 |
|---|---|---|---|---|
| 자르지 않음 | 37,665px | — | — | `219554_002` **OCR 오류**(OpenCV 32,767px 한계). 나머지 긴 이미지도 4,000px로 축소돼 글자 **36~89%만** 찾음 |
| 2,000px 띠 · 겹침 없음 | 2,000px | 12 | 1 | 이음선 13건 중 **12건 실제 오독** — `4주 후`→`4수후` · `데일리 진정 선케어`→`고이나리나` |
| 2,000px 띠 · 300px 겹침 | 2,000px | 0 | 0 | 글자 높이 300px 이하면 구조상 안 잘림 |
| 여백 처리 단위 (`ws_std`) | 2,112px | 0 | 0 | |
| **의미 섹션 (`color_snap_vlm2`)** | 4,491px | **0** | **0** | 4,000px 초과 섹션 1개는 축소됨 — 그 섹션 안 34/37(92%) |

→ **섹션 단위로 넣으면 이음선 문제 없음.** 남는 것은 섹션 길이 상한이 없어 **4,000px 넘는 섹션이 축소되는 것**뿐 — 규칙으로 막음(9장 27번).

### 계획서 판정 기준 대비

| 기준 | 상태 |
|---|---|
| 경계마다 절단 가능한 여백이 실재하는가 | **확인** — 스냅 실패 1건, 강제 분할 0건 |
| 의미 단위 경계가 텍스트 블록을 가르지 않는가 | **확인** — 관통 0 (아래 블록 관통 검사) |
| 실패 시 단일 섹션 폴백 | 스냅 실패 시 **자르지 않음**으로 구현. 폴백 발동 1건 |
| `top_offset` 재기준화 검산 | **확인** — 연속성 · 시작점 · 크롭 높이 · 좌표 왕복 복원 모두 오류 0 |

### 실측

| variant | 호출 | 토큰 | 소요 | 비용 |
|---|---|---|---|---|
| `color_snap_vlm` (v1) | 11 | in 45,738 / out 307 | 70.0s | $0.0355 |
| `color_snap_vlm2` (v2) | 11 | in 46,541 / out 682 | 80.6s | $0.0375 |
| 로컬 4종 | — | — | 34장 3~7초 | 0 |

---

## 6. 브랜드 로고 제외 — **확정**

### 결과

| 항목 | 결정 |
|---|---|
| 채택 | **`block_exact`** — 줄·문단 병합 블록의 **텍스트 전체가 브랜드명과 같으면** 제외 |
| 위치 | 줄·문단 병합 → 제품 라벨 판정 → **로고 제외(문자열 후처리)**. 확정된 `vlm_relation` 프롬프트는 건드리지 않음 |
| 입력 | 블록 텍스트 + 상품 브랜드명 사전(영·한 표기) |
| 정규화 | NFKC · 소문자 · 공백·문장부호 제거 (`b.clinicx` → `bclinicx`) |
| 결과 | 페이지 로고 7개 중 **찾음 6 · 놓침 1 · 오탐 0** |
| 표본 | 골든 샘플 34장 · 페이지 로고 7개(5장) — 34장 전수 확인 |
| 비용 | **0.** OCR·병합은 파이프라인 공통 단계라 대조 0.25초만 추가 |
| 오탐 신호(계획 미결 10) | 오탐 0 — 신호 추가 근거 없음 |
| 탈락 | **템플릿 매칭** — 로고 파일 필요 · 34장 최대 348초 · 배율 상한으로 대형 로고 놓침 |

### 방식 전환

템플릿 매칭으로 착수함. 페이지 로고 주변을 OCR로 읽어보니 **전부 글자로 읽힘**(신뢰도 0.93~1.00) — 표본 로고가 전부 워드마크라 OCR 영역에 이미 들어 있음. **로고 파일 없이 브랜드명 대조로 대체**함.

### variant 비교

Claude 육안 판정. 패키지 위 로고는 라벨 판정 소관이라 세지 않음.

| 방식 | variant | 조건 | 찾음 | 놓침 | 오탐 | 소요 (34장) | 판단 |
|---|---|---|---|---|---|---|---|
| 텍스트 | **`block_exact`** | 블록 전체 == 브랜드명 | 6 | 1 | **0** | 0.25s | **채택** |
| 텍스트 | `line_exact` | 줄 병합 후 == 브랜드명 | 6 | 1 | 2 | 0.22s | 여러 줄 제목의 첫 줄 오탐 |
| 텍스트 | `text_exact` | OCR 한 줄 == 브랜드명 | **7** | **0** | 6 | 0.07s | 끊겨 읽힌 제목 첫 줄 오탐 |
| 텍스트 | `text_fuzzy` | 유사도 ≥ 0.8 | 7 | 0 | 9 | 0.05s | 새로 찾은 로고 없이 오탐만 늚 — 탈락 |
| 텍스트 | `text_contains` | 브랜드명 포함 | 7 | 0 | 48 | 0.03s | 본문 문장까지 제외 — 탈락 |
| 템플릿 | `template_gray_lo` | 밝기 매칭 · 임계 0.60 | 3 | 1 | 0 | 348s | 템플릿 최선 |
| 템플릿 | `template_gray` | 밝기 매칭 · 0.80 | 2 | 2 | 0 | 135s | 필기체 로고 점수 미달 |
| 템플릿 | `template_edge` | 윤곽선 · 0.45 | 2 | 2 | 0 | 182s | 밝기 매칭과 동일 — 이점 없음 |
| 템플릿 | `template_edge_lo` | 윤곽선 · 0.30 | 3 | 1 | 48 (+남의 페이지 248) | 362s | 글자 윤곽 오탐 폭증 — 탈락 |
| 템플릿 | `feature_orb` | ORB + 호모그래피 | 0 | 4 | 0 | 6s | 워드마크에서 특징점 부족 — 탈락 |

> 텍스트 소요는 대조만. OCR(2,000px 띠 · 300px 겹침) 32.6초는 공통이며 파이프라인에서 어차피 돎. 병합은 `heuristic_v2`(줄·문단 병합 1단계)만 씀.
> 템플릿 대상은 4개 — 템플릿을 잘라낸 원래 자리와 템플릿이 없는 celimax를 뺌. 같은 4개로 보면 `block_exact` 4/4 · `template_gray_lo` 3/4.

### 놓침과 오탐의 맞교환

| variant | 오류 | 사례 | 원인 |
|---|---|---|---|
| `text_exact` | 오탐 6 | `셀리맥스` \| `구매인증 페이백 3천원` · `비클리닉스` / `브라이트닝 세럼` | OCR이 제목을 끊어 읽거나 여러 줄 제목의 첫 줄이 브랜드명 |
| `block_exact` | 놓침 1 | `celimax × OLIVE YOUNG` (`250199_002`) | 공동 로고가 줄 병합으로 `celimax× OLIVE`로 묶임 |

두 오류 모두 검수 화면에 드러남 — 오탐은 한글 제목이 미번역으로 남고, 놓침은 로고가 일반 글자로 재조판됨. **빈도로 골라 `block_exact` 채택.**

> 계획서 판정 전제("실패는 확인 필요 신호로 노출")는 **템플릿 방식 기준**임. 텍스트 대조는 페이지 대부분에 로고가 없어 "브랜드명 미검출"을 실패로 볼 수 없음 → **놓침 신호 없음.** 계약 `logo_match_failed` 의미 재정의 필요(9장 23번).

### 템플릿 판정 정정

`219554_002` y36418 대형 goodal(폭 332px)을 1차 판정에서 빠뜨림. 템플릿 112px × 배율 상한 2.0배로 크기에 못 미쳐 **템플릿 5종 전부 놓침** → 놓침 +1 (`template_gray_lo` 놓침 0 → 1).

---

## 7. 검증 중 확인된 사실

### ① 과분할의 원인은 임계가 아니라 **음수 gap 하한**이었음

`4.jpg`가 두 휴리스틱 variant 공통으로 병합 C였음. 원인은 인접 글자의 bbox가 서로 파고들어(음수 간격) 병합이 거부된 것임.

| 과분할 | 실제 간격 | 허용 하한 | 차이 |
|---|---|---|---|
| `My First` \| `Collagen` (세로) | −90px | −0.3 × 274 = −82px | 8px |
| `지금부터 시작하는` \| `원이의 콜라겐` (가로) | −35px | −0.5 × 61 = −30.5px | 4.5px |

v1·v2가 조정한 것은 **양수 상한**뿐이고 음수 하한은 두 variant가 같았음. 시뮬레이션 결과 **하한을 −0.8h(가로)/−0.5h(세로)로 풀면 `4.jpg`가 3블록으로 정상화**되며, −1.0/−0.8과 결과가 같아 **그 지점이 포화점**임.

### ② LLM에 분할을 막으면 휴리스틱의 과병합이 **상속됨**

`7.jpg`의 `heuristic_v2` 과병합 1건이 `llm_assist`에 그대로 남음. 축을 분리하려고 분할을 막은 결과이며, **과병합은 복구 불가 축**이라 실서비스에서는 분할 허용 여부를 재검토해야 함.

### ③ 표본의 70%가 제품 인쇄 글자임

블록 159개 중 111개가 라벨이라 **실제 판정 대상은 48개**였음. 이미지당 판정 블록이 3~7개라 오분류 1건이 바로 C가 됨(`7.jpg` 1/2 = 50%). **이미지 단위 등급이 작은 분모에서 튐** — 전체 오분류율은 8%로 A 수준임.

### ④ 제목 판정의 이미지 전역 기준이 상세페이지와 안 맞음

글자 높이 상위 25%를 제목으로 보는데, **상세페이지는 섹션마다 위계가 리셋됨.** 위쪽 큰 배너에 제목이 몰리고 아래 섹션 소제목은 본문으로 떨어짐.

### ⑤ 라벨 판정에는 **이미지가 결정적**임

`role_ext`와 `vlm_relation`은 **같은 모델·같은 프롬프트**이고 이미지 유무만 다름. 결과는 **미탐 7 대 0**임.

미탐 7건은 전부 **`11.jpg` 한 장**에 몰림 — 마스크팩 박스 5개가 겹쳐 나열된 컷이며, 놓친 블록은 전부 왼쪽 앞 패키지 인쇄면(`MEDIHEAL`·`붙이는 콜라겐 앰플`·`#탄력리프팅#모공채움`·`한정기획 8+1 추가증정!` 등)임. 문구가 상세페이지 카피처럼 생겨 **텍스트·좌표만으로는 배경 배너와 구분되지 않음.**

→ 개발계획서 v3.4 §2.1의 "역할 분류로 판정한다"로는 미탐이 남음. **라벨 판정은 역할 분류와 별개 단계이며 이미지를 봐야 함.** v3.4 파이프라인 서술 개정 필요.

### ⑥ 모델을 올려도 결과가 같음

벤더 대비로 `claude-opus-5`를 같은 조건(이미지 동봉)에서 실행함(`vlm_opus`). **110블록 전부 `gemini-3.8-flash`와 동일 판정**임.

| variant | 모델 | 라벨 | 소요 | 비용 |
|---|---|---|---|---|
| `vlm_relation` | `gemini-3.8-flash` | 60 | 41.9s | **$0.027** |
| `vlm_opus` | `claude-opus-5` | 60 | 53.3s | **$0.213** |

8배 비싸고 느린데 결과가 같음 → **이 축에서 모델 상향은 실익 없음.** `vlm_opus`는 `summary.md`에서 제외했고 실행 기록은 `results/vlm_opus/meta.json`에 있음.

> 조건 차이 1건 — `anthropic` SDK 1.0.0이 `temperature`를 받지 않아 `vlm_opus`만 온도 미지정임. 판정이 완전 일치해 영향은 없어 보이나 재현 시 조건이 다름.

### ⑦ 정렬 판정 실패의 원인은 임계가 아니라 **비교 단위**였음

처음에는 조판 대상 110영역 중 **46건(42%)이 `불명`**이었음. 임계를 풀어야 하나 검토했으나, 원인은 **같은 줄에 나란히 있는 영역들끼리 정렬을 비교**한 것이었음 — 한 줄 안에서는 좌·중·우가 당연히 다 다르므로 어느 축도 걸리지 않음.

줄 단위로 묶어 비교하도록 고치니 **불명 0건**이 됐고, 정렬 판정이 **27% → 91%**로 올랐음.

> 여기에 `무관` 범주를 더함 — 줄 폭이 거의 같으면 어느 정렬로 그려도 결과가 같으므로 축을 고를 필요가 없음. 43건이 여기 해당함. **억지로 고르면 오판이 되고, `불명`으로 두면 정보가 없다고 오해됨.**

### ⑧ 색 반전은 "글자는 소수"라는 가정이 깨질 때 남

`dominant_color`·`contrast_split` 모두 **화소가 적은 쪽을 글자로 봄**. 굵은 대형 글자가 박스를 가득 채우면 글자가 다수가 되어 **글자색과 배경색이 뒤집힘** — 반전 6건이 그렇게 나옴.

`otsu_border`는 그 가정을 버리고 **테두리 접촉**으로 가름. bbox는 글자를 감싸는 상자라 배경이 테두리를 두름.

| 사례 | 기존 | `otsu_border` |
|---|---|---|
| `9.jpg` 역대최대 (흰 글자) | `#1E1508` 검정 | **`#F9F3D1`** ✓ |
| `11.jpg` 올리브영 (흰 글자) | `#141474` 남색 | **`#F6F7FA`** ✓ |
| `12.jpg` 콜라겐 (흰 글자) | `#FA7493` 분홍 | **`#FEFAFB`** ✓ |
| `9.jpg` +1+1 (초록 글자) | `#63F060` ✓ | **`#161102`** ✗ |

**반전 5건 해소, 1건 신규.** 신규 실패는 **글자에 검정 아웃라인이 둘러진 경우** — 아웃라인이 상자 가장자리에 닿아 배경으로 잡힘. 정상 케이스는 건드리지 않음(280건 중 6건만 변화).

남은 색 실패는 두 유형뿐임 — **아웃라인 글자**와 **원형 배지 테두리를 글자색으로 잡는 것**(`ONLY`·`50ml`). 둘 다 "글자는 단색 덩어리"라는 전제가 깨지는 경우로 원인이 같음.

### ⑨ 굵기·폰트 계열은 **지금 방식으로 안 됨**

계획서상 범위 밖(12월)이었으나 되는지만 시험함.

**굵기 — 방법 수준 실패.** 획 두께 ÷ 글자 높이로 재는데, 굵기를 아는 표본에서 bold와 regular가 역전됨.

| 표본 | 육안 | top20 평균 | top5 평균 | 최대 |
|---|---|---|---|---|
| `6.jpg` 더블 | **bold** | 0.089 | 0.102 | 0.118 |
| `8.jpg` BIODANCE'S | regular | **0.100** | 0.106 | 0.165 |

어떤 추정식·임계로도 안 갈림. **임계 조정으로 풀 문제가 아님.** 크기와의 상관도 +0.22로 약해 "큰 글자라서"도 아님.

**계열 — 변별 기회가 없었음.** 설치 폰트 14종 렌더 대조로 70건 중 69건을 고딕, 1건을 명조로 판정했고 결과는 맞음. 그러나 **표본에 명조 문구가 하나도 없어** 맞혔다기보다 틀릴 기회가 없었음. 대조 신뢰도(IoU)도 글자 수에 급락함 — 2~3자 0.509 → 13자 이상 0.206.

> 폰트 대조가 내놓는 굵기(68/70이 bold)는 **쓰면 안 됨.** 겹침 비교가 획이 두꺼운 후보에 유리해 생기는 편향이며, 획 두께 판정과 27%만 일치함.

### ⑩ VLM에게 좌표를 묻지 말고 **후보를 그려 번호를 고르게** 할 것

섹션 경계를 VLM에 맡길 때 y좌표를 추정시키지 않았음. **자를 수 있는 여백마다 번호 붙은 빨간 선을 그려** 보여주고 번호만 받음.

| 효과 | 내용 |
|---|---|
| 좌표 오차 | **없음** — VLM의 픽셀 좌표 추정은 부정확함 |
| 글자 관통 | **구조적으로 불가** — 여백이 아닌 곳은 후보에 없음 |
| 스냅 실패 | **0** — 이미 스냅된 후보에서 고르므로 |

### ⑪ VLM 분할의 입도는 **프롬프트 한 문장**이 좌우함

v1과 v2는 모델·입력·후보가 같고 **프롬프트만** 다름.

| | v1 | v2 |
|---|---|---|
| 지시 | "한 섹션은 화면 여러 장 분량. **잘게 나누지 않는다**" | "**큰 주제가 같아도 소제목으로 새 논점이 시작되면 나눈다**" |
| VLM 절단 | 15 | **31** |
| 최대 섹션 | 7,521px | 4,491px |

→ 채택 입도를 정하는 것은 모델 성능이 아니라 **요구 정의**임. 하류가 소제목 단위를 원하는지 큰 문맥 단위를 원하는지가 먼저 정해져야 함.

> 재발한 버그 — 캐시 키에 **프롬프트 지문이 없어** 프롬프트를 고쳐도 옛 응답을 재사용할 뻔함. 번역 과업에서 한 번 겪은 것과 같음. SHA-256 지문을 넣어 고침.

---

## 8. 표본 한계 — 종결 조건

**가격·주의문구가 표본에 각 1건뿐이라 5종 중 2종이 사실상 미검증임.** 판정 대상 48블록 중 라벨이 70%를 차지한 결과임.

`캡션↔본문` 혼동은 개발 중 조정으로 덮이지만, **주의문구 미탐은 규제 판정이 통째로 빠지는 복구 불가 축**이라 덮이지 않음.

→ **조건부 종결.** 골든 샘플(초장축 1장 포함) 확보 시 **같은 코드로 재실행**해 확인함. 추가 개발 없이 약 20분 소요.

> **골든 샘플 확보됨(2026-09-09)** — 3상품 34장. 위 과업들의 재실행은 아직 안 함.

### 제품 라벨 판정

| # | 한계 |
|---|---|
| 1 | **정답지가 채택 후보 자신임**(2장). 교차 검증 없음 |
| 2 | **결정적 실패 유형의 표본이 1장뿐** — 겹친 패키지 컷은 `11.jpg` 하나이고 미탐 7건이 전부 거기서 나옴. 유형이 늘면 `vlm_relation`도 미탐이 날 수 있음 |
| 3 | 라벨이 없는 이미지는 `4.jpg` 1장뿐 — **오탐 검증 표본이 얇음** |

→ 같은 조건부 종결. 골든 샘플에 **겹친 패키지 컷과 라벨 없는 컷**이 들어가야 함(계획 문서 미결 9와 함께 요청).

### 번역 길이 팽창률

| # | 한계 |
|---|---|
| 1 | **ko → en 한 방향만 측정.** 다른 언어는 팽창률이 다름 |
| 2 | 폰트를 **Arial로 가정**함. 실물 폰트가 정해지면 재측정 필요 |
| 3 | 규제 표가 더미라 대체표현 길이가 결과에 섞임 |
| 4 | 표본 문구가 짧음(`block` 평균 27.5자). 긴 본문 위주 페이지에서는 팽창률이 다를 수 있음 |

### 스타일 추출

| # | 한계 |
|---|---|
| 1 | **블록 대표값 미산출.** 색·크기는 region 단위인데 조판 계약은 block 단위 `font_color`·`font_size`를 요구함. 첫 줄 값·최빈값·면적 가중 중 무엇을 쓸지 미정 |
| 2 | 정렬 판정 대상이 **23건뿐**(나머지는 단일행 44 · 무관 43). 표본이 넓어지면 오판이 더 나올 수 있음 |
| 3 | 굵기·계열은 **범위 밖 확인 결과 불가**. 다른 접근 필요 |
| 4 | 명조 문구가 표본에 없어 계열 변별 미검증 |

### 섹션 분해

| # | 한계 |
|---|---|
| 1 | **표본이 3상품뿐.** 배경색을 섹션마다 바꾸지 않는 페이지는 ③(VLM)에 더 기댐 |
| 2 | 동료 평가는 **4장**뿐 — 34장 전체 육안 판정은 안 함. 의미 단위의 정답은 없음 |
| 3 | 관통 검사는 **전체 페이지 띠 OCR** 기준 — 섹션별 OCR과 검출 조건이 다름. 블록은 `heuristic_v2`만 씀 |
| 4 | **소제목 라벨–제목 분리 같은 의미 분리는 관통 검사로 안 잡힘** — 1건 확인 |

### 브랜드 로고 제외

| # | 한계 |
|---|---|
| 1 | **페이지 로고 7개(5장)뿐.** 판정은 Claude 육안 판정 |
| 2 | **심볼형 로고가 표본에 없음** — 전부 워드마크. 글자가 아닌 로고는 텍스트 대조로 못 잡음 |
| 3 | 병합은 1단계 `heuristic_v2`만 씀. 확정안의 **LLM 보정이 로고를 옆 글자와 더 묶으면 놓침이 늘 수 있음** |
| 4 | 브랜드명 사전은 가정 — 페이지에서 확인한 표기 |
| 5 | 공동 로고 사례 1건뿐 |

---

## 9. 개발 이관 항목

검증에서 근거가 나왔으나 PoC 범위 밖인 것. **개발 단계에서 조정함.**

| # | 항목 | 근거 |
|---|---|---|
| 1 | 음수 gap 하한을 **−0.8h(가로) / −0.5h(세로)**로 완화 검토 | 7장 ①. −1.0/−0.8과 결과 동일해 포화점 |
| 2 | 제목 판정을 **섹션 단위 상대 기준**으로 변경 | 7장 ④ |
| 3 | LLM에 **분할 허용 여부** 재검토 | 7장 ② |
| 4 | 성분 목록을 `role` 유형으로 둘지 결정 | `8.jpg` 성분 나열이 `본문`으로 분류됨 |
| 5 | 판촉 표기(`1+1`·`8+1`)를 담을 유형 검토 | 국가별 관행 표기가 달라 현지화 대상일 수 있음 |
| 6 | **라벨 판정 시 bbox를 이미지 축소 비율에 맞춰 스케일해 보낼 것** | 현재는 원본 좌표 + 1024px 축소 이미지를 함께 보내 모델이 환산해야 함. 12장 중 7장이 축소 대상이었음 |
| 7 | LLM 판정에 **신뢰도 출력을 요구할지** 검토 | 참/거짓만 와서 경계선이 안 보임. 틀리면 미세 조정이 아니라 프롬프트 재설계가 필요 |
| 8 | `is_product_label` → 계약 `is_excluded` 매핑 | 계획 문서 §2. 블록 제외 필드는 단일화돼 있음 |
| 9 | **폰트 축소 상한은 80% 유지 권고** | 60·50%까지 열어도 흡수는 12·30%뿐인데 가독성만 잃음. 3장 |
| 10 | **초과 블록을 검수 화면에 플래그로 노출할 것** | 초과율 94%가 검수로 넘어감. `auto_shrunk`만으로는 부족 — 프론트·백엔드 인계 필요 |
| 11 | 검수 화면에 **문구 직접 수정** 기능 필요 | 조판 단독 채택의 전제. 이것이 없으면 초과가 그대로 남음 |
| 12 | 실물 폰트 확정 후 **폭 재측정** | Arial 가정. 측정 코드는 상수만 바꾸면 재실행됨 |
| 13 | **블록 대표 스타일 산출 규칙** 결정 | 스타일은 region 단위, 계약은 block 단위. 8장 |
| 14 | 아웃라인·배지 테두리 글자 대응 | 남은 색 실패 2유형의 공통 원인. 7장 ⑧ |
| 15 | 정렬 `무관`을 계약에서 어떻게 표현할지 | 43건이 해당. 기본값으로 접을지 별도 값으로 둘지 |
| 16 | ~~섹션 입도 결정~~ — **소제목 단위로 확정**(동료 투표). 하류 요구가 다르면 프롬프트 한 문장으로 조정 | VLM 프롬프트 한 문장으로 15절단 ↔ 31절단이 갈림. 7장 ⑪ |
| 17 | 사진 배경 섹션의 경계 처리 | 여백·색 경계선이 모두 없어 스냅 실패. 현재는 자르지 않음 |
| 18 | 섹션 입력을 VLM에 줄 때 **후보선 번호 방식** 유지 | 좌표 추정 오차·글자 관통이 구조적으로 없음. 7장 ⑩ |
| 19 | **LLM 보정 블록 기준으로 로고 제외 재확인** | 6장. 1단계 병합만으로 확인함 |
| 20 | **공동 로고 처리** — 제휴처 이름 사전 / 블록 내 토큰 대조 | `celimax × OLIVE YOUNG` 놓침. 사례 1건이라 PoC에서 규칙화하지 않음 |
| 21 | **브랜드명 사전 출처** — 상품 메타의 영·한·약칭 표기 | PoC 사전은 페이지에서 확인한 가정 |
| 22 | **제품명 제외 범위** 결정 | 본문 속 제품명 번역 여부 — 기획 결정 |
| 23 | 계약 `logo_match_failed` **의미 재정의 또는 제거** | 텍스트 대조는 로고 파일 매칭이 아니며 놓침 신호가 없음 — AI→BE |
| 24 | 심볼형 로고가 필요해지면 템플릿 매칭 보조 + **로고 파일은 SVG로 받음** | 벡터라 배율 상한 없음 · 투명 배경을 마스크로 씀. 업로드 시 스크립트·외부 참조 정리 필요 |
| 25 | VLM 보조 분해 프롬프트에 **소제목 라벨을 제목과 떼지 말 것** 추가 | `213548_018` `RECOMMEND` / `이런 분들에게 추천해요` 분리. 5장 |
| 26 | 섹션 OCR 후 **크롭 위아래 경계에 닿은 글자 박스**를 관통 의심 신호로 쓸지 검토 | PoC는 전체 OCR로 검사했으나 실서비스는 섹션 단위 OCR만 돎. 5장 |
| 27 | 섹션이 **4,000px를 넘으면 OCR 전에 추가 분할** — 여백 우선, 없으면 **300px 겹침 띠** | 검출기 긴 변 상한 4,000px. 겹침 없이 자르면 이음선 글자 13건 중 12건 오독. 5장 |

---

## 10. 재현

```bash
# 줄·문단 병합 + 역할 분류
python poc/block_role/run.py --variant all      # 휴리스틱 2종
python poc/block_role/run_llm.py                # llm_assist (캐시 우선)
python poc/block_role/compare.py
python poc/block_role/review.py

# 제품 라벨 판정
python poc/product_label/run.py --variant all   # 로컬 3종
python poc/product_label/run_llm.py --variant all
python poc/product_label/compare.py
python poc/product_label/review.py              # variant 나란히 보기

# 번역 길이 팽창률
python poc/length_expansion/run_llm.py --input all          # 번역 (유료)
python poc/length_expansion/run_llm.py --input block --prompt compress
python poc/length_expansion/run.py --variant all            # 폭 측정 (로컬)
python poc/length_expansion/compare.py

# 스타일 추출
python poc/style_extract/run.py --variant all        # 색·크기·정렬 3종
python poc/style_extract/run_font.py --variant all   # 굵기·계열 (범위 밖 확인)
python poc/style_extract/compare.py

# 섹션 분해 (골든 샘플)
python poc/section_split/run.py --variant all             # ws_std · ws_edge · gap_major
python poc/section_split/run_color.py                     # color_snap_ws (①② + 후보 1)
python poc/section_split/run_color_vlm.py --prompt v2     # color_snap_vlm2 (①② + 후보 4)
python poc/section_split/review.py --variant color_snap_vlm2   # 판정용 대지
python poc/section_split/check_cut.py                    # 블록 관통 · top_offset 검산 — logo_match run_text.py OCR 캐시 필요
python poc/ocr_split/run.py --variant all                # 섹션 단위 OCR 입력 — 분할 방식 5종
python poc/ocr_split/compare.py

# 브랜드 로고 제외 (골든 샘플)
python poc/logo_match/run.py --variant all        # 템플릿 매칭 5종 (로컬)
python poc/logo_match/run_text.py --variant all   # 텍스트 대조 5종 — block_exact (OCR 캐시)
python poc/logo_match/compare.py
```

API 응답은 각 과업의 `cache/` 에 있음. 재실행 시 추가 과금 없음.

---

## 11. 지출

| 과업 | 실비 |
|---|---|
| 줄·문단 병합 + 역할 분류 | **$0.0231** (`llm_assist` 12장) |
| 제품 라벨 판정 | **$0.2568** — `role_ext` 0.0170 / `vlm_relation` 0.0270 / `vlm_opus` 0.2128 |
| 스타일 추출 | **0** (전부 로컬) |
| 섹션 분해 | **$0.0730** — `color_snap_vlm` 0.0355 / `color_snap_vlm2` 0.0375 |
| 번역 길이 팽창률 | **$0.0517** — `block` 0.0138 / `region` 0.0211 / `block_compress` 0.0168 |
| 브랜드 로고 제외 | **0** (전부 로컬) |

**합계 $0.41.** 이 중 $0.21이 `vlm_opus` 벤더 대비 1회이며, 결과가 동일해 실서비스에는 쓰지 않음.
